<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>ocman – Documentation</title><link>https://ocman.stenic.io/docs/</link><description>Recent content in Documentation on ocman</description><generator>Hugo -- gohugo.io</generator><language>en</language><atom:link href="https://ocman.stenic.io/docs/index.xml" rel="self" type="application/rss+xml"/><item><title>Multi-remote</title><link>https://ocman.stenic.io/docs/features/multi-remote/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://ocman.stenic.io/docs/features/multi-remote/</guid><description>
&lt;p&gt;Manage coding-agent sessions running on several machines from one ocman
dashboard. One ocman acts as the hub. It attaches to other ocman instances
(the remotes) over a long-lived gRPC connection and shows every machine&amp;rsquo;s
sessions in a single list. You can open, watch live, and drive any remote
session, sending prompts, answering permission and question prompts, aborting
and compacting, exactly as if it were local. Creating a session is
machine-aware: the hub picks the host where the project already lives, and
asks you to choose when it lives on more than one.&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;Off by default. A plain &lt;code&gt;./ocman&lt;/code&gt; with no remotes behaves exactly as
before, and nothing below is required for single-machine use.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2&gt;How it works&lt;span class="hx:absolute hx:-mt-20" id="how-it-works"&gt;&lt;/span&gt;
&lt;a href="#how-it-works" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Every ocman has a stable random instance ID and a remote-access token,
generated on first run and stored in &lt;code&gt;state.db&lt;/code&gt;. A remote opts in to being
managed by starting its gRPC server with &lt;code&gt;-remote-listen&lt;/code&gt;. The hub dials each
remote, authenticates with that remote&amp;rsquo;s token, and registers its sessions
under a host badge.&lt;/p&gt;
&lt;p&gt;The browser only ever talks to the hub over the normal REST/SSE API, and the
hub fans out to remotes over gRPC. Host-local work (tmux, git worktrees, the
in-app terminal, the agent process) always runs on the machine that owns it,
so the hub never reaches into a remote&amp;rsquo;s filesystem directly. Opening the
terminal on a remote project&amp;rsquo;s session opens a shell on the remote machine:
the hub tunnels the browser WebSocket to the owner over a bidirectional gRPC
stream, so keystrokes and PTY output cross the wire while the shell, cwd and
tmux windows all stay on the remote.&lt;/p&gt;
&lt;h2&gt;Prerequisites&lt;span class="hx:absolute hx:-mt-20" id="prerequisites"&gt;&lt;/span&gt;
&lt;a href="#prerequisites" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;ul&gt;
&lt;li&gt;ocman running on each machine you want to manage. The same version is
safest; a protocol-version handshake flags incompatible builds.&lt;/li&gt;
&lt;li&gt;Network reachability from the hub to each remote&amp;rsquo;s gRPC port. ocman does
not manage networking, so use direct reachability, a VPN, or a trusted
overlay such as Tailscale or WireGuard.&lt;/li&gt;
&lt;li&gt;TLS certificates, unless both machines sit on a trusted overlay.&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Step-by-step setup&lt;span class="hx:absolute hx:-mt-20" id="step-by-step-setup"&gt;&lt;/span&gt;
&lt;a href="#step-by-step-setup" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;h3&gt;1. On each remote machine, turn on remote access&lt;span class="hx:absolute hx:-mt-20" id="1-on-each-remote-machine-turn-on-remote-access"&gt;&lt;/span&gt;
&lt;a href="#1-on-each-remote-machine-turn-on-remote-access" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h3&gt;&lt;p&gt;Start ocman with &lt;code&gt;-remote-listen&lt;/code&gt; set to the address its gRPC server
should bind. Use a routable address (not &lt;code&gt;127.0.0.1&lt;/code&gt;) so the hub can
reach it across the network:&lt;/p&gt;
&lt;div class="hextra-code-block hx:relative hx:mt-6 hx:first:mt-0 hx:group/code"&gt;
&lt;div&gt;&lt;div class="highlight"&gt;&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-sh" data-lang="sh"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="c1"&gt;# On the workstation you want to manage remotely:&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;ocman -remote-listen 0.0.0.0:8230 &lt;span class="se"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; -remote-tls-cert cert.pem -remote-tls-key key.pem&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class="hextra-code-copy-btn-container hx:opacity-0 hx:transition hx:group-hover/code:opacity-100 hx:flex hx:gap-1 hx:absolute hx:m-[11px] hx:right-0 hx:top-0"&gt;
&lt;button
class="hextra-code-copy-btn hx:group/copybtn hx:cursor-pointer hx:transition-all hx:active:opacity-50 hx:bg-primary-700/5 hx:border hx:border-black/5 hx:text-gray-600 hx:hover:text-gray-900 hx:rounded-md hx:p-1.5 hx:dark:bg-primary-300/10 hx:dark:border-white/10 hx:dark:text-gray-400 hx:dark:hover:text-gray-50"
title="Copy code"
aria-label="Copy code"
data-copied-label="Copied!"
&gt;
&lt;div class="hextra-copy-icon hx:group-[.copied]/copybtn:hidden hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;div class="hextra-success-icon hx:hidden hx:group-[.copied]/copybtn:block hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;/button&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;p&gt;This starts the gRPC server in addition to the normal web UI on &lt;code&gt;:8228&lt;/code&gt;. The
remote&amp;rsquo;s existing REST behaviour and localhost posture are unchanged.&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;Pick any free port for the gRPC server; &lt;code&gt;8230&lt;/code&gt; is just a convention.
The web UI port (&lt;code&gt;-addr&lt;/code&gt;, default &lt;code&gt;:8228&lt;/code&gt;) is independent.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h3&gt;2. On each remote machine, copy its access token&lt;span class="hx:absolute hx:-mt-20" id="2-on-each-remote-machine-copy-its-access-token"&gt;&lt;/span&gt;
&lt;a href="#2-on-each-remote-machine-copy-its-access-token" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h3&gt;&lt;p&gt;Open the remote&amp;rsquo;s own dashboard and go to &lt;strong&gt;Settings → Remotes&lt;/strong&gt;. The
&lt;strong&gt;This machine&lt;/strong&gt; entry shows the remote&amp;rsquo;s instance ID and listen address.
Click &lt;strong&gt;Reveal token&lt;/strong&gt; and copy the value.&lt;/p&gt;
&lt;p&gt;You can also fetch it over the API on that machine:&lt;/p&gt;
&lt;div class="hextra-code-block hx:relative hx:mt-6 hx:first:mt-0 hx:group/code"&gt;
&lt;div&gt;&lt;div class="highlight"&gt;&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-sh" data-lang="sh"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;curl -s -X POST http://localhost:8228/api/settings/remote-access/reveal-token
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="c1"&gt;# -&amp;gt; { &amp;#34;token&amp;#34;: &amp;#34;…&amp;#34; }&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class="hextra-code-copy-btn-container hx:opacity-0 hx:transition hx:group-hover/code:opacity-100 hx:flex hx:gap-1 hx:absolute hx:m-[11px] hx:right-0 hx:top-0"&gt;
&lt;button
class="hextra-code-copy-btn hx:group/copybtn hx:cursor-pointer hx:transition-all hx:active:opacity-50 hx:bg-primary-700/5 hx:border hx:border-black/5 hx:text-gray-600 hx:hover:text-gray-900 hx:rounded-md hx:p-1.5 hx:dark:bg-primary-300/10 hx:dark:border-white/10 hx:dark:text-gray-400 hx:dark:hover:text-gray-50"
title="Copy code"
aria-label="Copy code"
data-copied-label="Copied!"
&gt;
&lt;div class="hextra-copy-icon hx:group-[.copied]/copybtn:hidden hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;div class="hextra-success-icon hx:hidden hx:group-[.copied]/copybtn:block hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;/button&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;p&gt;The token authenticates inbound gRPC connections. It is independent of
the web UI password (&lt;code&gt;OCMAN_AUTH_PASSWORD&lt;/code&gt;).&lt;/p&gt;
&lt;h3&gt;3. On the hub, attach the remote&lt;span class="hx:absolute hx:-mt-20" id="3-on-the-hub-attach-the-remote"&gt;&lt;/span&gt;
&lt;a href="#3-on-the-hub-attach-the-remote" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h3&gt;&lt;p&gt;Open the hub&amp;rsquo;s dashboard, go to &lt;strong&gt;Settings → Remotes → Attach a remote&lt;/strong&gt;,
and enter:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Address.&lt;/strong&gt; Use &lt;code&gt;grpcs://workstation.lan:8230&lt;/code&gt; to dial TLS. Plaintext
requires an explicit &lt;code&gt;grpc://&lt;/code&gt; address and &lt;code&gt;-remote-trusted-overlay&lt;/code&gt; on
the remote; use it only over Tailscale, WireGuard, or an equivalent private
encrypted overlay.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Token.&lt;/strong&gt; The value you revealed in step 2.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Display name&lt;/strong&gt; (optional). A friendly label for the host badge.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Click &lt;strong&gt;Add remote&lt;/strong&gt;. The hub dials, runs the handshake, and the row
flips to &lt;strong&gt;connected&lt;/strong&gt;. The remote&amp;rsquo;s sessions now appear in the unified
list with its host badge.&lt;/p&gt;
&lt;h3&gt;4. Use it&lt;span class="hx:absolute hx:-mt-20" id="4-use-it"&gt;&lt;/span&gt;
&lt;a href="#4-use-it" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Unified list.&lt;/strong&gt; Local and remote sessions are interleaved, each tagged
with the owning machine. A slow or offline remote never blocks the list; its
last-known sessions show dimmed with an &lt;em&gt;offline&lt;/em&gt; marker.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Open and drive.&lt;/strong&gt; Click any remote session to stream its transcript and
use the composer, permission replies, abort and compact. These route to the
owning remote and run there.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;New session.&lt;/strong&gt; When you start a session for a project, the hub looks up
which machines already have that project checked out. On exactly one
machine it starts there automatically; on several it asks you to pick; on
none it asks which machine to start on.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;PRs and issues.&lt;/strong&gt; The project sidebar sends the owning &lt;code&gt;remoteId&lt;/code&gt; for
upstream detection, lists, checks, identity lookups, branch highlighting,
and session/worktree launches. Repository work runs on the owner.&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Managing remotes&lt;span class="hx:absolute hx:-mt-20" id="managing-remotes"&gt;&lt;/span&gt;
&lt;a href="#managing-remotes" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;The hub&amp;rsquo;s &lt;strong&gt;Settings → Remotes&lt;/strong&gt; page lists each attached remote with
its health, hostname, reported instance ID, session count, and last-seen
time. Per-remote actions:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Reconnect.&lt;/strong&gt; Force a fresh dial. This also happens automatically with
backoff after a transient drop.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Edit.&lt;/strong&gt; Change the display name or address, replace the token, or
enable and disable the remote.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Remove.&lt;/strong&gt; Detach the remote. Its sessions disappear from the list.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;The &lt;strong&gt;This machine&lt;/strong&gt; entry is non-removable, and its address and token
cannot be edited.&lt;/p&gt;
&lt;h2&gt;Security&lt;span class="hx:absolute hx:-mt-20" id="security"&gt;&lt;/span&gt;
&lt;a href="#security" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Token auth is mandatory&lt;/strong&gt; on the gRPC channel. A missing or wrong
token is rejected and shown as &lt;code&gt;auth-failed&lt;/code&gt; on the hub.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;TLS is the default requirement.&lt;/strong&gt; Start the remote with both
&lt;code&gt;-remote-tls-cert&lt;/code&gt; and &lt;code&gt;-remote-tls-key&lt;/code&gt;, and use a &lt;code&gt;grpcs://&lt;/code&gt; address on
the hub. Plaintext requires both &lt;code&gt;-remote-trusted-overlay&lt;/code&gt; on the remote
and an explicit &lt;code&gt;grpc://&lt;/code&gt; address on the hub; bare addresses are rejected.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Plaintext exposes the bearer token.&lt;/strong&gt; With &lt;code&gt;grpc://&lt;/code&gt;, the remote-access
token and all session traffic are unencrypted. Use this mode only when an
encrypted trusted overlay such as Tailscale or WireGuard already protects
the complete route between hub and remote. A private LAN alone is not an
encrypted trusted overlay.&lt;/li&gt;
&lt;li&gt;Stored remote tokens on the hub are encrypted at rest with an app-local
key and are never returned to the browser. That protects against casual
disclosure, not against an attacker who can read both the state DB and the
app-local secret. Ocman enforces owner-only filesystem permissions on these
files at startup.&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Flags&lt;span class="hx:absolute hx:-mt-20" id="flags"&gt;&lt;/span&gt;
&lt;a href="#flags" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Flag&lt;/th&gt;
&lt;th&gt;Default&lt;/th&gt;
&lt;th&gt;Where&lt;/th&gt;
&lt;th&gt;Description&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;-remote-listen&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;&lt;em&gt;(unset, off)&lt;/em&gt;&lt;/td&gt;
&lt;td&gt;remote&lt;/td&gt;
&lt;td&gt;Bind address for the remote-access gRPC server, e.g. &lt;code&gt;0.0.0.0:8230&lt;/code&gt;. Empty disables it.&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;-remote-tls-cert&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;&lt;em&gt;(unset)&lt;/em&gt;&lt;/td&gt;
&lt;td&gt;remote&lt;/td&gt;
&lt;td&gt;TLS certificate file. Required with &lt;code&gt;-remote-tls-key&lt;/code&gt; unless using a trusted overlay.&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;-remote-tls-key&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;&lt;em&gt;(unset)&lt;/em&gt;&lt;/td&gt;
&lt;td&gt;remote&lt;/td&gt;
&lt;td&gt;TLS key file.&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;-remote-trusted-overlay&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;&lt;code&gt;false&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;remote&lt;/td&gt;
&lt;td&gt;Explicitly allow plaintext only on a trusted overlay network.&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;The hub needs no flags. You add remotes at runtime from the Settings page,
and ocman persists them in &lt;code&gt;state.db&lt;/code&gt;.&lt;/p&gt;
&lt;h2&gt;Troubleshooting&lt;span class="hx:absolute hx:-mt-20" id="troubleshooting"&gt;&lt;/span&gt;
&lt;a href="#troubleshooting" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Symptom&lt;/th&gt;
&lt;th&gt;Likely cause&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;Health shows &lt;code&gt;auth-failed&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;Wrong token, or the remote regenerated its token. Reveal it again on the remote and update it on the hub (&lt;strong&gt;Edit&lt;/strong&gt;).&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Health shows &lt;code&gt;offline&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;The hub can&amp;rsquo;t reach the remote&amp;rsquo;s gRPC address. Check the port, firewall, and overlay network. Sessions appear stale until it reconnects.&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Health shows &lt;code&gt;incompatible-version&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;Hub and remote are different ocman builds with an incompatible wire protocol. Align versions.&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Remote connected but no sessions&lt;/td&gt;
&lt;td&gt;The remote may simply have none, or its OpenCode database isn&amp;rsquo;t where it expects. Check the remote&amp;rsquo;s own dashboard.&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;New-session picker never finds a remote&amp;rsquo;s project&lt;/td&gt;
&lt;td&gt;The remote&amp;rsquo;s project inventory refreshes on connect and periodically, so a freshly checked-out project may take a moment to appear.&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;h2&gt;Limitations (v1)&lt;span class="hx:absolute hx:-mt-20" id="limitations-v1"&gt;&lt;/span&gt;
&lt;a href="#limitations-v1" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;ul&gt;
&lt;li&gt;Auto-approve for a remote session runs on the owning remote, with that
machine&amp;rsquo;s settings, not the hub&amp;rsquo;s.&lt;/li&gt;
&lt;li&gt;Stale sessions from an offline remote are kept in memory only. After a hub
restart an offline remote shows no sessions until it reconnects.&lt;/li&gt;
&lt;/ul&gt;</description></item><item><title>MCP server</title><link>https://ocman.stenic.io/docs/features/mcp/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://ocman.stenic.io/docs/features/mcp/</guid><description>
&lt;p&gt;Ocman embeds an optional MCP (Model Context Protocol) server exposing Factory,
Inbox, routine, session inspection and creation, and file-embedding tools.&lt;/p&gt;
&lt;p&gt;Ocman works fine as a plain dashboard without this. Install it only if you
want conversational Factory handoff, Inbox delivery, routine management,
session inspection, or embedded file display.&lt;/p&gt;
&lt;h2&gt;Endpoint&lt;span class="hx:absolute hx:-mt-20" id="endpoint"&gt;&lt;/span&gt;
&lt;a href="#endpoint" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;The server uses the Streamable HTTP transport and listens on its own
loopback-only port, separate from the web UI:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;http://127.0.0.1:8227/mcp&lt;/code&gt; is the dedicated MCP listener (&lt;code&gt;-mcp-addr&lt;/code&gt;).
It works the same in dev and production, and needs no credentials.&lt;/li&gt;
&lt;li&gt;&lt;code&gt;http://localhost:8228/mcp&lt;/code&gt; is the same endpoint on the web UI&amp;rsquo;s port,
both in dev (Vite proxies it to the backend on &lt;code&gt;:8229&lt;/code&gt;) and for the
production binary&amp;rsquo;s default &lt;code&gt;-addr&lt;/code&gt;. Password auth applies there, so a
native MCP client gets &lt;code&gt;403&lt;/code&gt; when auth is configured.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;code&gt;/api/capabilities&lt;/code&gt; also reports the recommended URL as &lt;code&gt;mcpServer.url&lt;/code&gt;. Both
paths are localhost-only. Origin-less native MCP clients work,
cross-origin browser requests are rejected.&lt;/p&gt;
&lt;p&gt;The dedicated listener exists because MCP clients cannot present an auth
cookie, so it has to treat the loopback peer address as the credential. That
is unsafe on the web UI&amp;rsquo;s port, where every request forwarded by a reverse
proxy arrives from &lt;code&gt;127.0.0.1&lt;/code&gt;. Binding a separate loopback-only listener
keeps it out of reach of a proxy pointed at the main port. Ocman refuses to
bind &lt;code&gt;-mcp-addr&lt;/code&gt; to a non-loopback address, and &lt;code&gt;-mcp-addr &amp;quot;&amp;quot;&lt;/code&gt; disables the
dedicated listener entirely.&lt;/p&gt;
&lt;h2&gt;Setup&lt;span class="hx:absolute hx:-mt-20" id="setup"&gt;&lt;/span&gt;
&lt;a href="#setup" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Ocman checks OpenCode&amp;rsquo;s global config on every page load and, if it doesn&amp;rsquo;t
find its own entry, shows a toast offering to install it. Clicking &lt;strong&gt;Install&lt;/strong&gt;
writes the entry below into &lt;code&gt;~/.config/opencode/opencode.json&lt;/code&gt;
(&lt;code&gt;$XDG_CONFIG_HOME&lt;/code&gt; and &lt;code&gt;OPENCODE_CONFIG&lt;/code&gt; are honoured), after copying the
original to &lt;code&gt;opencode.&amp;lt;timestamp&amp;gt;-backup.json&lt;/code&gt; in the same directory. Every
other key in the file survives, including any other MCP servers. Restart
OpenCode afterwards, since it reads the config at startup.&lt;/p&gt;
&lt;p&gt;Ocman won&amp;rsquo;t touch a config it can&amp;rsquo;t rewrite losslessly, meaning a &lt;code&gt;.jsonc&lt;/code&gt;
file or a &lt;code&gt;.json&lt;/code&gt; file with comments. The toast then shows the URL to paste in
yourself. It also offers to update a stale entry, say one still pointing at an
older port. &lt;code&gt;GET /api/mcp/config&lt;/code&gt; returns the same information.&lt;/p&gt;
&lt;p&gt;To do it by hand, add the server to your project&amp;rsquo;s &lt;code&gt;opencode.json&lt;/code&gt; or the
global config:&lt;/p&gt;
&lt;div class="hextra-code-block hx:relative hx:mt-6 hx:first:mt-0 hx:group/code"&gt;
&lt;div&gt;&lt;div class="highlight"&gt;&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-json" data-lang="json"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="p"&gt;{&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; &lt;span class="nt"&gt;&amp;#34;$schema&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt; &lt;span class="s2"&gt;&amp;#34;https://opencode.ai/config.json&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; &lt;span class="nt"&gt;&amp;#34;mcp&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt; &lt;span class="p"&gt;{&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; &lt;span class="nt"&gt;&amp;#34;ocman&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt; &lt;span class="p"&gt;{&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; &lt;span class="nt"&gt;&amp;#34;type&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt; &lt;span class="s2"&gt;&amp;#34;remote&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; &lt;span class="nt"&gt;&amp;#34;url&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt; &lt;span class="s2"&gt;&amp;#34;http://127.0.0.1:8227/mcp&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; &lt;span class="nt"&gt;&amp;#34;enabled&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt; &lt;span class="kc"&gt;true&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; &lt;span class="p"&gt;}&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; &lt;span class="p"&gt;}&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="p"&gt;}&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class="hextra-code-copy-btn-container hx:opacity-0 hx:transition hx:group-hover/code:opacity-100 hx:flex hx:gap-1 hx:absolute hx:m-[11px] hx:right-0 hx:top-0"&gt;
&lt;button
class="hextra-code-copy-btn hx:group/copybtn hx:cursor-pointer hx:transition-all hx:active:opacity-50 hx:bg-primary-700/5 hx:border hx:border-black/5 hx:text-gray-600 hx:hover:text-gray-900 hx:rounded-md hx:p-1.5 hx:dark:bg-primary-300/10 hx:dark:border-white/10 hx:dark:text-gray-400 hx:dark:hover:text-gray-50"
title="Copy code"
aria-label="Copy code"
data-copied-label="Copied!"
&gt;
&lt;div class="hextra-copy-icon hx:group-[.copied]/copybtn:hidden hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;div class="hextra-success-icon hx:hidden hx:group-[.copied]/copybtn:block hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;/button&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;p&gt;The MCP port is fixed, so this config works for both &lt;code&gt;make dev&lt;/code&gt; and the
production binary. Change it if you moved the listener with &lt;code&gt;-mcp-addr&lt;/code&gt;.&lt;/p&gt;
&lt;h2&gt;Tools&lt;span class="hx:absolute hx:-mt-20" id="tools"&gt;&lt;/span&gt;
&lt;a href="#tools" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Tool&lt;/th&gt;
&lt;th&gt;Description&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;factory&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;Native Factory control surface. Use &lt;code&gt;action: &amp;quot;help&amp;quot;&lt;/code&gt; for actions, validation, examples, output schemas, and domain errors. Formula actions accept TOML only. Implementation Issues run sequentially on a shared branch; &lt;code&gt;complete_attempt&lt;/code&gt; records a verified clean, pushed commit checkpoint without &lt;code&gt;pr_url&lt;/code&gt;. A separate final delivery Issue creates or reuses the review-ready PR and completes with &lt;code&gt;pr_url&lt;/code&gt;.&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;factory_unblock&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;Executes a user-approved &lt;code&gt;reopen&lt;/code&gt; or typed &lt;code&gt;mutate_graph&lt;/code&gt; action proposed by a read-only Factory unblock session. Ocman configures this tool as &lt;code&gt;ask&lt;/code&gt;, so OpenCode shows Allow and Reject buttons before execution.&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;inbox&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;Send owner-local Inbox items and recall them by opaque ID. Unknown and already recalled IDs are successful no-ops. Use &lt;code&gt;action: &amp;quot;help&amp;quot;&lt;/code&gt; for schemas and examples. Agents should send only asynchronous completions needing attention, blocked decisions, or important failures, not routine progress.&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;routines&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;Create, inspect, update, run, and soft-delete routines. Use &lt;code&gt;action: &amp;quot;help&amp;quot;&lt;/code&gt; for current inputs, examples, output schemas, and domain errors.&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;sessions&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;Session listing, search, detail inspection, and creation. Use &lt;code&gt;action: &amp;quot;help&amp;quot;&lt;/code&gt; for schemas and examples. &lt;code&gt;list&lt;/code&gt;, &lt;code&gt;search&lt;/code&gt;, and &lt;code&gt;get&lt;/code&gt; are read-only; search matches recent session IDs, titles, directories, platforms, and host names. With &lt;code&gt;content: true&lt;/code&gt; it also scans user and assistant message text (not tool output) from the last &lt;code&gt;since_days&lt;/code&gt; (default 7). Each hit returns up to five &lt;code&gt;{partId, messageId, role, snippet}&lt;/code&gt; matches. Content search reads this machine&amp;rsquo;s OpenCode database, so it is slow on large databases (tens of seconds for a week) and never covers remote sessions. &lt;code&gt;get&lt;/code&gt; needs only &lt;code&gt;session_id&lt;/code&gt;; it asks for &lt;code&gt;platform&lt;/code&gt; only when that ID exists on more than one platform. &lt;code&gt;create&lt;/code&gt; starts a new session. The tool cannot cancel or message existing sessions.&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;embed_file&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;Make a file on disk viewable to the user in the ocman UI. Takes an absolute &lt;code&gt;path&lt;/code&gt; (plus an optional &lt;code&gt;label&lt;/code&gt;) and returns a signed URL and a markdown snippet the agent pastes into its reply. Images and SVGs render inline in the conversation; PDFs and other types open or download in the browser. See &lt;a href="#embedding-generated-assets"&gt;Embedding generated assets&lt;/a&gt;
.&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;h2&gt;Factory&lt;span class="hx:absolute hx:-mt-20" id="factory"&gt;&lt;/span&gt;
&lt;a href="#factory" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Ocman installs the &lt;code&gt;ocman-factory&lt;/code&gt; skill globally for OpenCode. It teaches the
single action-based &lt;code&gt;factory&lt;/code&gt; tool and directs agents to &lt;code&gt;action: &amp;quot;help&amp;quot;&lt;/code&gt;
before using detailed actions. Agents copy the returned &lt;code&gt;[[ocman:card ...]]&lt;/code&gt;
marker verbatim in the response that creates an epic or requests a required
human action, not as a footer on every turn or routine status update. Markers
must appear as normal text, outside code blocks or markdown links.&lt;/p&gt;
&lt;p&gt;Factory tool errors intentionally contain only domain-level guidance. Open
Factory at &lt;code&gt;/factory&lt;/code&gt; to inspect the native Issue graph.&lt;/p&gt;
&lt;p&gt;Agents can list and append persistent Issue comments with &lt;code&gt;issue_comments&lt;/code&gt;
and &lt;code&gt;add_issue_comment&lt;/code&gt;. Comments are append-only and remain separate from the
linked session conversation.&lt;/p&gt;
&lt;p&gt;Agent MCP sessions cannot perform operator decisions, create executable graph
issues, or change Factory configuration. Non-executable graph edits remain
available through &lt;code&gt;mutate_graph&lt;/code&gt;; &lt;code&gt;create&lt;/code&gt; can create Epics after explicitly
acknowledging local execution and uses the built-in tracer Formula. &lt;code&gt;save_formula&lt;/code&gt;,
&lt;code&gt;set_capacity_policy&lt;/code&gt;, Plan decisions, recovery decisions, authority
decisions, and &lt;code&gt;reopen_issue&lt;/code&gt; (returning failed work to the queue) are
refused. Each permission denial returns a card marker for the agent&amp;rsquo;s reply.
Ocman interprets its type, target IDs, and action, then reads the live state to
decide whether to render a card. Action markers show nothing while loading or
after that action is resolved. Creation markers remain visible. Ordinary markdown
links stay links; markers inside code examples are displayed literally.&lt;/p&gt;
&lt;p&gt;For example:&lt;/p&gt;
&lt;div class="hextra-code-block hx:relative hx:mt-6 hx:first:mt-0 hx:group/code"&gt;
&lt;div&gt;&lt;div class="highlight"&gt;&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;[[ocman:card type=factory-epic epic=my-epic action=created]]
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;[[ocman:card type=factory-issue epic=my-epic issue=my-epic.3 action=reopen_issue]]&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class="hextra-code-copy-btn-container hx:opacity-0 hx:transition hx:group-hover/code:opacity-100 hx:flex hx:gap-1 hx:absolute hx:m-[11px] hx:right-0 hx:top-0"&gt;
&lt;button
class="hextra-code-copy-btn hx:group/copybtn hx:cursor-pointer hx:transition-all hx:active:opacity-50 hx:bg-primary-700/5 hx:border hx:border-black/5 hx:text-gray-600 hx:hover:text-gray-900 hx:rounded-md hx:p-1.5 hx:dark:bg-primary-300/10 hx:dark:border-white/10 hx:dark:text-gray-400 hx:dark:hover:text-gray-50"
title="Copy code"
aria-label="Copy code"
data-copied-label="Copied!"
&gt;
&lt;div class="hextra-copy-icon hx:group-[.copied]/copybtn:hidden hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;div class="hextra-success-icon hx:hidden hx:group-[.copied]/copybtn:block hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;/button&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;p&gt;IDs are percent-encoded when needed. The renderer handles live status, button
availability, and click results, so the model does not need to reproduce UI logic.
&lt;code&gt;reopen_issue&lt;/code&gt; with &lt;code&gt;epic_id&lt;/code&gt; and &lt;code&gt;issue_id&lt;/code&gt; produces a card with
&lt;strong&gt;Reopen issue&lt;/strong&gt; for failed or cancelled implementation work. Rendering the card
does nothing; clicking the button uses the same human-action endpoint as the
Factory action inbox. The card refreshes live state and reports action errors.
It also offers planning, materialization, plan decisions, recovery, and authority
decisions when available. Graph editing, formula editing, and capacity changes
link to their existing Factory screens. Requests without a resolvable target
remain links to the action inbox.&lt;/p&gt;
&lt;p&gt;&lt;code&gt;submit_proposal&lt;/code&gt; additionally requires the active
Planning Attempt&amp;rsquo;s &lt;code&gt;attempt_id&lt;/code&gt; and &lt;code&gt;attempt_token&lt;/code&gt;. Its manifest accepts an
issue graph with &lt;code&gt;nodes&lt;/code&gt; and typed &lt;code&gt;edges&lt;/code&gt; (&lt;code&gt;blocks&lt;/code&gt; or &lt;code&gt;on_failure&lt;/code&gt;); legacy
per-node &lt;code&gt;dependsOn&lt;/code&gt; remains accepted for stored and older proposals.&lt;/p&gt;
&lt;p&gt;For a complete plan made in a regular session, use &lt;code&gt;create&lt;/code&gt;, then &lt;code&gt;issues&lt;/code&gt; to
find the root &lt;code&gt;mol&lt;/code&gt; ID, then &lt;code&gt;import_proposal&lt;/code&gt;. It accepts &lt;code&gt;epic_id&lt;/code&gt;,
&lt;code&gt;manifest_json&lt;/code&gt; in the same format as &lt;code&gt;submit_proposal&lt;/code&gt;, and optional
&lt;code&gt;rationale_markdown&lt;/code&gt;, without attempt credentials. Import is allowed only before
any Factory attempt has been claimed and while the approval gate is unresolved.
It creates a proposal and returns a human approval card, without launching a
planning session or implementation. Re-import after a revision request to submit
the updated plan. The agent still cannot approve it. See
&lt;a href="../factory/#use-an-existing-plan"&gt;Use an existing plan&lt;/a&gt;
.&lt;/p&gt;
&lt;p&gt;Failed and terminally blocked work can launch a read-only unblock session from
the Factory action inbox. The agent explains one minimal repair, then invokes
&lt;code&gt;factory_unblock&lt;/code&gt;; OpenCode&amp;rsquo;s per-session permission prompt keeps the actual
reopen or graph mutation behind explicit user approval in the conversation.&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;&lt;strong&gt;Upgrade warning:&lt;/strong&gt; the native Factory cutover does not migrate legacy
Factory runs. Retired YAML Formula tables are kept under &lt;code&gt;legacy_factory_*&lt;/code&gt;
names for manual recovery when the v66 migration completed successfully; an
installation already stamped with the defective v66 migration may have empty
repair tables because dropped data cannot be reconstructed. The native
Factory does not read these tables.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2&gt;Routines&lt;span class="hx:absolute hx:-mt-20" id="routines"&gt;&lt;/span&gt;
&lt;a href="#routines" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Ocman installs the &lt;code&gt;ocman-routines&lt;/code&gt; skill globally for OpenCode. The
action-based &lt;code&gt;routines&lt;/code&gt; tool supports listing, reading, creating, replacing,
running, soft-deleting, and viewing run history. Its &lt;code&gt;help&lt;/code&gt; action is the
authoritative contract for agents.&lt;/p&gt;
&lt;h3&gt;Inbox&lt;span class="hx:absolute hx:-mt-20" id="inbox"&gt;&lt;/span&gt;
&lt;a href="#inbox" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h3&gt;&lt;p&gt;Ocman installs the &lt;code&gt;ocman-inbox&lt;/code&gt; skill globally for OpenCode. It permits only
the &lt;code&gt;inbox&lt;/code&gt; actions &lt;code&gt;help&lt;/code&gt;, &lt;code&gt;send&lt;/code&gt;, and &lt;code&gt;recall&lt;/code&gt;. Agents should use &lt;code&gt;send&lt;/code&gt; for
an asynchronous completion that needs attention, a blocked decision, or an
important failure outside the active conversation. Routine progress and
successful intermediate steps stay in the conversation. Reading or archiving
Inbox items is a user-only dashboard operation and is not exposed to agents.&lt;/p&gt;
&lt;h2&gt;Sessions&lt;span class="hx:absolute hx:-mt-20" id="sessions"&gt;&lt;/span&gt;
&lt;a href="#sessions" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;The &lt;code&gt;sessions&lt;/code&gt; tool inspects sessions with &lt;code&gt;list&lt;/code&gt;, &lt;code&gt;search&lt;/code&gt;, and &lt;code&gt;get&lt;/code&gt;, and
starts new top-level sessions with &lt;code&gt;create&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;&lt;code&gt;create&lt;/code&gt; sends &lt;code&gt;prompt&lt;/code&gt; to the new session. &lt;code&gt;model&lt;/code&gt; (&lt;code&gt;provider/model&lt;/code&gt;), &lt;code&gt;agent&lt;/code&gt;,
and &lt;code&gt;title&lt;/code&gt; are optional. &lt;code&gt;directory&lt;/code&gt; must be absolute. When it is omitted, the
session starts in the project root of the calling session, which the agent
identifies with &lt;code&gt;platform&lt;/code&gt; and &lt;code&gt;session_id&lt;/code&gt;. The calling session also
determines which machine runs the new one. New sessions use the platform&amp;rsquo;s
default permissions and do not inherit the caller&amp;rsquo;s rules.&lt;/p&gt;
&lt;p&gt;OpenCode permissions apply to whole tools, so requiring approval for &lt;code&gt;create&lt;/code&gt;
also requires it for the read-only actions.&lt;/p&gt;
&lt;p&gt;Use &lt;code&gt;create&lt;/code&gt; for independent work that should show up as its own session. For
work inside the current turn, OpenCode&amp;rsquo;s native Task tool handles subagent
delegation. A configured
subagent model overrides the caller&amp;rsquo;s model; otherwise the subagent inherits
the invoking primary agent&amp;rsquo;s model. The installed &lt;code&gt;ocman-sessions&lt;/code&gt; skill also
documents one-off Task model selection.&lt;/p&gt;
&lt;h2&gt;Embedding generated assets&lt;span class="hx:absolute hx:-mt-20" id="embedding-generated-assets"&gt;&lt;/span&gt;
&lt;a href="#embedding-generated-assets" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Agents routinely produce files a chat transcript cannot show: a rendered
chart, an SVG diagram, a generated PDF. &lt;code&gt;embed_file&lt;/code&gt; closes that gap.&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;The agent writes the file to disk as usual.&lt;/li&gt;
&lt;li&gt;It calls &lt;code&gt;embed_file&lt;/code&gt; with the absolute &lt;code&gt;path&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;Ocman returns a URL under &lt;code&gt;/api/file/{token}&lt;/code&gt; plus a &lt;code&gt;markdown&lt;/code&gt;
snippet, which the agent includes in its reply.&lt;/li&gt;
&lt;li&gt;The ocman UI renders that markdown: images and SVGs appear inline in
the conversation, other types become a link the browser opens or
downloads.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The token is an HMAC over the absolute path, signed with a key persisted
in &lt;code&gt;state.db&lt;/code&gt;, so links keep working across restarts while a hand-crafted
or altered path is rejected with &lt;code&gt;403&lt;/code&gt;. The endpoint sits behind the
normal dashboard auth guard, and responses carry &lt;code&gt;nosniff&lt;/code&gt; plus a
&lt;code&gt;Content-Security-Policy: sandbox&lt;/code&gt; header so an SVG or HTML asset opened
as a top-level document cannot run script.&lt;/p&gt;
&lt;p&gt;MCP callers are local and already run as your user, so the tool does not
restrict which paths may be embedded. An agent that can call it can read
those files directly anyway.&lt;/p&gt;
&lt;h2&gt;Installed skill&lt;span class="hx:absolute hx:-mt-20" id="installed-skill"&gt;&lt;/span&gt;
&lt;a href="#installed-skill" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;MCP interaction guidance lives in:&lt;/p&gt;
&lt;div class="hextra-code-block hx:relative hx:mt-6 hx:first:mt-0 hx:group/code"&gt;
&lt;div&gt;&lt;div class="highlight"&gt;&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;.opencode/skills/ocman-factory/SKILL.md
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;.opencode/skills/ocman-routines/SKILL.md
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;.opencode/skills/ocman-sessions/SKILL.md
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;.opencode/skills/ocman-inbox/SKILL.md&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class="hextra-code-copy-btn-container hx:opacity-0 hx:transition hx:group-hover/code:opacity-100 hx:flex hx:gap-1 hx:absolute hx:m-[11px] hx:right-0 hx:top-0"&gt;
&lt;button
class="hextra-code-copy-btn hx:group/copybtn hx:cursor-pointer hx:transition-all hx:active:opacity-50 hx:bg-primary-700/5 hx:border hx:border-black/5 hx:text-gray-600 hx:hover:text-gray-900 hx:rounded-md hx:p-1.5 hx:dark:bg-primary-300/10 hx:dark:border-white/10 hx:dark:text-gray-400 hx:dark:hover:text-gray-50"
title="Copy code"
aria-label="Copy code"
data-copied-label="Copied!"
&gt;
&lt;div class="hextra-copy-icon hx:group-[.copied]/copybtn:hidden hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;div class="hextra-success-icon hx:hidden hx:group-[.copied]/copybtn:block hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;/button&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;p&gt;At startup, ocman extracts and links the skill into OpenCode&amp;rsquo;s global skill
directory. Restart OpenCode after installing or upgrading ocman so every
conversation can discover it.&lt;/p&gt;</description></item><item><title>Routines</title><link>https://ocman.stenic.io/docs/features/routines/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://ocman.stenic.io/docs/features/routines/</guid><description>
&lt;p&gt;Routines save a prompt for a project. Run one by hand or give it a schedule.
Choose how each routine uses OpenCode sessions:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;New session&lt;/strong&gt; starts a fresh session for every run. This is the default.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Reuse session&lt;/strong&gt; starts a session on the first run and continues it later.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Existing session&lt;/strong&gt; continues a session you select from the project.&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Create and manage routines&lt;span class="hx:absolute hx:-mt-20" id="create-and-manage-routines"&gt;&lt;/span&gt;
&lt;a href="#create-and-manage-routines" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Open &lt;strong&gt;Routines&lt;/strong&gt; from the main navigation, then select &lt;strong&gt;New routine&lt;/strong&gt;. Enter
a unique name, the prompt, the target project, session behavior, and a schedule.
The searchable agent and model lists use values seen in that project. Select
the default option to let OpenCode choose. You can also disable a routine
without deleting it.&lt;/p&gt;
&lt;p&gt;Name, prompt, project, schedule, and enabled status stay visible at the top.
Expand &lt;strong&gt;Session and model&lt;/strong&gt;, &lt;strong&gt;After a run&lt;/strong&gt;, or &lt;strong&gt;Permissions&lt;/strong&gt; for optional
settings. These groups start collapsed when you create or edit a routine.&lt;/p&gt;
&lt;p&gt;Use &lt;strong&gt;Edit&lt;/strong&gt; to change any of these fields. The change applies to future runs.
Existing history keeps the name, prompt, project, session behavior, agent,
model, and trigger recorded when each run started.&lt;/p&gt;
&lt;p&gt;Select a routine row to open its editable settings and run history in the side
drawer.&lt;/p&gt;
&lt;p&gt;When a run finishes, ocman adds an Inbox item with the run status and the
session&amp;rsquo;s final assistant message.&lt;/p&gt;
&lt;h2&gt;Webhook deliveries&lt;span class="hx:absolute hx:-mt-20" id="webhook-deliveries"&gt;&lt;/span&gt;
&lt;a href="#webhook-deliveries" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;An accepted encrypted inbox delivery may trigger every matching subscription
for its owner. The routine prompt is sent unchanged, followed by this fixed
untrusted-data envelope:&lt;/p&gt;
&lt;div class="hextra-code-block hx:relative hx:mt-6 hx:first:mt-0 hx:group/code"&gt;
&lt;div&gt;&lt;div class="highlight"&gt;&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-json" data-lang="json"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="p"&gt;{&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; &lt;span class="nt"&gt;&amp;#34;inboxId&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt; &lt;span class="s2"&gt;&amp;#34;...&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; &lt;span class="nt"&gt;&amp;#34;deliveryId&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt; &lt;span class="s2"&gt;&amp;#34;...&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; &lt;span class="nt"&gt;&amp;#34;method&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt; &lt;span class="s2"&gt;&amp;#34;POST&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; &lt;span class="nt"&gt;&amp;#34;headers&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt; &lt;span class="p"&gt;{},&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; &lt;span class="nt"&gt;&amp;#34;query&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt; &lt;span class="p"&gt;{},&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; &lt;span class="nt"&gt;&amp;#34;receivedAt&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt; &lt;span class="mi"&gt;0&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; &lt;span class="nt"&gt;&amp;#34;bodyBase64&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt; &lt;span class="s2"&gt;&amp;#34;...&amp;#34;&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="p"&gt;}&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class="hextra-code-copy-btn-container hx:opacity-0 hx:transition hx:group-hover/code:opacity-100 hx:flex hx:gap-1 hx:absolute hx:m-[11px] hx:right-0 hx:top-0"&gt;
&lt;button
class="hextra-code-copy-btn hx:group/copybtn hx:cursor-pointer hx:transition-all hx:active:opacity-50 hx:bg-primary-700/5 hx:border hx:border-black/5 hx:text-gray-600 hx:hover:text-gray-900 hx:rounded-md hx:p-1.5 hx:dark:bg-primary-300/10 hx:dark:border-white/10 hx:dark:text-gray-400 hx:dark:hover:text-gray-50"
title="Copy code"
aria-label="Copy code"
data-copied-label="Copied!"
&gt;
&lt;div class="hextra-copy-icon hx:group-[.copied]/copybtn:hidden hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;div class="hextra-success-icon hx:hidden hx:group-[.copied]/copybtn:block hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;/button&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;p&gt;Subscriptions combine header and RFC 6901 JSON Pointer predicates with AND.
Predicates support only &lt;code&gt;exists&lt;/code&gt;, &lt;code&gt;equals&lt;/code&gt;, and &lt;code&gt;oneOf&lt;/code&gt;; scripts and regular
expressions are not evaluated. Disabled routines pause dispatch, while deleted
routines cancel queued dispatches. Delivery and dispatch history is retained
for 30 days.&lt;/p&gt;
&lt;p&gt;Use &lt;strong&gt;Delete&lt;/strong&gt; to remove a routine from the UI and stop future scheduled runs.
Deletion is soft: ocman retains the routine and its run history in &lt;code&gt;state.db&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;The &lt;strong&gt;Delete after a successful run&lt;/strong&gt; option performs the same soft delete
only after a run succeeds. A failed run leaves the routine available.&lt;/p&gt;
&lt;p&gt;Enable &lt;strong&gt;Archive session after a successful run&lt;/strong&gt; to hide completed sessions
from the active session list. This is off by default and applies to scheduled
and manual runs. Failed or interrupted runs stay visible. The routine and its
schedule remain active, and history still links to the archived session.
The setting is captured when each run starts; edits apply to future runs.&lt;/p&gt;
&lt;h2&gt;Run a routine&lt;span class="hx:absolute hx:-mt-20" id="run-a-routine"&gt;&lt;/span&gt;
&lt;a href="#run-a-routine" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;The Routines page and the composer provide different actions:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Run now&lt;/strong&gt; starts or selects a session according to the routine and sends the saved prompt.
It records a manual run in the routine&amp;rsquo;s history.&lt;/li&gt;
&lt;li&gt;&lt;code&gt;/routines&lt;/code&gt; in a session composer opens a picker and inserts the chosen
prompt into the composer. It does not send the prompt, start a session, or
create a history entry. Edit the text or send it like any other message.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Scheduled runs use the same session behavior as manual runs.&lt;/p&gt;
&lt;h2&gt;Schedule semantics&lt;span class="hx:absolute hx:-mt-20" id="schedule-semantics"&gt;&lt;/span&gt;
&lt;a href="#schedule-semantics" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;None&lt;/strong&gt; has no automatic run time. Use &lt;strong&gt;Run now&lt;/strong&gt; when needed.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Timeout&lt;/strong&gt; accepts a delay from now. When you save the routine, ocman turns
that relative delay into one absolute due time. Restarting ocman or editing
the routine later does not restart the original countdown. Saving an edit
with a timeout calculates a new due time from that save. A timeout missed
while ocman is offline expires at startup instead of running late.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Once&lt;/strong&gt; runs at one absolute date and time. The selected local time is
saved as an absolute instant.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Cron&lt;/strong&gt; uses a standard five-field cron expression plus an IANA timezone,
such as &lt;code&gt;0 9 * * 1-5&lt;/code&gt; with &lt;code&gt;Europe/Brussels&lt;/code&gt;. Ocman calculates each next
occurrence in that timezone after the previous run settles.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;The scheduler checks due routines every five seconds. Disable a routine to
stop automatic runs without deleting it. Webhook delivery polling is
independent of the scheduler and runs on the owner of the inbox.&lt;/p&gt;
&lt;h2&gt;Completion and history&lt;span class="hx:absolute hx:-mt-20" id="completion-and-history"&gt;&lt;/span&gt;
&lt;a href="#completion-and-history" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;A run remains &lt;strong&gt;running&lt;/strong&gt; while its session is active or waiting for the
session to settle. Session &lt;strong&gt;done&lt;/strong&gt; marks the run &lt;strong&gt;success&lt;/strong&gt;. Session
&lt;strong&gt;error&lt;/strong&gt; marks it &lt;strong&gt;failure&lt;/strong&gt;. An interrupted or unavailable session marks
the run &lt;strong&gt;interrupted&lt;/strong&gt;. Dispatch errors, including
an unavailable host or a session that could not be created or prompted, also
produce a failed history entry.&lt;/p&gt;
&lt;p&gt;Open a routine row to see when each run started, whether it was manual or
scheduled, and its current result. Failed runs show their error. Runs with a
linked session include an &lt;strong&gt;Open&lt;/strong&gt; link.&lt;/p&gt;
&lt;p&gt;Run records preserve their routine snapshot and remain available after a
restart. Ocman resumes observing linked running sessions after startup. If a
restart interrupted dispatch before ocman linked a session, the run fails
instead of starting a duplicate session.&lt;/p&gt;
&lt;h2&gt;Recover legacy Workflow data&lt;span class="hx:absolute hx:-mt-20" id="recover-legacy-workflow-data"&gt;&lt;/span&gt;
&lt;a href="#recover-legacy-workflow-data" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Older ocman versions stored DAG definitions and runs in &lt;code&gt;workflow_*&lt;/code&gt; tables.
Those tables and rows remain inert in &lt;code&gt;state.db&lt;/code&gt;. Routines do not read,
execute, migrate, or delete them. A DAG cannot be converted losslessly to one
prompt because its nodes, dependencies, approvals, commands, outputs, and
branching have no equivalent in a routine.&lt;/p&gt;
&lt;p&gt;There are no compatibility APIs. Use SQLite in read-only mode to inspect or
export the old data. Stop ocman first if you plan to copy the database.&lt;/p&gt;
&lt;div class="hextra-code-block hx:relative hx:mt-6 hx:first:mt-0 hx:group/code"&gt;
&lt;div&gt;&lt;div class="highlight"&gt;&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-sh" data-lang="sh"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nv"&gt;DB&lt;/span&gt;&lt;span class="o"&gt;=&lt;/span&gt;&lt;span class="s2"&gt;&amp;#34;&lt;/span&gt;&lt;span class="nv"&gt;$HOME&lt;/span&gt;&lt;span class="s2"&gt;/.local/share/ocman/state.db&amp;#34;&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;sqlite3 -readonly &lt;span class="s2"&gt;&amp;#34;&lt;/span&gt;&lt;span class="nv"&gt;$DB&lt;/span&gt;&lt;span class="s2"&gt;&amp;#34;&lt;/span&gt; &lt;span class="s2"&gt;&amp;#34;.tables &amp;#39;workflow_*&amp;#39;&amp;#34;&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;sqlite3 -readonly &lt;span class="s2"&gt;&amp;#34;&lt;/span&gt;&lt;span class="nv"&gt;$DB&lt;/span&gt;&lt;span class="s2"&gt;&amp;#34;&lt;/span&gt; &lt;span class="s2"&gt;&amp;#34;.dump &amp;#39;workflow_*&amp;#39;&amp;#34;&lt;/span&gt; &amp;gt; legacy-workflows.sql
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;sqlite3 -readonly -json &lt;span class="s2"&gt;&amp;#34;&lt;/span&gt;&lt;span class="nv"&gt;$DB&lt;/span&gt;&lt;span class="s2"&gt;&amp;#34;&lt;/span&gt; &lt;span class="s2"&gt;&amp;#34;SELECT * FROM workflow_definition ORDER BY created_at;&amp;#34;&lt;/span&gt; &amp;gt; workflow-definitions.json
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;sqlite3 -readonly -json &lt;span class="s2"&gt;&amp;#34;&lt;/span&gt;&lt;span class="nv"&gt;$DB&lt;/span&gt;&lt;span class="s2"&gt;&amp;#34;&lt;/span&gt; &lt;span class="s2"&gt;&amp;#34;SELECT * FROM workflow_version ORDER BY workflow_id, revision;&amp;#34;&lt;/span&gt; &amp;gt; workflow-versions.json
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;sqlite3 -readonly -json &lt;span class="s2"&gt;&amp;#34;&lt;/span&gt;&lt;span class="nv"&gt;$DB&lt;/span&gt;&lt;span class="s2"&gt;&amp;#34;&lt;/span&gt; &lt;span class="s2"&gt;&amp;#34;SELECT * FROM workflow_run ORDER BY created_at;&amp;#34;&lt;/span&gt; &amp;gt; workflow-runs.json
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;sqlite3 -readonly -json &lt;span class="s2"&gt;&amp;#34;&lt;/span&gt;&lt;span class="nv"&gt;$DB&lt;/span&gt;&lt;span class="s2"&gt;&amp;#34;&lt;/span&gt; &lt;span class="s2"&gt;&amp;#34;SELECT * FROM workflow_node_run ORDER BY run_id, position;&amp;#34;&lt;/span&gt; &amp;gt; workflow-node-runs.json
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;sqlite3 -readonly -json &lt;span class="s2"&gt;&amp;#34;&lt;/span&gt;&lt;span class="nv"&gt;$DB&lt;/span&gt;&lt;span class="s2"&gt;&amp;#34;&lt;/span&gt; &lt;span class="s2"&gt;&amp;#34;SELECT * FROM workflow_node_attempt ORDER BY run_id, node_id, seq;&amp;#34;&lt;/span&gt; &amp;gt; workflow-node-attempts.json&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class="hextra-code-copy-btn-container hx:opacity-0 hx:transition hx:group-hover/code:opacity-100 hx:flex hx:gap-1 hx:absolute hx:m-[11px] hx:right-0 hx:top-0"&gt;
&lt;button
class="hextra-code-copy-btn hx:group/copybtn hx:cursor-pointer hx:transition-all hx:active:opacity-50 hx:bg-primary-700/5 hx:border hx:border-black/5 hx:text-gray-600 hx:hover:text-gray-900 hx:rounded-md hx:p-1.5 hx:dark:bg-primary-300/10 hx:dark:border-white/10 hx:dark:text-gray-400 hx:dark:hover:text-gray-50"
title="Copy code"
aria-label="Copy code"
data-copied-label="Copied!"
&gt;
&lt;div class="hextra-copy-icon hx:group-[.copied]/copybtn:hidden hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;div class="hextra-success-icon hx:hidden hx:group-[.copied]/copybtn:block hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;/button&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;p&gt;The full authored DAG is in &lt;code&gt;workflow_version.definition_json&lt;/code&gt;. To list the
saved definitions in a readable form:&lt;/p&gt;
&lt;div class="hextra-code-block hx:relative hx:mt-6 hx:first:mt-0 hx:group/code"&gt;
&lt;div&gt;&lt;div class="highlight"&gt;&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-sql" data-lang="sql"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="k"&gt;SELECT&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;d&lt;/span&gt;&lt;span class="p"&gt;.&lt;/span&gt;&lt;span class="n"&gt;name&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;v&lt;/span&gt;&lt;span class="p"&gt;.&lt;/span&gt;&lt;span class="n"&gt;revision&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;v&lt;/span&gt;&lt;span class="p"&gt;.&lt;/span&gt;&lt;span class="n"&gt;created_at&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;v&lt;/span&gt;&lt;span class="p"&gt;.&lt;/span&gt;&lt;span class="n"&gt;definition_json&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="k"&gt;FROM&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;workflow_definition&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;AS&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;d&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="k"&gt;JOIN&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;workflow_version&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;AS&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;v&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;ON&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;v&lt;/span&gt;&lt;span class="p"&gt;.&lt;/span&gt;&lt;span class="n"&gt;workflow_id&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="o"&gt;=&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;d&lt;/span&gt;&lt;span class="p"&gt;.&lt;/span&gt;&lt;span class="n"&gt;id&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="k"&gt;ORDER&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;BY&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;d&lt;/span&gt;&lt;span class="p"&gt;.&lt;/span&gt;&lt;span class="n"&gt;name&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;COLLATE&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;NOCASE&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;v&lt;/span&gt;&lt;span class="p"&gt;.&lt;/span&gt;&lt;span class="n"&gt;revision&lt;/span&gt;&lt;span class="p"&gt;;&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class="hextra-code-copy-btn-container hx:opacity-0 hx:transition hx:group-hover/code:opacity-100 hx:flex hx:gap-1 hx:absolute hx:m-[11px] hx:right-0 hx:top-0"&gt;
&lt;button
class="hextra-code-copy-btn hx:group/copybtn hx:cursor-pointer hx:transition-all hx:active:opacity-50 hx:bg-primary-700/5 hx:border hx:border-black/5 hx:text-gray-600 hx:hover:text-gray-900 hx:rounded-md hx:p-1.5 hx:dark:bg-primary-300/10 hx:dark:border-white/10 hx:dark:text-gray-400 hx:dark:hover:text-gray-50"
title="Copy code"
aria-label="Copy code"
data-copied-label="Copied!"
&gt;
&lt;div class="hextra-copy-icon hx:group-[.copied]/copybtn:hidden hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;div class="hextra-success-icon hx:hidden hx:group-[.copied]/copybtn:block hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;/button&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;p&gt;Open that query without modifying the database:&lt;/p&gt;
&lt;div class="hextra-code-block hx:relative hx:mt-6 hx:first:mt-0 hx:group/code"&gt;
&lt;div&gt;&lt;div class="highlight"&gt;&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-sh" data-lang="sh"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;sqlite3 -readonly -header -column &lt;span class="s2"&gt;&amp;#34;&lt;/span&gt;&lt;span class="nv"&gt;$DB&lt;/span&gt;&lt;span class="s2"&gt;&amp;#34;&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class="hextra-code-copy-btn-container hx:opacity-0 hx:transition hx:group-hover/code:opacity-100 hx:flex hx:gap-1 hx:absolute hx:m-[11px] hx:right-0 hx:top-0"&gt;
&lt;button
class="hextra-code-copy-btn hx:group/copybtn hx:cursor-pointer hx:transition-all hx:active:opacity-50 hx:bg-primary-700/5 hx:border hx:border-black/5 hx:text-gray-600 hx:hover:text-gray-900 hx:rounded-md hx:p-1.5 hx:dark:bg-primary-300/10 hx:dark:border-white/10 hx:dark:text-gray-400 hx:dark:hover:text-gray-50"
title="Copy code"
aria-label="Copy code"
data-copied-label="Copied!"
&gt;
&lt;div class="hextra-copy-icon hx:group-[.copied]/copybtn:hidden hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;div class="hextra-success-icon hx:hidden hx:group-[.copied]/copybtn:block hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;/button&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;p&gt;Paste the SQL at the SQLite prompt. Extract any useful agent prompt from
&lt;code&gt;definition_json&lt;/code&gt;, then create a routine manually. Keep commands, approval
steps, dependencies, and output handling outside the routine prompt unless
the agent can safely perform them itself.&lt;/p&gt;</description></item><item><title>Encrypted Webhooks</title><link>https://ocman.stenic.io/docs/features/webhooks/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://ocman.stenic.io/docs/features/webhooks/</guid><description>
&lt;p&gt;An ocman owner can register an inbox with an &lt;code&gt;ocman-relay&lt;/code&gt; and expose its
ingestion URL to a provider. The relay accepts requests while the owner is
offline. It stores only age ciphertext and visible size/timing metadata; the
owner later decrypts and queues the request in its local &lt;code&gt;state.db&lt;/code&gt;.&lt;/p&gt;
&lt;h2&gt;Format And Boundaries&lt;span class="hx:absolute hx:-mt-20" id="format-and-boundaries"&gt;&lt;/span&gt;
&lt;a href="#format-and-boundaries" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;The relay encrypts a versioned JSON envelope with age X25519. The recipient is
an &lt;code&gt;age1...&lt;/code&gt; X25519 public key and the owner keeps the matching
&lt;code&gt;AGE-SECRET-KEY-&lt;/code&gt; identity in &lt;code&gt;state.db&lt;/code&gt;. &lt;code&gt;formatVersion&lt;/code&gt; and &lt;code&gt;keyVersion&lt;/code&gt; are
required, so a future format can be rejected rather than guessed.&lt;/p&gt;
&lt;p&gt;The envelope contains the plaintext body at ingestion, request method, query,
filtered headers, delivery identity, and &lt;code&gt;receivedAt&lt;/code&gt; milliseconds. Sensitive
headers such as &lt;code&gt;Authorization&lt;/code&gt;, cookies, and the configured shared-secret
header are excluded. Relay logs, storage metadata, and telemetry must never
contain the body, credentials, age identity, or private keys. Object sizes and
timestamps are intentionally visible to the relay operator.&lt;/p&gt;
&lt;p&gt;The relay persists inbox metadata and ciphertext deliveries. The owner
persists the private identity, relay credentials, accepted delivery, inbox
item, retry state, and routine-dispatch claim in its own &lt;code&gt;state.db&lt;/code&gt;; these are
separate persistence boundaries. Registration returns &lt;code&gt;202 Accepted&lt;/code&gt; only
after ciphertext is durably stored. The owner acknowledges a delivery only
after decryption, local acceptance, and all matching routine dispatch claims
have been committed. A lost acknowledgment is safe: the next poll finds the
durable delivery record and cannot schedule the same routine occurrence twice.&lt;/p&gt;
&lt;h2&gt;Operations&lt;span class="hx:absolute hx:-mt-20" id="operations"&gt;&lt;/span&gt;
&lt;a href="#operations" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Configure relay enrollment and limits before registration. Body size, retained
header size, pending delivery count/bytes, ingest rate, inbox TTL, and relay
storage TTL are enforced. Expiry and revocation delete the relay metadata and
deliveries. A malformed or undecryptable delivery is recorded with bounded
backoff and does not block later deliveries; it remains available for retry or
operator investigation until expiry.&lt;/p&gt;
&lt;p&gt;Key rotation changes the recipient and increments &lt;code&gt;keyVersion&lt;/code&gt;. Existing
pending ciphertext remains decryptable only with the old identity if it is
retained. Resetting or losing the private identity makes those deliveries
unreadable; revoke and register a new inbox when recovery is not possible.&lt;/p&gt;
&lt;p&gt;The UI exposes only the ingestion URL, key version, owner, and aggregate
delivery states. Management, fetch, acknowledgment, enrollment, and private
identity credentials stay server-side. Local and remote owners use the same
owner API; the hub routes registration and polling to the owning ocman.&lt;/p&gt;
&lt;h2&gt;Non-goals&lt;span class="hx:absolute hx:-mt-20" id="non-goals"&gt;&lt;/span&gt;
&lt;a href="#non-goals" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;ul&gt;
&lt;li&gt;Provider signature verification or challenge/handshake protocols.&lt;/li&gt;
&lt;li&gt;Provider event deduplication beyond the relay delivery identity.&lt;/li&gt;
&lt;li&gt;Arbitrary user validators, scripts, or regular expressions.&lt;/li&gt;
&lt;li&gt;Exactly-once external side effects. Local routine scheduling is durable and
deduplicated, but an agent or provider side effect can still be retried.&lt;/li&gt;
&lt;/ul&gt;</description></item><item><title>Architecture</title><link>https://ocman.stenic.io/docs/other/architecture/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://ocman.stenic.io/docs/other/architecture/</guid><description>
&lt;h2&gt;Introduction&lt;span class="hx:absolute hx:-mt-20" id="introduction"&gt;&lt;/span&gt;
&lt;a href="#introduction" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Ocman is a single Go binary that serves a React SPA and acts as a control
plane for coding-agent sessions. The main diagrams cover the system context
(what ocman talks to), the backend composition (the Go packages), the
session/event data flow, frontend composition, and plugin action flow. Detail
lives in the text below each diagram.&lt;/p&gt;
&lt;h3&gt;Cross-machine conversation sharing&lt;span class="hx:absolute hx:-mt-20" id="cross-machine-conversation-sharing"&gt;&lt;/span&gt;
&lt;a href="#cross-machine-conversation-sharing" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h3&gt;&lt;p&gt;Ocman can publish a shared conversation through the standalone &lt;code&gt;ocman-relay&lt;/code&gt;
binary. The relay stores only AES-256-GCM ciphertext through the &lt;code&gt;share.Store&lt;/code&gt;
abstraction (disk today; object storage can implement the same
&lt;code&gt;Put&lt;/code&gt;/&lt;code&gt;Get&lt;/code&gt;/&lt;code&gt;List&lt;/code&gt;/&lt;code&gt;DeletePrefix&lt;/code&gt; contract). The per-share key stays in the URL
fragment, so it never reaches the relay.&lt;/p&gt;
&lt;div role="img" aria-label="Diagram"&gt;
&lt;pre class="mermaid hx:mt-6"&gt;
flowchart LR
Owner[Owner ocman] --&amp;gt;|sealed completed turns| Relay[ocman-relay]
Relay --&amp;gt; Store[(share.Store)]
Viewer[Browser viewer] --&amp;gt;|ciphertext poll| Relay
Viewer --&amp;gt;|WebCrypto decrypt| Thread[Shared conversation]
Thread --&amp;gt;|explicit local fork| Local[Recipient ocman]
&lt;/pre&gt;
&lt;/div&gt;&lt;ul&gt;
&lt;li&gt;Chunk zero is a complete snapshot; later chunks contain the latest
completed turn and the viewer merges them as id-keyed upserts.&lt;/li&gt;
&lt;li&gt;The owner allocates sequence numbers. Nonces derive from those sequence
numbers, and the share id plus sequence is authenticated as GCM AAD.&lt;/li&gt;
&lt;li&gt;Revocation deletes the relay prefix. The relay stores only a hash of its
append/delete token and needs no database.&lt;/li&gt;
&lt;li&gt;Forking fetches and decrypts in the recipient&amp;rsquo;s authenticated local ocman
UI, lets the recipient choose a local project, and parks the transcript as
an unsent composer draft. Imported text never runs automatically.&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;1. System context&lt;span class="hx:absolute hx:-mt-20" id="1-system-context"&gt;&lt;/span&gt;
&lt;a href="#1-system-context" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Everything external that the ocman process touches.&lt;/p&gt;
&lt;div role="img" aria-label="Diagram"&gt;
&lt;pre class="mermaid hx:mt-6"&gt;
flowchart LR
Browser[Browser SPA&amp;lt;br/&amp;gt;REST + SSE] --&amp;gt;|core APIs + brokered plugin actions| Ocman[ocman&amp;lt;br/&amp;gt;Go binary :8228]
Agent[AI agents&amp;lt;br/&amp;gt;MCP clients] --&amp;gt;|/mcp| Ocman
Ocman --&amp;gt;|read-only SQLite&amp;lt;br/&amp;gt;maintenance writes| OCDB[(opencode.db)]
Ocman --&amp;gt;|read/write SQLite&amp;lt;br/&amp;gt;Inbox + state| StateDB[(state.db)]
Ocman --&amp;gt;|Authenticated HTTP/SSE proxy| OCInst[Running OpenCode&amp;lt;br/&amp;gt;instances]
Ocman --&amp;gt;|exec| Shell[git / tmux / lsof / bd&amp;lt;br/&amp;gt;host tools]
Ocman --&amp;gt;|describe + supervised serve / NDJSON| PluginExec[Trusted native plugin processes]
Ocman --&amp;gt;|REST| APIs[GitHub / Forgejo&amp;lt;br/&amp;gt;provider usage APIs]
Ocman &amp;lt;--&amp;gt;|gRPC + token: sessions, hosts, plugins| Remotes[Remote ocman&amp;lt;br/&amp;gt;instances]
Ocman --&amp;gt;|encrypted webhook poll| Relay[ocman-relay&amp;lt;br/&amp;gt;ciphertext persistence]
Ocman -.-&amp;gt;|OTLP, optional| Otel[Telemetry collector]
&lt;/pre&gt;
&lt;/div&gt;&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Browser SPA.&lt;/strong&gt; The only UI. It talks REST/SSE to the hub, never to
remotes directly. &lt;code&gt;:8228&lt;/code&gt; is the production &lt;code&gt;-addr&lt;/code&gt; default; in dev the
Vite server on :8228 proxies &lt;code&gt;/api&lt;/code&gt; to the air backend on :8229.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;opencode.db.&lt;/strong&gt; Foreign data, opened read-only. The one exception is the
user-started maintenance job (&lt;code&gt;internal/ocmaint&lt;/code&gt;, Settings → Maintenance).
It stops the managed instances, refuses while any other process holds the
file, and moves old &lt;code&gt;summary.diffs&lt;/code&gt; patches into a restorable dump.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;state.db.&lt;/strong&gt; Ocman&amp;rsquo;s own state: archive flags, routines and run history,
permission approval provenance, live session commit observations, settings,
Factory records, Inbox items, remote tokens, and the last local projects-index
snapshot. Inbox sends are owner-local and persist until recalled or archived
by the user. Legacy &lt;code&gt;workflow_*&lt;/code&gt; rows remain inert for manual recovery.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Provider usage APIs.&lt;/strong&gt; The subscription usage page reads OpenCode&amp;rsquo;s local
OAuth credentials server-side and returns only normalized quota windows;
provider tokens and account identifiers never reach the browser.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Native plugin executables.&lt;/strong&gt; Startup and explicit rescans describe direct
executables from the local plugin directory. Each describe has a fresh token,
minimal environment, bounded output, and a three-second deadline. Duplicate
identities conflict; changed binaries lose approval. Discovery itself executes
trusted code. Grants minimize brokered context, not operating-system access.
See &lt;a href="https://ocman.stenic.io/docs/features/plugins/"&gt;Plugins&lt;/a&gt;
for installation and operations.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Remote ocman instances.&lt;/strong&gt; The hub dials remotes over gRPC and re-exposes
their sessions and hosts transparently. The owning remote enriches session
detail with its persisted approvals and tees synthetic approval events into
the gRPC event stream before the hub forwards them to the browser.
Plugin catalog, management, health, actions and artifact reads use the same
authenticated connection. Each machine discovers its own binaries and stores
its own configuration and secrets. The hub never installs remote binaries.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Encrypted webhooks.&lt;/strong&gt; Providers submit plaintext to the relay&amp;rsquo;s ingestion
URL, where ocman encrypts a versioned age X25519 envelope. The relay
persists ciphertext plus visible size/timing metadata; the owning local or
remote ocman decrypts it, durably accepts it in &lt;code&gt;state.db&lt;/code&gt;, claims routine
dispatches, then acknowledges the relay. A lost acknowledgment is retried
without duplicate scheduling.&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;2. Backend composition&lt;span class="hx:absolute hx:-mt-20" id="2-backend-composition"&gt;&lt;/span&gt;
&lt;a href="#2-backend-composition" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;The Go package graph, collapsed to the seams that matter.&lt;/p&gt;
&lt;div role="img" aria-label="Diagram"&gt;
&lt;pre class="mermaid hx:mt-6"&gt;
flowchart TD
Server[internal/server&amp;lt;br/&amp;gt;HTTP, SSE, handlers] --&amp;gt; Registry[platforms.Registry&amp;lt;br/&amp;gt;session seam]
Server --&amp;gt; Router[hostsvc.Router&amp;lt;br/&amp;gt;host/dir seam]
Server --&amp;gt; Routines[internal/routines&amp;lt;br/&amp;gt;saved prompts + schedules]
Server --&amp;gt; Factory[internal/factory&amp;lt;br/&amp;gt;YAML workflows + Issue dispatch]
Factory --&amp;gt; FactoryModel[internal/factory/model&amp;lt;br/&amp;gt;shared persistence records]
Factory --&amp;gt; State
State --&amp;gt; FactoryModel
Factory --&amp;gt; Registry
Factory --&amp;gt; Router
Factory --&amp;gt;|Project Delivery validation + merge observation| Forge
Routines --&amp;gt; Registry
Routines --&amp;gt; Router
Routines --&amp;gt; State
Server --&amp;gt; MCP[internal/mcp&amp;lt;br/&amp;gt;MCP tools]
MCP --&amp;gt; Factory
MCP --&amp;gt; Inbox[Inbox&amp;lt;br/&amp;gt;state + owner routing]
MCP --&amp;gt; Routines
MCP --&amp;gt; Registry
Registry --&amp;gt; OC[platforms/opencode + internal/db&amp;lt;br/&amp;gt;adapter and read-only queries]
Registry --&amp;gt; RP[internal/remote&amp;lt;br/&amp;gt;platform adapter + owner RPCs]
Router --&amp;gt; Local[hostsvc/local&amp;lt;br/&amp;gt;git, tmux, worktree, Beads, runtimes]
Server --&amp;gt; State[internal/state&amp;lt;br/&amp;gt;state.db]
Inbox --&amp;gt; State
Inbox -.-&amp;gt;|remote RPC| Router
Server --&amp;gt; Forge[forge + integrations&amp;lt;br/&amp;gt;GitHub/Forgejo clients]
State -.-&amp;gt;|registration types| Plugins[internal/plugins&amp;lt;br/&amp;gt;protocol, supervision, action broker]
Server --&amp;gt;|management + authenticated actions| Plugins
Plugins --&amp;gt;|token-bound NDJSON + private configuration fd| PluginExec[External plugin process]
Server --&amp;gt;|owner-routed PluginOperation RPC| RP
PluginSDK[sdk/plugin&amp;lt;br/&amp;gt;optional public Go SDK] --&amp;gt;|canonical DTO aliases + validators| Plugins
&lt;/pre&gt;
&lt;/div&gt;&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;internal/server.&lt;/strong&gt; The HTTP mux, SSE broadcast and fanout, around 60
handler files, plus tmux, terminal, whisper, auto-approve and routine ticks.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;internal/plugins.&lt;/strong&gt; External wire DTOs, bounded NDJSON, version negotiation,
handshake and stream-order validation, executable discovery, and description
validation. &lt;code&gt;Server.StartOnListener&lt;/code&gt; scans once; &lt;code&gt;RescanPlugins&lt;/code&gt; repeats the scan
and records identities, checksums, and conflicts in state. The server supervises
one serve process per enabled local plugin from its private data directory, with
bounded calls/output, cancellation, restart cutoff and process-group shutdown.
Process health is durable; sensitive stderr stays bounded in local memory.
Authenticated management routes expose catalog, health, grant approval and
revocation, configuration validation/update, restart/retry, and data removal.
Mutations and redacted stderr reads require localhost and safe browser origins.
Configuration reaches the child on a private inherited descriptor before
readiness; failed activation restores the last working public and secret values.
Authenticated action list/invoke/download endpoints use the action broker. It
filters context by each action&amp;rsquo;s approved grants, requires host-issued confirmation
tokens when declared, and returns only typed results. Grant changes serialize with
dispatch and are checked again before results or artifact bytes are returned. See the
&lt;a href="https://forgejo.nousefreak.be/dries/ocman/src/branch/main/internal/plugins/README.md" target="_blank" rel="noopener"&gt;wire contract&lt;/a&gt;
.
The remote &lt;code&gt;PluginOperation&lt;/code&gt; RPC accepts a closed set of host operations, never
raw stdio messages. Both transports use the same lifecycle and action broker.
Catalog identities are &lt;code&gt;(ownerId, description.id)&lt;/code&gt;; nested capabilities and
instances inherit that owner. Management and artifact endpoints take &lt;code&gt;ownerId&lt;/code&gt;.
Action requests distinguish the installation &lt;code&gt;ownerId&lt;/code&gt; from &lt;code&gt;context.ownerId&lt;/code&gt;.
Project/session listings combine the owner&amp;rsquo;s owner-scoped actions with the hub&amp;rsquo;s
hub-scoped actions. Owner-scoped actions run on the owner; hub-scoped actions run
only on the hub. Global placements always resolve to the hub. Explicit disconnected
project owners fail closed. Remote secret updates pass through without hub
persistence, and responses contain secret presence only.
Configuration writes are capped at 1 MiB of canonical JSON; catalog responses
are capped at 16 MiB on both transports.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;sdk/plugin.&lt;/strong&gt; Optional public Go lifecycle and action helpers reuse the canonical
protocol DTOs and validators. &lt;code&gt;sdk/plugin/conformance&lt;/code&gt; runs black-box executable
tests, including host-broker grant checks. &lt;code&gt;examples/ocman-plugin-fixture&lt;/code&gt; is the
deterministic executable used by the supervisor integration tests. Plugins in
other languages can implement the same NDJSON contract directly.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Plugin persistence.&lt;/strong&gt; &lt;code&gt;internal/state&lt;/code&gt; stores discovered identities, capability
instances, enablement, grants, health and public configuration in schema v91.
Schema v92 adds operation receipts committed before action dispatch, preventing
replay after a host restart even when the original outcome is unknown.
Secret snapshots use separate &lt;code&gt;0600&lt;/code&gt; files beside the configured database;
plugin-owned data uses its own directory. Configuration checkpoints restore
public values and secret references together. Disable and removal retain data;
permanent deletion is explicit. Catalog reads expose secret-presence flags only.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;internal/factory.&lt;/strong&gt; The independent Software Factory boundary. It stores
Epics, Mols, typed Issues, dependencies, attempts, Formula revisions,
Plan revisions, approvals, and materialization provenance in &lt;code&gt;state.db&lt;/code&gt;.
YAML Formulas declare named steps with prompts, configuration, and &lt;code&gt;needs&lt;/code&gt;.
They compile to canonical JSON; historical TOML revisions remain readable.
Poured steps keep their frozen definitions in &lt;code&gt;factory_workflow_step&lt;/code&gt;.
Implementation is a group whose required children must succeed before checks
or later approvals can proceed. Verification and delivery expand per changed
project; dependent steps wait for every project instance of each prerequisite.
A Plan session is instructed not to
modify files, but its configurable permission rules may allow shell commands
for research; approval of an exact revision automatically materializes the
proposed Implementation Issues and dependencies atomically. As defined by
&lt;a href="https://ocman.stenic.io/docs/adr/0008-coordinate-factory-work-across-projects/"&gt;ADR 0008&lt;/a&gt;
, an Epic
owns an admitted local project set, each executable Issue targets one project,
and each project has an independent shared branch, checkpoint, and Delivery
lineage. Ready Issues launch configured worktree sessions in their target
project. Each implementation handoff records the clean, pushed commit in its
Attempt result; the next Attempt for that project freezes the checkpoint and
target branch in its policy. Workflow Project Deliveries follow the declared
checks and approvals before publishing each project&amp;rsquo;s PR. Legacy revisions
create deliveries progressively as their required project work completes.
Forge observations satisfy merge-gated cross-project dependencies; a recorded
merge makes later work create a successor instead of refreshing the Delivery.
Delivery retries preserve completed implementation Issues.
Failed or terminally blocked work can
launch a read-only diagnosis session; its scoped &lt;code&gt;factory_unblock&lt;/code&gt; MCP tool
remains permission-gated in the conversation before reopening work or
applying a graph mutation. Implementation agents can also pause behind a
durable project-admission gate; human REST approval adopts the local project,
records execution acknowledgement, and launches an additive scoped Plan,
while rejection resumes the same session with feedback. The browser uses REST
while agents use MCP.
Routines are not involved.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Factory persistence.&lt;/strong&gt; Native &lt;code&gt;factory_*&lt;/code&gt; tables own the graph and its
provenance in &lt;code&gt;state.db&lt;/code&gt;; they do not reference or alter routine tables.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;internal/factory/model.&lt;/strong&gt; Dependency-neutral persistence records shared by
&lt;code&gt;internal/factory&lt;/code&gt; and &lt;code&gt;internal/state&lt;/code&gt;. Factory owns their meaning; state
only stores them, which avoids making Factory depend on its SQLite adapter.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;platforms.Registry.&lt;/strong&gt; The session-scoped seam. One adapter per platform;
remotes register as compound-ID platforms so handlers can&amp;rsquo;t tell local from
remote.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;hostsvc.Router.&lt;/strong&gt; The directory-scoped seam (git, worktrees, tmux, Beads,
projects and forge-repository detection/fetch). It resolves the owning host
and delegates, the same transparency
trick as the registry. Worktree sessions run in-app on the project&amp;rsquo;s single
opencode instance, one per project, ensured through
&lt;code&gt;EnsureProjectOpencode&lt;/code&gt;, with a per-session working directory. There is no
opencode or tmux process per worktree. &lt;code&gt;EnsureProjectOpencodeResult&lt;/code&gt; is
runtime-neutral: callers use the full &lt;code&gt;Endpoint&lt;/code&gt; URL (or its &lt;code&gt;Port()&lt;/code&gt;) plus
an opaque &lt;code&gt;ocruntime.Instance&lt;/code&gt;. The owning host may use discovery once to
adopt a healthy instance that started before its managed registry entry
existed. &lt;code&gt;RestartProjectOpencode&lt;/code&gt; stops and relaunches the tracked
instance.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;internal/ocruntime.&lt;/strong&gt; The runtime abstraction behind the managed launch
path. A &lt;code&gt;Runtime&lt;/code&gt; interface (&lt;code&gt;Launch&lt;/code&gt;/&lt;code&gt;Probe&lt;/code&gt;/&lt;code&gt;Stop&lt;/code&gt;) hides how a project&amp;rsquo;s
opencode is hosted; the native-tmux implementation runs &lt;code&gt;opencode --port N&lt;/code&gt;
on an ocman-allocated loopback port and probes authenticated
&lt;code&gt;GET {endpoint}/config&lt;/code&gt; for health. It is where the container runtime (epic
#375) plugs in as a second implementation.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;platforms/opencode.&lt;/strong&gt; Wraps the read-only DB queries (&lt;code&gt;internal/db&lt;/code&gt;) plus
an HTTP client that attaches to live instances, with &lt;code&gt;lsof&lt;/code&gt;-based discovery
for instances started outside ocman. One process-wide &lt;code&gt;/global/event&lt;/code&gt; stream
per instance keeps pending permission and question state in memory across
all session directories. The same live stream captures completed bash parts
containing Git commit summaries and stores immutable observations in
&lt;code&gt;state.db&lt;/code&gt;; transcript reads never trigger capture.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;internal/routines.&lt;/strong&gt; Validates and stores manual, timeout, one-time and
cron routines. Manual and scheduled dispatch share the durable occurrence
claim, launch a fresh managed session through &lt;code&gt;hostsvc.Router&lt;/code&gt; and
&lt;code&gt;sessionsvc&lt;/code&gt;, and leave the run active until platform-neutral session status
reports success or failure. Startup resumes observation of linked runs.
Timeout schedules become an absolute due time when saved. Cron schedules
use a five-field expression and IANA timezone. Webhook polling is an
independent owner-local/remote delivery loop.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Legacy Workflow storage.&lt;/strong&gt; The &lt;code&gt;workflow_*&lt;/code&gt; tables remain in &lt;code&gt;state.db&lt;/code&gt; as
inert historical data. No API, MCP tool, UI, or scheduler reads them. A DAG
cannot be converted losslessly to one routine prompt, so recovery is a
manual read-only SQLite export.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;internal/mcp.&lt;/strong&gt; MCP handlers expose action-based &lt;code&gt;factory&lt;/code&gt;,
permission-gated &lt;code&gt;factory_unblock&lt;/code&gt;, &lt;code&gt;inbox&lt;/code&gt;, and &lt;code&gt;routines&lt;/code&gt; tools, read-only
session inspection, and &lt;code&gt;embed_file&lt;/code&gt;. File embedding uses signed tokens
persisted in &lt;code&gt;state.db&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Inbox.&lt;/strong&gt; The &lt;code&gt;inbox&lt;/code&gt; MCP tool is deliberately limited to &lt;code&gt;help&lt;/code&gt;, &lt;code&gt;send&lt;/code&gt;, and
&lt;code&gt;recall&lt;/code&gt;. Sends write the owning host&amp;rsquo;s &lt;code&gt;state.db&lt;/code&gt;; remote sends and recalls
cross the owner-routed gRPC seam. The browser alone lists, reads, and
archives Inbox state through REST. Messages carry Permission, Factory, Routine,
or Primary categories. The headless permission watcher creates owner-local
actionable messages even when auto-approval is disabled. Successful replies,
including TUI and automatic replies, archive them; a periodic reconciliation
handles missed events and aborted requests. Permission replies use the existing
session service with an explicit platform, including remote compound IDs.
Routine completion writes its notification in the same transaction as the run
outcome, and Factory deliveries explicitly use the Factory category.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;internal/opencodeskills.&lt;/strong&gt; Extracts binary-embedded ocman skills into
XDG data and installs only ocman-owned symlinks for OpenCode discovery.
Retirement unlinks only the exact verified symlink and preserves extracted data.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;internal/state.&lt;/strong&gt; Ocman&amp;rsquo;s writable SQLite store: migrations, settings,
routines and their immutable run snapshots, legacy inert Workflow rows,
and the independent native Factory Issue graph.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;forge and integrations.&lt;/strong&gt; Forge-agnostic types in &lt;code&gt;internal/forge&lt;/code&gt;, per-forge
HTTP clients in &lt;code&gt;internal/forge/{github,forgejo}&lt;/code&gt;. PR/Issue handlers obtain repository
identity from the owner Host, then use the hub clients for metadata.&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;3. Session and event data flow&lt;span class="hx:absolute hx:-mt-20" id="3-session-and-event-data-flow"&gt;&lt;/span&gt;
&lt;a href="#3-session-and-event-data-flow" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;See &lt;a href="../architecture-events/"&gt;Session and event data flow&lt;/a&gt;
for the sequence
diagram covering session reads, SSE activity updates, remote streams, and
routine dispatch. Sidebar activity updates arrive over global SSE and sort
within stable one-minute buckets, with reduced-motion-aware reorder animation.&lt;/p&gt;
&lt;h2&gt;4. Frontend composition&lt;span class="hx:absolute hx:-mt-20" id="4-frontend-composition"&gt;&lt;/span&gt;
&lt;a href="#4-frontend-composition" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;div role="img" aria-label="Diagram"&gt;
&lt;pre class="mermaid hx:mt-6"&gt;
flowchart TD
Pages[pages/&amp;lt;br/&amp;gt;routes] --&amp;gt; Comp[components/&amp;lt;br/&amp;gt;shared controls + feature UI]
Pages --&amp;gt; Stores[Client state&amp;lt;br/&amp;gt;TanStack Query + Zustand]
Comp --&amp;gt; Stores
Comp --&amp;gt;|plugin Settings + palette actions: explicit ownerId| API
Stores --&amp;gt; API[lib/ API client]
Stores --&amp;gt; SSE[SSE subscription]
Pages --&amp;gt; Scopes[Ref-counted activity scopes]
Comp --&amp;gt; Scopes
Scopes --&amp;gt; Reporter[Client activity lease reporter]
Reporter --&amp;gt; API
API --&amp;gt;|/api| Hub[ocman backend]
SSE --&amp;gt;|events| Hub
Comp --&amp;gt; Caps[useCapabilities&amp;lt;br/&amp;gt;capability gating]
Comp --&amp;gt; Speech[Browser speech synthesis&amp;lt;br/&amp;gt;local or online voice]
&lt;/pre&gt;
&lt;/div&gt;&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Shared controls.&lt;/strong&gt; &lt;code&gt;Control&lt;/code&gt; provides buttons, button groups, text inputs,
textareas, search inputs, and selects with native form semantics and shared
focus, disabled, and invalid styles. &lt;code&gt;IconButton&lt;/code&gt;, &lt;code&gt;RefreshButton&lt;/code&gt;, and
&lt;code&gt;CopyButton&lt;/code&gt; add action feedback; copying is confirmed only after success.
&lt;code&gt;Pagination&lt;/code&gt; shares previous/next controls while callers own indexing, page
boundaries, totals, and requests.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Selection and overlays.&lt;/strong&gt; &lt;code&gt;SegmentedControl&lt;/code&gt; uses native radios for filters.
&lt;code&gt;Tabs&lt;/code&gt; and &lt;code&gt;DropdownMenu&lt;/code&gt; wrap Radix UI for keyboard navigation, focus, and
accessible associations, styled with the app&amp;rsquo;s CSS. Tabs activate on click,
Enter, or Space and unmount inactive content by default. Menu actions that open
dialogs wait for the menu to restore focus so the dialog records a stable
opener. &lt;code&gt;ModalHeader&lt;/code&gt; and &lt;code&gt;ModalFooter&lt;/code&gt; compose the existing &lt;code&gt;Modal&lt;/code&gt;,
&lt;code&gt;IconButton&lt;/code&gt;, and &lt;code&gt;ButtonGroup&lt;/code&gt;; &lt;code&gt;Modal&lt;/code&gt; retains dismissal and focus handling.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Loading and errors.&lt;/strong&gt; &lt;code&gt;LoadingState&lt;/code&gt; pairs a decorative &lt;code&gt;Spinner&lt;/code&gt; with one
status announcement and respects reduced-motion preferences. Structured
skeletons remain separate. &lt;code&gt;InlineAlert&lt;/code&gt; announces errors and can show a
busy-aware retry button; &lt;code&gt;EmptyState&lt;/code&gt; renders empty messages. Callers own
loading, stale-data, error, and empty-state decisions.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Capability gating.&lt;/strong&gt; The UI never branches on platform identity. Features
toggle via &lt;code&gt;/api/capabilities&lt;/code&gt;, enforced by a lint script.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Read aloud.&lt;/strong&gt; Turn-end controls select original final-answer text parts and
use browser speech synthesis. Opt-in autoplay waits for the idle reconciliation
in the focused session tab. Voice preferences stay in browser storage; audio
does not pass through the ocman backend.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Plugin Settings.&lt;/strong&gt; Settings uses shared setting rows and each host&amp;rsquo;s
&lt;code&gt;pluginManagement&lt;/code&gt; capability. The hub probes remote plugin support with a
bounded &lt;code&gt;available&lt;/code&gt; RPC. Catalog, lifecycle, configuration, and stderr calls
carry the selected &lt;code&gt;ownerId&lt;/code&gt;; switching owners clears the previous view.
Enabling requires checksum and grant review. Secret inputs are write-only,
and failed configuration activation reloads persisted configuration and health.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Client state.&lt;/strong&gt; Shared Zustand stores hold broad session state. The
Routines page loads definitions and history over REST and keeps its form and
selected edits locally.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Plugin actions.&lt;/strong&gt; The command palette merges &lt;code&gt;action.v1&lt;/code&gt; contributions for
global, project, and session contexts. It sends opaque IDs and core route names
through the authenticated backend, resolving older contexts independently of
the recent-session search cache. Host dialogs render confirmation text and
typed results, including owner-routed artifact downloads. Confirmation reuses
the operation ID; failures and timeouts never trigger automatic invocation retries.
Plugins supply no browser JavaScript or HTML.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Activity leases.&lt;/strong&gt; Mounted data subscriptions ref-count their scopes. One
authenticated reporter renews the visible tab&amp;rsquo;s lease, allowing the backend
to skip view-serving session, project and metrics refreshes
when every tab is hidden or gone. Scheduled routines and auto-approve do not
consult these leases.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Beads status.&lt;/strong&gt; The right panel queries the repository owner&amp;rsquo;s
&lt;code&gt;hostsvc.Host&lt;/code&gt; through &lt;code&gt;/api/project/beads-status&lt;/code&gt;; remote owners proxy the
same operation over gRPC. Ticket data stays in the repository and is polled
only while the available pane is open.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Factory.&lt;/strong&gt; &lt;code&gt;/factory&lt;/code&gt; presents actionable approval Gates, Epics, Issues,
Queue, and Configuration through TanStack Query. Browser mutations create
native Epics, pour Mols, decide exact Plan revisions, and explicitly close
completed containers. It can also launch a read-only unblock conversation;
approved repairs return through the scoped MCP tool. The dispatcher records
attempts before launching the prompt-constrained planning or configured
implementation session.
Factory MCP results can carry &lt;code&gt;[[ocman:card ...]]&lt;/code&gt; markers in the conversation.
The markdown renderer turns them into creation or human-action cards using
the same TanStack Query state and REST mutations as the Factory pages.
Resolved actions disappear on refresh; rendering a marker never executes it.&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;5. Plugin action and remote projection flow&lt;span class="hx:absolute hx:-mt-20" id="5-plugin-action-and-remote-projection-flow"&gt;&lt;/span&gt;
&lt;a href="#5-plugin-action-and-remote-projection-flow" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;div role="img" aria-label="Diagram"&gt;
&lt;pre class="mermaid hx:mt-6"&gt;
sequenceDiagram
participant B as Browser palette / Settings
participant H as Hub HTTP handlers
participant O as Installation owner
participant A as internal/plugins action broker
participant D as Owner state.db
participant P as External plugin process
B-&amp;gt;&amp;gt;H: Catalog / management with ownerId
H-&amp;gt;&amp;gt;O: Local call or authenticated PluginOperation RPC
O--&amp;gt;&amp;gt;B: Via hub: owner-qualified catalog, safe health, secret presence
B-&amp;gt;&amp;gt;H: List actions for placement and context owner
H-&amp;gt;&amp;gt;O: Owner-scoped actions; merge hub-scoped actions
H--&amp;gt;&amp;gt;B: Typed action declarations
B-&amp;gt;&amp;gt;H: Invoke with installation ownerId and stable operationId
H-&amp;gt;&amp;gt;O: Route to installation owner
O-&amp;gt;&amp;gt;A: Validate enablement, scope, grants, confirmation
opt Confirmation required
A--&amp;gt;&amp;gt;B: Via owner and hub: confirmation text and bound token
B-&amp;gt;&amp;gt;H: Same invocation and operationId plus confirmed token
H-&amp;gt;&amp;gt;O: Route confirmed invocation
O-&amp;gt;&amp;gt;A: Revalidate and admit
end
A-&amp;gt;&amp;gt;D: Commit operation receipt before dispatch
A-&amp;gt;&amp;gt;P: NDJSON action.v1 invoke with minimized context
P--&amp;gt;&amp;gt;A: One typed terminal result
A--&amp;gt;&amp;gt;B: Via owner and hub: authorized results / artifact handles
B-&amp;gt;&amp;gt;H: Download artifact with installation ownerId
H-&amp;gt;&amp;gt;O: Recheck grants and read owner-local artifact
O--&amp;gt;&amp;gt;B: Via hub: attachment bytes
&lt;/pre&gt;
&lt;/div&gt;&lt;ul&gt;
&lt;li&gt;The installation owner supervises the process and owns its configuration,
secret files, operation receipts, and in-memory results. The hub projects
remote operations through a closed RPC, never raw plugin stdio.&lt;/li&gt;
&lt;li&gt;The browser can target a hub installation while carrying a remote project&amp;rsquo;s
context. These are separate owner identities; disconnected explicit project
owners fail closed. Global placements resolve to the hub.&lt;/li&gt;
&lt;li&gt;Confirmation and result rendering belong to ocman. Calls are not replayed after
crashes, and durable receipts prevent uncertain side effects from being repeated
after host restart. Downloads recheck the installation&amp;rsquo;s current grants.&lt;/li&gt;
&lt;li&gt;Only &lt;code&gt;action.v1&lt;/code&gt; is shipped. Conversation-provider, platform-provider, iframe UI,
relay inbox capability, registry/updates, signatures, sandboxing, Slack, and
Codex remain future work. Existing core Inbox and webhook services do not imply
a plugin capability for either.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;See &lt;a href="https://ocman.stenic.io/docs/features/plugins/"&gt;Plugins&lt;/a&gt;
for the operator and authoring guide.&lt;/p&gt;</description></item><item><title>Contributing</title><link>https://ocman.stenic.io/docs/other/contributing/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://ocman.stenic.io/docs/other/contributing/</guid><description>
&lt;h2&gt;Architecture&lt;span class="hx:absolute hx:-mt-20" id="architecture"&gt;&lt;/span&gt;
&lt;a href="#architecture" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;div role="img" aria-label="Diagram"&gt;
&lt;pre class="mermaid hx:mt-6"&gt;
graph TD
subgraph Browser
SPA[React SPA]
end
subgraph ocman Server
HTTP[HTTP Server]
Static[Embedded Static Assets]
Registry[Platform Registry]
Archive[Auto-Archive Goroutine]
end
subgraph Adapters
OCAd[OpenCode adapter]
end
subgraph Storage
OC_DB[(OpenCode DB&amp;lt;br/&amp;gt;read-only)]
State_DB[(ocman state.db&amp;lt;br/&amp;gt;read-write)]
end
subgraph External
OC[Running OpenCode instances]
end
SPA --&amp;gt;|/api/*| HTTP
SPA --&amp;gt;|static files| Static
HTTP --&amp;gt; Registry
Registry --&amp;gt; OCAd
OCAd --&amp;gt; OC_DB
OCAd --&amp;gt;|lsof + HTTP| OC
HTTP --&amp;gt;|archived/seen| State_DB
Archive --&amp;gt; State_DB
&lt;/pre&gt;
&lt;/div&gt;&lt;h2&gt;Project structure&lt;span class="hx:absolute hx:-mt-20" id="project-structure"&gt;&lt;/span&gt;
&lt;a href="#project-structure" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;div class="hextra-code-block hx:relative hx:mt-6 hx:first:mt-0 hx:group/code"&gt;
&lt;div&gt;&lt;pre&gt;&lt;code&gt;main.go entrypoint (-addr, -db, -platforms, -auth-* flags)
frontend/ React &amp;#43; TypeScript &amp;#43; Vite SPA
e2e/ Playwright end-to-end suite
internal/platforms/ Platform interface, Registry, common types/errors
internal/platforms/opencode/ OpenCode adapter (DB &amp;#43; HTTP proxy)
internal/db/ SQLite queries against OpenCode&amp;#39;s DB
internal/state/ ocman&amp;#39;s own writable state DB (archived/seen,
auth secret, favorites, cached projects)
internal/server/ HTTP server, API handlers, static file serving,
OpenCode port discovery, auth
internal/webui/static/ Vite build output (embedded into Go binary)
internal/tmux/ tmux process control &amp;#43; opencode launchers
internal/term/ in-app browser terminals (tmux windows &amp;#43; PTY bridge)
internal/whisper/ voice transcription via local whisper-cpp
internal/autoapprove/ LLM-judged permission auto-approve pipeline
spec/ Requirements &amp;#43; architecture notes per feature&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;div class="hextra-code-copy-btn-container hx:opacity-0 hx:transition hx:group-hover/code:opacity-100 hx:flex hx:gap-1 hx:absolute hx:m-[11px] hx:right-0 hx:top-0"&gt;
&lt;button
class="hextra-code-copy-btn hx:group/copybtn hx:cursor-pointer hx:transition-all hx:active:opacity-50 hx:bg-primary-700/5 hx:border hx:border-black/5 hx:text-gray-600 hx:hover:text-gray-900 hx:rounded-md hx:p-1.5 hx:dark:bg-primary-300/10 hx:dark:border-white/10 hx:dark:text-gray-400 hx:dark:hover:text-gray-50"
title="Copy code"
aria-label="Copy code"
data-copied-label="Copied!"
&gt;
&lt;div class="hextra-copy-icon hx:group-[.copied]/copybtn:hidden hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;div class="hextra-success-icon hx:hidden hx:group-[.copied]/copybtn:block hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;/button&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;h2&gt;Development&lt;span class="hx:absolute hx:-mt-20" id="development"&gt;&lt;/span&gt;
&lt;a href="#development" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;h3&gt;Requirements&lt;span class="hx:absolute hx:-mt-20" id="requirements"&gt;&lt;/span&gt;
&lt;a href="#requirements" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;Go 1.24+&lt;/li&gt;
&lt;li&gt;Node.js 22+ (provided by &lt;code&gt;mise&lt;/code&gt;)&lt;/li&gt;
&lt;li&gt;&lt;a href="https://pnpm.io/" target="_blank" rel="noopener"&gt;pnpm&lt;/a&gt;
(provided by &lt;code&gt;mise&lt;/code&gt;; pinned via &lt;code&gt;packageManager&lt;/code&gt; in &lt;code&gt;frontend/package.json&lt;/code&gt;)&lt;/li&gt;
&lt;li&gt;&lt;a href="https://mise.jdx.dev/" target="_blank" rel="noopener"&gt;mise&lt;/a&gt;
, which provides &lt;code&gt;air&lt;/code&gt;, &lt;code&gt;node&lt;/code&gt;, and &lt;code&gt;pnpm&lt;/code&gt; (&lt;code&gt;mise install&lt;/code&gt;)&lt;/li&gt;
&lt;li&gt;&lt;code&gt;tmux&lt;/code&gt; on &lt;code&gt;PATH&lt;/code&gt; for the in-UI launcher&lt;/li&gt;
&lt;/ul&gt;
&lt;blockquote&gt;
&lt;p&gt;All Node-side commands use &lt;code&gt;pnpm&lt;/code&gt;. &lt;code&gt;npm&lt;/code&gt; is no longer supported: running
&lt;code&gt;npm install&lt;/code&gt; will not produce the expected &lt;code&gt;pnpm-lock.yaml&lt;/code&gt;, and CI
rejects it.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h3&gt;Quick command reference&lt;span class="hx:absolute hx:-mt-20" id="quick-command-reference"&gt;&lt;/span&gt;
&lt;a href="#quick-command-reference" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h3&gt;&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Command&lt;/th&gt;
&lt;th&gt;Port&lt;/th&gt;
&lt;th&gt;Live Reload&lt;/th&gt;
&lt;th&gt;Use Case&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;make dev&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;8228&lt;/td&gt;
&lt;td&gt;Yes (HMR)&lt;/td&gt;
&lt;td&gt;Regular development&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;make dev-prod-watch&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;8228&lt;/td&gt;
&lt;td&gt;Manual refresh&lt;/td&gt;
&lt;td&gt;Memory profiling, production testing&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;make dev-prod&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;8228&lt;/td&gt;
&lt;td&gt;No&lt;/td&gt;
&lt;td&gt;One-time production test&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;h3&gt;All commands&lt;span class="hx:absolute hx:-mt-20" id="all-commands"&gt;&lt;/span&gt;
&lt;a href="#all-commands" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h3&gt;&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Command&lt;/th&gt;
&lt;th&gt;Description&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;make dev&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;Backend (air) + frontend (vite dev) with HMR on :8228&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;make dev-prod&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;Backend (air) + frontend (vite preview of production build) on :8228&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;make dev-prod-watch&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;Backend (air) + frontend (vite preview, auto-rebuilds on changes) on :8228&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;make dev-backend&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;Go backend only (air, port 8229)&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;make dev-frontend&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;Vite dev server only (port 8228)&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;make test&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;&lt;code&gt;go test ./...&lt;/code&gt;, &lt;code&gt;vitest run&lt;/code&gt;, and the Playwright e2e suite&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;make lint&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;&lt;code&gt;go vet&lt;/code&gt;, &lt;code&gt;tsc -b&lt;/code&gt;, &lt;code&gt;eslint&lt;/code&gt;, and the platform-branching check&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;make build&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;Production build (frontend then Go binary)&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;make clean&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;Remove binary, tmp/, and static/assets/&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;h3&gt;Build pipeline&lt;span class="hx:absolute hx:-mt-20" id="build-pipeline"&gt;&lt;/span&gt;
&lt;a href="#build-pipeline" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h3&gt;&lt;ol&gt;
&lt;li&gt;&lt;code&gt;cd frontend &amp;amp;&amp;amp; pnpm install --frozen-lockfile &amp;amp;&amp;amp; pnpm build&lt;/code&gt; builds the frontend into &lt;code&gt;internal/webui/static/&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;&lt;code&gt;go build -o ocman .&lt;/code&gt; embeds &lt;code&gt;internal/webui/static/&lt;/code&gt; via &lt;code&gt;//go:embed&lt;/code&gt;.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Order matters: build the frontend before &lt;code&gt;go build&lt;/code&gt;.&lt;/p&gt;
&lt;h3&gt;Development workflows&lt;span class="hx:absolute hx:-mt-20" id="development-workflows"&gt;&lt;/span&gt;
&lt;a href="#development-workflows" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h3&gt;&lt;p&gt;Regular frontend development:&lt;/p&gt;
&lt;div class="hextra-code-block hx:relative hx:mt-6 hx:first:mt-0 hx:group/code"&gt;
&lt;div&gt;&lt;div class="highlight"&gt;&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;make dev
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="c1"&gt;# → Instant HMR updates; best for UI iteration&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class="hextra-code-copy-btn-container hx:opacity-0 hx:transition hx:group-hover/code:opacity-100 hx:flex hx:gap-1 hx:absolute hx:m-[11px] hx:right-0 hx:top-0"&gt;
&lt;button
class="hextra-code-copy-btn hx:group/copybtn hx:cursor-pointer hx:transition-all hx:active:opacity-50 hx:bg-primary-700/5 hx:border hx:border-black/5 hx:text-gray-600 hx:hover:text-gray-900 hx:rounded-md hx:p-1.5 hx:dark:bg-primary-300/10 hx:dark:border-white/10 hx:dark:text-gray-400 hx:dark:hover:text-gray-50"
title="Copy code"
aria-label="Copy code"
data-copied-label="Copied!"
&gt;
&lt;div class="hextra-copy-icon hx:group-[.copied]/copybtn:hidden hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;div class="hextra-success-icon hx:hidden hx:group-[.copied]/copybtn:block hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;/button&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;p&gt;Memory profiling or production testing:&lt;/p&gt;
&lt;div class="hextra-code-block hx:relative hx:mt-6 hx:first:mt-0 hx:group/code"&gt;
&lt;div&gt;&lt;div class="highlight"&gt;&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-bash" data-lang="bash"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;make dev-prod-watch
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="c1"&gt;# → Edit code → auto-rebuild → manually refresh browser&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class="hextra-code-copy-btn-container hx:opacity-0 hx:transition hx:group-hover/code:opacity-100 hx:flex hx:gap-1 hx:absolute hx:m-[11px] hx:right-0 hx:top-0"&gt;
&lt;button
class="hextra-code-copy-btn hx:group/copybtn hx:cursor-pointer hx:transition-all hx:active:opacity-50 hx:bg-primary-700/5 hx:border hx:border-black/5 hx:text-gray-600 hx:hover:text-gray-900 hx:rounded-md hx:p-1.5 hx:dark:bg-primary-300/10 hx:dark:border-white/10 hx:dark:text-gray-400 hx:dark:hover:text-gray-50"
title="Copy code"
aria-label="Copy code"
data-copied-label="Copied!"
&gt;
&lt;div class="hextra-copy-icon hx:group-[.copied]/copybtn:hidden hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;div class="hextra-success-icon hx:hidden hx:group-[.copied]/copybtn:block hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;/button&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;blockquote&gt;
&lt;p&gt;Vite&amp;rsquo;s preview mode has no HMR. The watcher rebuilds automatically but you refresh the
browser yourself. That&amp;rsquo;s deliberate: production builds are for testing, not rapid
iteration.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2&gt;Tests&lt;span class="hx:absolute hx:-mt-20" id="tests"&gt;&lt;/span&gt;
&lt;a href="#tests" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Three suites: Go tests (unit and integration), frontend tests in vitest, and a Playwright
end-to-end suite covering auth, the dashboard, composer, SSE streaming, and prompts. CI runs
all three on every PR, and &lt;code&gt;make test&lt;/code&gt; runs them locally. Coverage is ratcheted, so new code
ships with tests.&lt;/p&gt;
&lt;h2&gt;Platform-agnostic frontend&lt;span class="hx:absolute hx:-mt-20" id="platform-agnostic-frontend"&gt;&lt;/span&gt;
&lt;a href="#platform-agnostic-frontend" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;The frontend must not branch on &lt;code&gt;session.platform === 'opencode'&lt;/code&gt; (or similar string comparisons).
UI capability gating goes through &lt;code&gt;/api/capabilities&lt;/code&gt; and &lt;code&gt;useCapabilities()&lt;/code&gt; instead.
&lt;code&gt;scripts/check-platform-branching.sh&lt;/code&gt; enforces this, and &lt;code&gt;make lint&lt;/code&gt; runs it. If you hit a false
positive, the pragma &lt;code&gt;// ocman:allow-platform-branch&lt;/code&gt; suppresses the check on the next line. Use
it sparingly, for generic helpers that genuinely need the platform identity.&lt;/p&gt;</description></item><item><title>Profiling</title><link>https://ocman.stenic.io/docs/other/profiling/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://ocman.stenic.io/docs/other/profiling/</guid><description>
&lt;p&gt;Where ocman&amp;rsquo;s performance work stands. This records the suspects we found
during recon, the instrumentation we added to measure them, and the fixes that
&lt;em&gt;should&lt;/em&gt; follow once we have data, in priority order.&lt;/p&gt;
&lt;p&gt;The starting symptom: the frontend occasionally feels stuck, and it wasn&amp;rsquo;t
clear whether slow API calls or the UI itself caused it.&lt;/p&gt;
&lt;h2&gt;Phase 1: reconnaissance findings (OpenCode-only)&lt;span class="hx:absolute hx:-mt-20" id="phase-1-reconnaissance-findings-opencode-only"&gt;&lt;/span&gt;
&lt;a href="#phase-1-reconnaissance-findings-opencode-only" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;ocman&amp;rsquo;s default deployment runs only the OpenCode adapter
(&lt;code&gt;-platforms opencode&lt;/code&gt;). Findings below assume that configuration.&lt;/p&gt;
&lt;h3&gt;Polling map (steady state, idle session)&lt;span class="hx:absolute hx:-mt-20" id="polling-map-steady-state-idle-session"&gt;&lt;/span&gt;
&lt;a href="#polling-map-steady-state-idle-session" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h3&gt;&lt;p&gt;When a user is on the &lt;code&gt;SessionDetail&lt;/code&gt; page, these all run
simultaneously:&lt;/p&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;#&lt;/th&gt;
&lt;th&gt;Source&lt;/th&gt;
&lt;th&gt;Cadence&lt;/th&gt;
&lt;th&gt;Notes&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;1&lt;/td&gt;
&lt;td&gt;&lt;code&gt;App.tsx&lt;/code&gt; → &lt;code&gt;useFaviconNotify&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;10 s&lt;/td&gt;
&lt;td&gt;&lt;code&gt;GET /api/sessions/notify&lt;/code&gt;&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;2&lt;/td&gt;
&lt;td&gt;&lt;code&gt;App.tsx&lt;/code&gt; → &lt;code&gt;useBellNotify&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;10 s&lt;/td&gt;
&lt;td&gt;Independent poller hitting the same endpoint&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;3&lt;/td&gt;
&lt;td&gt;&lt;code&gt;App.tsx&lt;/code&gt; → &lt;code&gt;BackendStats&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;5 s&lt;/td&gt;
&lt;td&gt;&lt;code&gt;GET /api/system/stats&lt;/code&gt;&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;4&lt;/td&gt;
&lt;td&gt;&lt;code&gt;App.tsx&lt;/code&gt; → &lt;code&gt;useMemoryMonitor&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;30 s&lt;/td&gt;
&lt;td&gt;Local-only, fine&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;5&lt;/td&gt;
&lt;td&gt;&lt;code&gt;App.tsx&lt;/code&gt; → &lt;code&gt;usePerformanceCleanup&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;60 s (dev)&lt;/td&gt;
&lt;td&gt;Local-only, fine&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;6&lt;/td&gt;
&lt;td&gt;&lt;code&gt;SessionDetail&lt;/code&gt; line ~1078&lt;/td&gt;
&lt;td&gt;5 s&lt;/td&gt;
&lt;td&gt;Sidebar &lt;code&gt;recentSessions&lt;/code&gt; refresh&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;7&lt;/td&gt;
&lt;td&gt;&lt;code&gt;SessionDetail&lt;/code&gt; line ~1752&lt;/td&gt;
&lt;td&gt;10 s&lt;/td&gt;
&lt;td&gt;Fallback &lt;code&gt;GET /api/session/{id}&lt;/code&gt; when SSE drops&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;8&lt;/td&gt;
&lt;td&gt;&lt;code&gt;SessionDetail&lt;/code&gt; line ~2416&lt;/td&gt;
&lt;td&gt;2 s&lt;/td&gt;
&lt;td&gt;While a Task tool is running&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;9&lt;/td&gt;
&lt;td&gt;&lt;code&gt;SessionDetail&lt;/code&gt; line ~2523&lt;/td&gt;
&lt;td&gt;1 s&lt;/td&gt;
&lt;td&gt;Recompute live tokens/sec&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;10&lt;/td&gt;
&lt;td&gt;&lt;code&gt;SessionDetail&lt;/code&gt; line ~579&lt;/td&gt;
&lt;td&gt;&lt;strong&gt;0 ms&lt;/strong&gt; (≈250 Hz)&lt;/td&gt;
&lt;td&gt;Polls Zustand for the palette command&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;11&lt;/td&gt;
&lt;td&gt;&lt;code&gt;SessionDetail&lt;/code&gt; SSE&lt;/td&gt;
&lt;td&gt;streaming&lt;/td&gt;
&lt;td&gt;&lt;code&gt;GET /api/session/{id}/events&lt;/code&gt;&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;On the dashboard:&lt;/p&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;#&lt;/th&gt;
&lt;th&gt;Source&lt;/th&gt;
&lt;th&gt;Cadence&lt;/th&gt;
&lt;th&gt;Notes&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;12&lt;/td&gt;
&lt;td&gt;&lt;code&gt;Dashboard.tsx&lt;/code&gt; line 132&lt;/td&gt;
&lt;td&gt;5 s&lt;/td&gt;
&lt;td&gt;&lt;code&gt;GET /api/sessions&lt;/code&gt; (full fan-out + git status, all the time)&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;/td&gt;
&lt;td&gt;+ 1-5 above&lt;/td&gt;
&lt;td&gt;&lt;/td&gt;
&lt;td&gt;&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;h3&gt;Top suspects&lt;span class="hx:absolute hx:-mt-20" id="top-suspects"&gt;&lt;/span&gt;
&lt;a href="#top-suspects" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h3&gt;&lt;p&gt;The list is ordered by likelihood × impact for an OpenCode-only
deployment. The numeric IDs (S1, S2, …) are referenced from the
Phase 3 task list further down.&lt;/p&gt;
&lt;h4&gt;S1: &lt;code&gt;setInterval(..., 0)&lt;/code&gt; in &lt;code&gt;SessionDetail.tsx:579&lt;/code&gt;&lt;span class="hx:absolute hx:-mt-20" id="s1-setinterval-0-in-sessiondetailtsx579"&gt;&lt;/span&gt;
&lt;a href="#s1-setinterval-0-in-sessiondetailtsx579" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h4&gt;&lt;div class="hextra-code-block hx:relative hx:mt-6 hx:first:mt-0 hx:group/code"&gt;
&lt;div&gt;&lt;div class="highlight"&gt;&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-ts" data-lang="ts"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nx"&gt;useEffect&lt;/span&gt;&lt;span class="p"&gt;(()&lt;/span&gt; &lt;span class="o"&gt;=&amp;gt;&lt;/span&gt; &lt;span class="p"&gt;{&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; &lt;span class="kr"&gt;const&lt;/span&gt; &lt;span class="nx"&gt;interval&lt;/span&gt; &lt;span class="o"&gt;=&lt;/span&gt; &lt;span class="nx"&gt;setInterval&lt;/span&gt;&lt;span class="p"&gt;(()&lt;/span&gt; &lt;span class="o"&gt;=&amp;gt;&lt;/span&gt; &lt;span class="p"&gt;{&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; &lt;span class="kr"&gt;const&lt;/span&gt; &lt;span class="nx"&gt;cmd&lt;/span&gt; &lt;span class="o"&gt;=&lt;/span&gt; &lt;span class="nx"&gt;useUiStore&lt;/span&gt;&lt;span class="p"&gt;.&lt;/span&gt;&lt;span class="nx"&gt;getState&lt;/span&gt;&lt;span class="p"&gt;().&lt;/span&gt;&lt;span class="nx"&gt;paletteCommand&lt;/span&gt;&lt;span class="p"&gt;;&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; &lt;span class="k"&gt;if&lt;/span&gt; &lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="o"&gt;!&lt;/span&gt;&lt;span class="nx"&gt;cmd&lt;/span&gt; &lt;span class="o"&gt;||&lt;/span&gt; &lt;span class="nx"&gt;cmd&lt;/span&gt;&lt;span class="p"&gt;.&lt;/span&gt;&lt;span class="nx"&gt;kind&lt;/span&gt; &lt;span class="o"&gt;!==&lt;/span&gt; &lt;span class="s1"&gt;&amp;#39;scoped&amp;#39;&lt;/span&gt;&lt;span class="p"&gt;)&lt;/span&gt; &lt;span class="k"&gt;return&lt;/span&gt;&lt;span class="p"&gt;;&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; &lt;span class="p"&gt;...&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; &lt;span class="p"&gt;},&lt;/span&gt; &lt;span class="mi"&gt;0&lt;/span&gt;&lt;span class="p"&gt;);&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; &lt;span class="k"&gt;return&lt;/span&gt; &lt;span class="p"&gt;()&lt;/span&gt; &lt;span class="o"&gt;=&amp;gt;&lt;/span&gt; &lt;span class="nx"&gt;clearInterval&lt;/span&gt;&lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="nx"&gt;interval&lt;/span&gt;&lt;span class="p"&gt;);&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="p"&gt;},&lt;/span&gt; &lt;span class="p"&gt;[&lt;/span&gt;&lt;span class="nx"&gt;tmux&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt; &lt;span class="nx"&gt;archiveSession&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt; &lt;span class="nx"&gt;createSession&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt; &lt;span class="nx"&gt;navigate&lt;/span&gt;&lt;span class="p"&gt;]);&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class="hextra-code-copy-btn-container hx:opacity-0 hx:transition hx:group-hover/code:opacity-100 hx:flex hx:gap-1 hx:absolute hx:m-[11px] hx:right-0 hx:top-0"&gt;
&lt;button
class="hextra-code-copy-btn hx:group/copybtn hx:cursor-pointer hx:transition-all hx:active:opacity-50 hx:bg-primary-700/5 hx:border hx:border-black/5 hx:text-gray-600 hx:hover:text-gray-900 hx:rounded-md hx:p-1.5 hx:dark:bg-primary-300/10 hx:dark:border-white/10 hx:dark:text-gray-400 hx:dark:hover:text-gray-50"
title="Copy code"
aria-label="Copy code"
data-copied-label="Copied!"
&gt;
&lt;div class="hextra-copy-icon hx:group-[.copied]/copybtn:hidden hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;div class="hextra-success-icon hx:hidden hx:group-[.copied]/copybtn:block hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;/button&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;p&gt;&lt;code&gt;setInterval(0)&lt;/code&gt; clamps to ≈4 ms per the HTML spec, so this fires
~250 times per second forever, just to read a Zustand store. The
dependency array also includes &lt;code&gt;tmux&lt;/code&gt; (which probably changes on
parent re-renders), so the interval is also re-created frequently.&lt;/p&gt;
&lt;p&gt;This is the strongest candidate for the &amp;ldquo;stuck&amp;rdquo; feeling. It can
saturate the event loop on a busy machine and fight against React
scheduling. Suggested fix: convert to a &lt;code&gt;useUiStore.subscribe&lt;/code&gt;
selector or a normal &lt;code&gt;useEffect&lt;/code&gt; keyed on &lt;code&gt;paletteCommand&lt;/code&gt;.&lt;/p&gt;
&lt;h4&gt;S2: &lt;code&gt;/api/sessions&lt;/code&gt; cost on a 5 s loop&lt;span class="hx:absolute hx:-mt-20" id="s2-apisessions-cost-on-a-5-s-loop"&gt;&lt;/span&gt;
&lt;a href="#s2-apisessions-cost-on-a-5-s-loop" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h4&gt;&lt;p&gt;Per request, &lt;code&gt;handleSessions&lt;/code&gt; does:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;db.GetSessions&lt;/code&gt;: one SQLite query, indexed reads, fast.&lt;/li&gt;
&lt;li&gt;&lt;code&gt;discoverOpenCodePorts&lt;/code&gt;: runs &lt;code&gt;lsof&lt;/code&gt; on macOS/Linux. &lt;strong&gt;3 s TTL
cache&lt;/strong&gt;, so the dashboard&amp;rsquo;s 5 s poll cadence means roughly every
&lt;em&gt;other&lt;/em&gt; request shells out. macOS &lt;code&gt;lsof&lt;/code&gt; invocations are
non-trivial (~30-150 ms) and fork a process.&lt;/li&gt;
&lt;li&gt;&lt;code&gt;collectPendingPromptsByDir(ports)&lt;/code&gt;: HTTP probes to every running
OpenCode instance.&lt;/li&gt;
&lt;li&gt;&lt;code&gt;applyGitInfo&lt;/code&gt;: up to 8 parallel &lt;code&gt;git status&lt;/code&gt; per unique dir,
30 s TTL. With many sessions across many repos, every 30 s = burst
of fork/exec.&lt;/li&gt;
&lt;li&gt;SQLite reads in &lt;code&gt;applySessionState&lt;/code&gt; (archived/seen).&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The dominant cost on the steady-state path is &lt;strong&gt;lsof + per-port HTTP
probes + git&lt;/strong&gt;, none of which are about the database. Worth
verifying with the timing middleware before optimising.&lt;/p&gt;
&lt;h4&gt;S3: &lt;code&gt;SessionDetail.tsx&lt;/code&gt; size and render fan-out&lt;span class="hx:absolute hx:-mt-20" id="s3-sessiondetailtsx-size-and-render-fan-out"&gt;&lt;/span&gt;
&lt;a href="#s3-sessiondetailtsx-size-and-render-fan-out" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h4&gt;&lt;p&gt;3102 lines, many &lt;code&gt;useEffect&lt;/code&gt; chains, no obvious virtualization on
&lt;code&gt;parts&lt;/code&gt;. SSE event firehose during a busy turn could be triggering
large re-renders. &lt;code&gt;useInfiniteRows.ts&lt;/code&gt; exists but I haven&amp;rsquo;t traced
where it&amp;rsquo;s wired.&lt;/p&gt;
&lt;h4&gt;S4: duplicate &lt;code&gt;/api/sessions/notify&lt;/code&gt; pollers (favicon + bell)&lt;span class="hx:absolute hx:-mt-20" id="s4-duplicate-apisessionsnotify-pollers-favicon--bell"&gt;&lt;/span&gt;
&lt;a href="#s4-duplicate-apisessionsnotify-pollers-favicon--bell" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h4&gt;&lt;p&gt;&lt;code&gt;useFaviconNotify&lt;/code&gt; and &lt;code&gt;useBellNotify&lt;/code&gt; independently call
&lt;code&gt;api.sessionsNotify&lt;/code&gt; every 10 s with identical params. Free
request-coalescing if combined into one shared store subscription.&lt;/p&gt;
&lt;h4&gt;S5: dashboard polls while hidden&lt;span class="hx:absolute hx:-mt-20" id="s5-dashboard-polls-while-hidden"&gt;&lt;/span&gt;
&lt;a href="#s5-dashboard-polls-while-hidden" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h4&gt;&lt;p&gt;&lt;code&gt;SessionDetail&lt;/code&gt;&amp;rsquo;s sidebar refresh pauses on &lt;code&gt;document.hidden&lt;/code&gt;, but
&lt;code&gt;Dashboard.tsx:130-134&lt;/code&gt; keeps polling regardless. Background tabs
keep flogging the backend.&lt;/p&gt;
&lt;h3&gt;Things that are already good&lt;span class="hx:absolute hx:-mt-20" id="things-that-are-already-good"&gt;&lt;/span&gt;
&lt;a href="#things-that-are-already-good" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;&lt;code&gt;apiStore&lt;/code&gt; request-status tracking is well-structured (single
source of truth).&lt;/li&gt;
&lt;li&gt;&lt;code&gt;internal/git&lt;/code&gt; (status lookup) has a per-dir 30 s cache, an 8-worker bound, a 2 s
timeout, and dedups by dir within a request.&lt;/li&gt;
&lt;li&gt;&lt;code&gt;SessionDetail&lt;/code&gt;&amp;rsquo;s sidebar refresh pauses on &lt;code&gt;document.hidden&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;SSE path exists; the 10 s fallback only fires when SSE is broken.&lt;/li&gt;
&lt;li&gt;Session-detail cache in &lt;code&gt;apiStore&lt;/code&gt; (3-entry LRU) makes back-nav
instant.&lt;/li&gt;
&lt;li&gt;&lt;code&gt;useMemoryMonitor&lt;/code&gt; + &lt;code&gt;usePerformanceCleanup&lt;/code&gt; already exist, and
their comments document past memory-pressure history.&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Phase 2: instrumentation (landed)&lt;span class="hx:absolute hx:-mt-20" id="phase-2-instrumentation-landed"&gt;&lt;/span&gt;
&lt;a href="#phase-2-instrumentation-landed" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Three independent, low-risk additions, each on its own commit:&lt;/p&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Commit&lt;/th&gt;
&lt;th&gt;Subject&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;dc57a65&lt;/td&gt;
&lt;td&gt;&lt;code&gt;feat: Log per-request HTTP timing as structured logrus events&lt;/code&gt;&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;7bca3ae&lt;/td&gt;
&lt;td&gt;&lt;code&gt;feat: Track main-thread long tasks and surface in footer stats&lt;/code&gt;&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;93db4f6&lt;/td&gt;
&lt;td&gt;&lt;code&gt;feat: Buffer recent API call timings under window.__ocmanPerf&lt;/code&gt;&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;h3&gt;What landed&lt;span class="hx:absolute hx:-mt-20" id="what-landed"&gt;&lt;/span&gt;
&lt;a href="#what-landed" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h3&gt;&lt;p&gt;&lt;strong&gt;A. Backend request-timing middleware&lt;/strong&gt;
(&lt;code&gt;internal/server/middleware.go&lt;/code&gt;)&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Wraps the mux. Logs &lt;code&gt;method&lt;/code&gt;, &lt;code&gt;path&lt;/code&gt;, &lt;code&gt;status&lt;/code&gt;, &lt;code&gt;duration_ms&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;INFO when &lt;code&gt;duration ≥ 250 ms&lt;/code&gt;, DEBUG otherwise.&lt;/li&gt;
&lt;li&gt;Skips SSE (&lt;code&gt;/api/session/{id}/events&lt;/code&gt;) and the recursive
&lt;code&gt;/api/debug/log&lt;/code&gt; sink.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;B. Frontend long-task observer&lt;/strong&gt;
(&lt;code&gt;frontend/src/lib/useLongTaskMonitor.ts&lt;/code&gt;)&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;PerformanceObserver({ entryTypes: ['longtask'] })&lt;/code&gt;, counts
main-thread blocks &amp;gt; 50 ms.&lt;/li&gt;
&lt;li&gt;Footer shows &lt;code&gt;lt: N / Xms&lt;/code&gt;; amber at &lt;code&gt;maxMs ≥ 100 ms&lt;/code&gt;, red at
&lt;code&gt;maxMs ≥ 250 ms&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;Safari (no longtask support) is a clean no-op.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;C. Frontend fetch timing ring buffer&lt;/strong&gt;
(&lt;code&gt;frontend/src/lib/perfRing.ts&lt;/code&gt; + &lt;code&gt;api.ts&lt;/code&gt; wrap)&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Last 100 calls kept in memory with normalized URL templates
(so all &lt;code&gt;/api/session/abc.../info&lt;/code&gt; aggregate as
&lt;code&gt;/api/session/:id/info&lt;/code&gt;).&lt;/li&gt;
&lt;li&gt;Devtools handles:
&lt;div class="hextra-code-block hx:relative hx:mt-6 hx:first:mt-0 hx:group/code"&gt;
&lt;div&gt;&lt;div class="highlight"&gt;&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-js" data-lang="js"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nx"&gt;__ocmanPerf&lt;/span&gt;&lt;span class="p"&gt;.&lt;/span&gt;&lt;span class="nx"&gt;summary&lt;/span&gt;&lt;span class="p"&gt;()&lt;/span&gt; &lt;span class="c1"&gt;// pathTemplate × percentiles
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nx"&gt;__ocmanPerf&lt;/span&gt;&lt;span class="p"&gt;.&lt;/span&gt;&lt;span class="nx"&gt;entries&lt;/span&gt;&lt;span class="p"&gt;()&lt;/span&gt; &lt;span class="c1"&gt;// chronological list
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nx"&gt;__ocmanPerf&lt;/span&gt;&lt;span class="p"&gt;.&lt;/span&gt;&lt;span class="nx"&gt;clear&lt;/span&gt;&lt;span class="p"&gt;()&lt;/span&gt; &lt;span class="c1"&gt;// reset before reproducing a stall
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nx"&gt;console&lt;/span&gt;&lt;span class="p"&gt;.&lt;/span&gt;&lt;span class="nx"&gt;table&lt;/span&gt;&lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="nx"&gt;__ocmanPerf&lt;/span&gt;&lt;span class="p"&gt;.&lt;/span&gt;&lt;span class="nx"&gt;summary&lt;/span&gt;&lt;span class="p"&gt;())&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class="hextra-code-copy-btn-container hx:opacity-0 hx:transition hx:group-hover/code:opacity-100 hx:flex hx:gap-1 hx:absolute hx:m-[11px] hx:right-0 hx:top-0"&gt;
&lt;button
class="hextra-code-copy-btn hx:group/copybtn hx:cursor-pointer hx:transition-all hx:active:opacity-50 hx:bg-primary-700/5 hx:border hx:border-black/5 hx:text-gray-600 hx:hover:text-gray-900 hx:rounded-md hx:p-1.5 hx:dark:bg-primary-300/10 hx:dark:border-white/10 hx:dark:text-gray-400 hx:dark:hover:text-gray-50"
title="Copy code"
aria-label="Copy code"
data-copied-label="Copied!"
&gt;
&lt;div class="hextra-copy-icon hx:group-[.copied]/copybtn:hidden hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;div class="hextra-success-icon hx:hidden hx:group-[.copied]/copybtn:block hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;/button&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;How to use it&lt;span class="hx:absolute hx:-mt-20" id="how-to-use-it"&gt;&lt;/span&gt;
&lt;a href="#how-to-use-it" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h3&gt;&lt;p&gt;When the UI next feels stuck:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;Watch the footer.&lt;/strong&gt; A red &lt;code&gt;lt: N / Xms&lt;/code&gt; value confirms the
problem is main-thread blocking, not API latency. Strong signal
for S1 / S3.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Open devtools and run&lt;/strong&gt;
&lt;code&gt;console.table(__ocmanPerf.summary())&lt;/code&gt;. The summary is sorted by
max latency descending. Look for endpoints with high &lt;code&gt;p95Ms&lt;/code&gt; or
&lt;code&gt;maxMs&lt;/code&gt;. &lt;code&gt;/api/sessions&lt;/code&gt; consistently &amp;gt; 200 ms ⇒ S2 confirmed.
Everything &amp;lt; 50 ms ⇒ the frontend is the problem.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Tail server logs&lt;/strong&gt; (e.g. &lt;code&gt;tmp/air.log&lt;/code&gt;) for
&lt;code&gt;level=info msg=&amp;quot;http request&amp;quot;&lt;/code&gt; lines. The &lt;code&gt;duration_ms&lt;/code&gt; field
tells you what the &lt;em&gt;server&lt;/em&gt; was doing during a perceived stall.&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;What&amp;rsquo;s intentionally not measured&lt;span class="hx:absolute hx:-mt-20" id="whats-intentionally-not-measured"&gt;&lt;/span&gt;
&lt;a href="#whats-intentionally-not-measured" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;Inline &lt;code&gt;fetch()&lt;/code&gt; calls in &lt;code&gt;api.ts&lt;/code&gt; aren&amp;rsquo;t instrumented
(createSession, sendMessage, archiveSession, transcribe, etc.).
Those are user-initiated POSTs; &lt;code&gt;perfRing&lt;/code&gt; focuses on the
polling-driven GETs that we suspect drive the stuckness.&lt;/li&gt;
&lt;li&gt;No persistence. Closing the tab loses the ring.&lt;/li&gt;
&lt;li&gt;No alerting; the footer color tiers are the only UI signal.&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Phase 2.5: measurement results&lt;span class="hx:absolute hx:-mt-20" id="phase-25-measurement-results"&gt;&lt;/span&gt;
&lt;a href="#phase-25-measurement-results" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;A representative &lt;code&gt;__ocmanPerf.summary()&lt;/code&gt; snapshot, taken after ≈3
minutes of &amp;ldquo;sit on dashboard, click a session, look at it&amp;rdquo;:&lt;/p&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;pathTemplate&lt;/th&gt;
&lt;th&gt;count&lt;/th&gt;
&lt;th&gt;avgMs&lt;/th&gt;
&lt;th&gt;p50Ms&lt;/th&gt;
&lt;th&gt;p95Ms&lt;/th&gt;
&lt;th&gt;maxMs&lt;/th&gt;
&lt;th&gt;err&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;/api/session/:id/models&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;3&lt;/td&gt;
&lt;td&gt;2287&lt;/td&gt;
&lt;td&gt;2948&lt;/td&gt;
&lt;td&gt;2948&lt;/td&gt;
&lt;td&gt;2948&lt;/td&gt;
&lt;td&gt;0&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;/api/session/:id/info&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;1&lt;/td&gt;
&lt;td&gt;1876&lt;/td&gt;
&lt;td&gt;1876&lt;/td&gt;
&lt;td&gt;1876&lt;/td&gt;
&lt;td&gt;1876&lt;/td&gt;
&lt;td&gt;0&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;/api/git/diff&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;1&lt;/td&gt;
&lt;td&gt;1875&lt;/td&gt;
&lt;td&gt;1875&lt;/td&gt;
&lt;td&gt;1875&lt;/td&gt;
&lt;td&gt;1875&lt;/td&gt;
&lt;td&gt;0&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;/api/session/:id/changes&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;1&lt;/td&gt;
&lt;td&gt;1872&lt;/td&gt;
&lt;td&gt;1872&lt;/td&gt;
&lt;td&gt;1872&lt;/td&gt;
&lt;td&gt;1872&lt;/td&gt;
&lt;td&gt;0&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;/api/sessions/notify&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;21&lt;/td&gt;
&lt;td&gt;293&lt;/td&gt;
&lt;td&gt;266&lt;/td&gt;
&lt;td&gt;310&lt;/td&gt;
&lt;td&gt;1854&lt;/td&gt;
&lt;td&gt;0&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;/api/session/:id/commands&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;2&lt;/td&gt;
&lt;td&gt;1341&lt;/td&gt;
&lt;td&gt;1345&lt;/td&gt;
&lt;td&gt;1345&lt;/td&gt;
&lt;td&gt;1345&lt;/td&gt;
&lt;td&gt;0&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;/api/session/:id&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;3&lt;/td&gt;
&lt;td&gt;842&lt;/td&gt;
&lt;td&gt;1157&lt;/td&gt;
&lt;td&gt;1345&lt;/td&gt;
&lt;td&gt;1345&lt;/td&gt;
&lt;td&gt;1&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;/api/sessions&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;37&lt;/td&gt;
&lt;td&gt;198&lt;/td&gt;
&lt;td&gt;128&lt;/td&gt;
&lt;td&gt;522&lt;/td&gt;
&lt;td&gt;1340&lt;/td&gt;
&lt;td&gt;0&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;/api/session/:id/agents&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;1&lt;/td&gt;
&lt;td&gt;965&lt;/td&gt;
&lt;td&gt;965&lt;/td&gt;
&lt;td&gt;965&lt;/td&gt;
&lt;td&gt;965&lt;/td&gt;
&lt;td&gt;0&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;/api/session/:id/permissions&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;1&lt;/td&gt;
&lt;td&gt;758&lt;/td&gt;
&lt;td&gt;758&lt;/td&gt;
&lt;td&gt;758&lt;/td&gt;
&lt;td&gt;758&lt;/td&gt;
&lt;td&gt;0&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;/api/session/:id/questions&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;1&lt;/td&gt;
&lt;td&gt;758&lt;/td&gt;
&lt;td&gt;758&lt;/td&gt;
&lt;td&gt;758&lt;/td&gt;
&lt;td&gt;758&lt;/td&gt;
&lt;td&gt;0&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;/api/tmux/sessions&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;4&lt;/td&gt;
&lt;td&gt;30&lt;/td&gt;
&lt;td&gt;45&lt;/td&gt;
&lt;td&gt;47&lt;/td&gt;
&lt;td&gt;47&lt;/td&gt;
&lt;td&gt;0&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;/api/system/stats&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;20&lt;/td&gt;
&lt;td&gt;11&lt;/td&gt;
&lt;td&gt;8&lt;/td&gt;
&lt;td&gt;38&lt;/td&gt;
&lt;td&gt;38&lt;/td&gt;
&lt;td&gt;0&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;/api/whisper/status&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;2&lt;/td&gt;
&lt;td&gt;35&lt;/td&gt;
&lt;td&gt;36&lt;/td&gt;
&lt;td&gt;36&lt;/td&gt;
&lt;td&gt;36&lt;/td&gt;
&lt;td&gt;0&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;/api/projects&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;2&lt;/td&gt;
&lt;td&gt;11&lt;/td&gt;
&lt;td&gt;12&lt;/td&gt;
&lt;td&gt;12&lt;/td&gt;
&lt;td&gt;12&lt;/td&gt;
&lt;td&gt;0&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;The frontend &lt;code&gt;lt:&lt;/code&gt; counter did &lt;em&gt;not&lt;/em&gt; turn red during this session.&lt;/p&gt;
&lt;h3&gt;What the data overturned&lt;span class="hx:absolute hx:-mt-20" id="what-the-data-overturned"&gt;&lt;/span&gt;
&lt;a href="#what-the-data-overturned" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;S1 was wrong.&lt;/strong&gt; Long-task counter stayed quiet, so the
&lt;code&gt;setInterval(0)&lt;/code&gt; palette poller is not the cause of perceived
stalls. Still worth fixing for cleanliness, but it&amp;rsquo;s not the
bottleneck.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;The bottleneck is the OpenCode HTTP proxy.&lt;/strong&gt; Every per-session
call that fans out to the running OpenCode instance lands in the
750 ms to 2.95 s range. &lt;code&gt;/api/system/stats&lt;/code&gt; (38 ms) and
&lt;code&gt;/api/projects&lt;/code&gt; (12 ms) confirm the Go process is fine when it
doesn&amp;rsquo;t talk upstream.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;/api/sessions&lt;/code&gt; p95 522 ms / max 1340 ms is real&lt;/strong&gt; and matches S2:
the lsof + per-port pending-prompts fan-out path. Average is fine
because the 3 s lsof cache hides most calls; the tail is ugly
because cache expiry forces a synchronous fan-out.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;/api/session/:id/changes&lt;/code&gt; at 1.87 s is a mystery.&lt;/strong&gt; That handler
is a pure DB walk over parts; no HTTP. Either contention with a
sibling endpoint, or a missing index, or just a single noisy sample
(count = 1).&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;Code-side findings (independent of timing)&lt;span class="hx:absolute hx:-mt-20" id="code-side-findings-independent-of-timing"&gt;&lt;/span&gt;
&lt;a href="#code-side-findings-independent-of-timing" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h3&gt;&lt;p&gt;These came out of reading the proxy code paths after seeing the
numbers. Each one is referenced from a B-task below.&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;Catalog endpoints (&lt;code&gt;/agent&lt;/code&gt;, &lt;code&gt;/command&lt;/code&gt;, &lt;code&gt;/provider&lt;/code&gt;) are
uncached.&lt;/strong&gt; Configuration data that changes rarely is re-fetched
on every call. &lt;strong&gt;B1.&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;SessionInfo&lt;/code&gt; makes 4 upstream HTTP calls per request:&lt;/strong&gt;
&lt;code&gt;/mcp&lt;/code&gt;, &lt;code&gt;/lsp&lt;/code&gt;, &lt;code&gt;/provider&lt;/code&gt; in parallel, then &lt;code&gt;/session/{id}/message&lt;/code&gt;
sequentially after the parallel block. The 4th can join the wait
group. &lt;strong&gt;B2.&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Lsof discovery has TTL but no singleflight.&lt;/strong&gt; A cold mount fires
5+ endpoints simultaneously; the first does lsof while the others
block on the mutex. &lt;code&gt;golang.org/x/sync/singleflight&lt;/code&gt; would coalesce
them. &lt;strong&gt;B3.&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;/api/session/:id&lt;/code&gt; proxies to OpenCode by default.&lt;/strong&gt; The DB has
the same data and SSE pushes the live deltas. The live HTTP path
exists for token/cost freshness during an active turn, but it
costs 1.34 s p95 on every session refresh. &lt;strong&gt;B4.&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;/api/sessions&lt;/code&gt; re-queries &lt;code&gt;/permission&lt;/code&gt; and &lt;code&gt;/question&lt;/code&gt; per
running OpenCode every 5 s.&lt;/strong&gt; No cache, even though the SSE stream
already pushes these for the connected session. &lt;strong&gt;B6.&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;/api/session/:id/changes&lt;/code&gt; cost is unexplained.&lt;/strong&gt; Single sample,
pure DB walk. Needs a server-side timing split before we know
whether to fix anything. &lt;strong&gt;B5.&lt;/strong&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h2&gt;Phase 2.6: diagnostic patterns (lessons learned)&lt;span class="hx:absolute hx:-mt-20" id="phase-26-diagnostic-patterns-lessons-learned"&gt;&lt;/span&gt;
&lt;a href="#phase-26-diagnostic-patterns-lessons-learned" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;These are heuristics for diagnosing future regressions. They came
out of chasing the wrong thing once and not wanting to do it again.&lt;/p&gt;
&lt;h3&gt;&lt;code&gt;/api/system/stats&lt;/code&gt; is the canary&lt;span class="hx:absolute hx:-mt-20" id="apisystemstats-is-the-canary"&gt;&lt;/span&gt;
&lt;a href="#apisystemstats-is-the-canary" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h3&gt;&lt;p&gt;&lt;code&gt;/api/system/stats&lt;/code&gt; does pure local work. It reads
&lt;code&gt;runtime.MemStats&lt;/code&gt; and a few uptime values. There is no path in
that handler that can take more than single-digit milliseconds
under any realistic load.&lt;/p&gt;
&lt;p&gt;If &lt;code&gt;__ocmanPerf.summary()&lt;/code&gt; shows &lt;code&gt;/api/system/stats&lt;/code&gt; slow
&lt;strong&gt;alongside&lt;/strong&gt; other endpoints, the &lt;strong&gt;Go process itself is being
blocked&lt;/strong&gt;. Causes that fit this pattern:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Upstream OpenCode is hung, and ocman has many goroutines piled up
waiting on it (network resource pressure, GC roots).&lt;/li&gt;
&lt;li&gt;A goroutine is spinning at 100% CPU, starving the scheduler.&lt;/li&gt;
&lt;li&gt;The OS is paging hard (memory pressure), so even pure-Go work
takes seconds.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;Do not start optimising ocman code based on this signal.&lt;/strong&gt; Check
upstream and the system first:&lt;/p&gt;
&lt;div class="hextra-code-block hx:relative hx:mt-6 hx:first:mt-0 hx:group/code"&gt;
&lt;div&gt;&lt;div class="highlight"&gt;&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-sh" data-lang="sh"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="c1"&gt;# Is OpenCode itself slow? (Replace 4096 with your actual port.)&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nb"&gt;time&lt;/span&gt; curl -s http://127.0.0.1:4096/permission &amp;gt; /dev/null
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="c1"&gt;# How many running OpenCode instances?&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;lsof -iTCP -sTCP:LISTEN -P -n &lt;span class="p"&gt;|&lt;/span&gt; grep opencode
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="c1"&gt;# Is one ocman goroutine pegging a CPU?&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;ps -o pid,pcpu,pmem,etime,command -p &lt;span class="s2"&gt;&amp;#34;&lt;/span&gt;&lt;span class="k"&gt;$(&lt;/span&gt;pgrep -f &lt;span class="s1"&gt;&amp;#39;tmp/ocman&amp;#39;&lt;/span&gt; &lt;span class="p"&gt;|&lt;/span&gt; head -1&lt;span class="k"&gt;)&lt;/span&gt;&lt;span class="s2"&gt;&amp;#34;&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class="hextra-code-copy-btn-container hx:opacity-0 hx:transition hx:group-hover/code:opacity-100 hx:flex hx:gap-1 hx:absolute hx:m-[11px] hx:right-0 hx:top-0"&gt;
&lt;button
class="hextra-code-copy-btn hx:group/copybtn hx:cursor-pointer hx:transition-all hx:active:opacity-50 hx:bg-primary-700/5 hx:border hx:border-black/5 hx:text-gray-600 hx:hover:text-gray-900 hx:rounded-md hx:p-1.5 hx:dark:bg-primary-300/10 hx:dark:border-white/10 hx:dark:text-gray-400 hx:dark:hover:text-gray-50"
title="Copy code"
aria-label="Copy code"
data-copied-label="Copied!"
&gt;
&lt;div class="hextra-copy-icon hx:group-[.copied]/copybtn:hidden hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;div class="hextra-success-icon hx:hidden hx:group-[.copied]/copybtn:block hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;/button&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;p&gt;If &lt;code&gt;/api/system/stats&lt;/code&gt; is fast (single-digit ms) but other
endpoints are slow, &lt;em&gt;that&amp;rsquo;s&lt;/em&gt; when ocman code is at fault and
optimisation work makes sense.&lt;/p&gt;
&lt;h3&gt;Idle vs. active comparison&lt;span class="hx:absolute hx:-mt-20" id="idle-vs-active-comparison"&gt;&lt;/span&gt;
&lt;a href="#idle-vs-active-comparison" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h3&gt;&lt;p&gt;The most reliable comparison is &lt;strong&gt;two &lt;code&gt;__ocmanPerf&lt;/code&gt; snapshots from
the same dashboard layout&lt;/strong&gt;: one with the tab idle for ≥ 30 seconds,
one after a few minutes of normal use. The idle snapshot isolates
ocman&amp;rsquo;s polling cost; the active snapshot adds the per-session
catalog and proxy paths. Comparing them tells you which side
regressions live on.&lt;/p&gt;
&lt;h2&gt;Phase 3: backend optimization plan&lt;span class="hx:absolute hx:-mt-20" id="phase-3-backend-optimization-plan"&gt;&lt;/span&gt;
&lt;a href="#phase-3-backend-optimization-plan" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Tasks are keyed &lt;code&gt;B1&lt;/code&gt;…&lt;code&gt;B7&lt;/code&gt;. Each is independent; any subset can be
shipped. The order below is impact-first within reasonable risk.
&lt;strong&gt;Re-run &lt;code&gt;__ocmanPerf.summary()&lt;/code&gt; after each landed task&lt;/strong&gt; to confirm
the next one is still worth doing.&lt;/p&gt;
&lt;h3&gt;Status&lt;span class="hx:absolute hx:-mt-20" id="status"&gt;&lt;/span&gt;
&lt;a href="#status" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h3&gt;&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Task&lt;/th&gt;
&lt;th&gt;Status&lt;/th&gt;
&lt;th&gt;Commit&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;B5&lt;/td&gt;
&lt;td&gt;landed&lt;/td&gt;
&lt;td&gt;&lt;code&gt;feat: Log per-operation timing for SessionChanges DB calls&lt;/code&gt;&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;B1&lt;/td&gt;
&lt;td&gt;landed&lt;/td&gt;
&lt;td&gt;&lt;code&gt;feat: Cache OpenCode catalog endpoints with 30s TTL and singleflight&lt;/code&gt;&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;B3&lt;/td&gt;
&lt;td&gt;landed&lt;/td&gt;
&lt;td&gt;&lt;code&gt;feat: Singleflight OpenCode port discovery to coalesce concurrent lsof&lt;/code&gt;&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;B2&lt;/td&gt;
&lt;td&gt;landed&lt;/td&gt;
&lt;td&gt;&lt;code&gt;perf: Run liveTokensAndCost in the SessionInfo parallel fan-out&lt;/code&gt;&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;B6&lt;/td&gt;
&lt;td&gt;landed (+timeout)&lt;/td&gt;
&lt;td&gt;&lt;code&gt;fix: Cap pending-prompt fetch at 500ms…&lt;/code&gt; + &lt;code&gt;perf: Cache pending-prompt fetches per port with 3s TTL&lt;/code&gt;&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;B4&lt;/td&gt;
&lt;td&gt;deferred&lt;/td&gt;
&lt;td&gt;Needs DB-vs-live freshness verification (see open questions)&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;B7&lt;/td&gt;
&lt;td&gt;deferred&lt;/td&gt;
&lt;td&gt;Only if measurements still show unexplained Go-side latency&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;The B6 work landed in two commits because the canary pattern above
turned up something else: a hung OpenCode instance was dragging every
dashboard request past 10 s through the shared 10 s HTTP timeout. The
fix is a tight 500 ms per-call timeout &lt;em&gt;plus&lt;/em&gt; the planned 3 s TTL
cache. Together, one slow instance no longer blocks the dashboard
fan-out, and the cache window amortises the steady-state poll cost.&lt;/p&gt;
&lt;h3&gt;B5: instrument &lt;code&gt;/api/session/:id/changes&lt;/code&gt; (do first, lowest risk)&lt;span class="hx:absolute hx:-mt-20" id="b5-instrument-apisessionidchanges-do-first-lowest-risk"&gt;&lt;/span&gt;
&lt;a href="#b5-instrument-apisessionidchanges-do-first-lowest-risk" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h3&gt;&lt;p&gt;The 1.87 s sample is unexplained. Before touching anything, split
that endpoint&amp;rsquo;s timing into &amp;ldquo;DB&amp;rdquo; vs &amp;ldquo;everything else&amp;rdquo; so we know
whether it&amp;rsquo;s a real bottleneck or a single noisy sample.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;File: &lt;code&gt;internal/server/handlers.go&lt;/code&gt; (handler) and a thin
&lt;code&gt;time.Since&lt;/code&gt; wrapper around the DB call inside the OpenCode
adapter&amp;rsquo;s &lt;code&gt;SessionChanges&lt;/code&gt; method.&lt;/li&gt;
&lt;li&gt;Acceptance: log line includes &lt;code&gt;db_ms&lt;/code&gt;, &lt;code&gt;total_ms&lt;/code&gt; for that
endpoint at INFO level when total exceeds the slow threshold.&lt;/li&gt;
&lt;li&gt;Risk: zero (instrumentation only).&lt;/li&gt;
&lt;li&gt;Effort: 30 min.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;B1: cache &lt;code&gt;/agent&lt;/code&gt;, &lt;code&gt;/command&lt;/code&gt;, &lt;code&gt;/provider&lt;/code&gt; per (port, endpoint)&lt;span class="hx:absolute hx:-mt-20" id="b1-cache-agent-command-provider-per-port-endpoint"&gt;&lt;/span&gt;
&lt;a href="#b1-cache-agent-command-provider-per-port-endpoint" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h3&gt;&lt;p&gt;These are configuration data; OpenCode itself rebuilds them rarely.
A 30-second TTL cache eliminates the bulk of the per-session-page
load cost.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;File: &lt;code&gt;internal/platforms/opencode/client.go&lt;/code&gt;, where a
&lt;code&gt;getJSONCached(port, path, ttl)&lt;/code&gt; helper goes.&lt;/li&gt;
&lt;li&gt;Wire into &lt;code&gt;AgentCatalog&lt;/code&gt;, &lt;code&gt;SlashCommands&lt;/code&gt;, and &lt;code&gt;fetchOpenCodeProviders&lt;/code&gt;.
The latter is shared by &lt;code&gt;SessionModels&lt;/code&gt; and &lt;code&gt;SessionInfo&lt;/code&gt;, so
both endpoints benefit.&lt;/li&gt;
&lt;li&gt;TTL: start at 30 s; expose as a constant for easy tuning.&lt;/li&gt;
&lt;li&gt;Acceptance: re-running the same &lt;code&gt;__ocmanPerf&lt;/code&gt; flow shows
&lt;code&gt;/api/session/:id/{models,agents,commands}&lt;/code&gt; p95 &amp;lt; 100 ms after
the first cache miss.&lt;/li&gt;
&lt;li&gt;Risk: low. Stale config for ≤ TTL. Worst case: user changes
agents.json and waits 30 s to see it.&lt;/li&gt;
&lt;li&gt;Effort: 2-3 hours including TTL/race tests.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;B3: singleflight the lsof port discovery&lt;span class="hx:absolute hx:-mt-20" id="b3-singleflight-the-lsof-port-discovery"&gt;&lt;/span&gt;
&lt;a href="#b3-singleflight-the-lsof-port-discovery" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h3&gt;&lt;p&gt;When five endpoints fire on a SessionDetail mount, only the first
should pay for lsof. Today they all serialize on a &lt;code&gt;sync.Mutex&lt;/code&gt; and
take turns observing the same answer.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;File: &lt;code&gt;internal/platforms/opencode/client.go&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;Add &lt;code&gt;golang.org/x/sync/singleflight.Group&lt;/code&gt; around the uncached
path. Keep the TTL cache as the fast path.&lt;/li&gt;
&lt;li&gt;Acceptance: a synthetic test that fires N concurrent
&lt;code&gt;discoverOpenCodePorts&lt;/code&gt; from a cold cache observes exactly one
uncached call.&lt;/li&gt;
&lt;li&gt;Risk: low. Standard library pattern.&lt;/li&gt;
&lt;li&gt;Effort: 1 h including the test.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;B2: fold &lt;code&gt;liveTokensAndCost&lt;/code&gt; into the SessionInfo parallel fan-out&lt;span class="hx:absolute hx:-mt-20" id="b2-fold-livetokensandcost-into-the-sessioninfo-parallel-fan-out"&gt;&lt;/span&gt;
&lt;a href="#b2-fold-livetokensandcost-into-the-sessioninfo-parallel-fan-out" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h3&gt;&lt;p&gt;&lt;code&gt;SessionInfo&lt;/code&gt; currently runs three calls in parallel, then a fourth
sequentially. Move all four into the same wait group.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;File: &lt;code&gt;internal/platforms/opencode/info.go&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;Acceptance: &lt;code&gt;/api/session/:id/info&lt;/code&gt; p95 drops to roughly
&lt;code&gt;max(individual upstream call)&lt;/code&gt; instead of
&lt;code&gt;sum(parallel block) + liveTokensAndCost&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;Risk: trivial. The four calls are independent; nothing in
&lt;code&gt;liveTokensAndCost&lt;/code&gt; depends on &lt;code&gt;mcp&lt;/code&gt; / &lt;code&gt;lsp&lt;/code&gt; / &lt;code&gt;provider&lt;/code&gt; results.&lt;/li&gt;
&lt;li&gt;Effort: 30 min.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;B4: stop preferring live over DB for &lt;code&gt;/api/session/:id&lt;/code&gt; (proposal, needs verification)&lt;span class="hx:absolute hx:-mt-20" id="b4-stop-preferring-live-over-db-for-apisessionid-proposal-needs-verification"&gt;&lt;/span&gt;
&lt;a href="#b4-stop-preferring-live-over-db-for-apisessionid-proposal-needs-verification" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h3&gt;&lt;p&gt;&lt;code&gt;fetchSessionFromOpenCode&lt;/code&gt; proxies two upstream calls (&lt;code&gt;/session/{id}&lt;/code&gt;
and &lt;code&gt;/session/{id}/message&lt;/code&gt;) on every refresh, even though the DB
has the same data and SSE delivers the live deltas. Cost: 1.34 s
p95 per refresh.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Open question before implementing:&lt;/strong&gt; how stale is the DB during
an active turn? OpenCode&amp;rsquo;s persistence cadence is one INSERT per
streamed delta, so it should be ≤ 1 s behind. Need to confirm by
diffing the DB rollup against &lt;code&gt;liveTokensAndCost&lt;/code&gt; while a session
is generating.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Files: &lt;code&gt;internal/platforms/opencode/operations.go&lt;/code&gt;, possibly a
query-flag plumbed through &lt;code&gt;platforms.Platform.Session&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;Acceptance: &lt;code&gt;/api/session/:id&lt;/code&gt; p95 &amp;lt; 100 ms in normal use; explicit
refresh button still hits the live path; tokens/cost in the
Composer don&amp;rsquo;t visibly lag during a turn (manual verification).&lt;/li&gt;
&lt;li&gt;Risk: medium. The reason live-by-default exists is composer
freshness; we&amp;rsquo;d need to confirm SSE-driven updates fully replace
it.&lt;/li&gt;
&lt;li&gt;Effort: 2-3 h, mostly verification.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;B6: cache pending prompts per port&lt;span class="hx:absolute hx:-mt-20" id="b6-cache-pending-prompts-per-port"&gt;&lt;/span&gt;
&lt;a href="#b6-cache-pending-prompts-per-port" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h3&gt;&lt;p&gt;&lt;code&gt;/api/sessions&lt;/code&gt; and &lt;code&gt;/api/sessions/notify&lt;/code&gt; both fan out to every
running OpenCode for &lt;code&gt;/permission&lt;/code&gt; and &lt;code&gt;/question&lt;/code&gt; on every call.
A 3-second TTL (matching the lsof cache) eliminates ~60-80% of
those calls.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;File: &lt;code&gt;internal/platforms/opencode/client.go&lt;/code&gt;
(&lt;code&gt;fetchPendingPrompts&lt;/code&gt;).&lt;/li&gt;
&lt;li&gt;Acceptance: &lt;code&gt;/api/sessions&lt;/code&gt; p95 drops below 200 ms; max latency
no longer spikes at TTL boundaries the way it does today.&lt;/li&gt;
&lt;li&gt;Risk: low. Stale prompt state for ≤ 3 s; the SSE stream still
pushes real-time updates to any connected SessionDetail.&lt;/li&gt;
&lt;li&gt;Effort: 1 h.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;B7: add backend pprof endpoint (only if needed after B1-B6)&lt;span class="hx:absolute hx:-mt-20" id="b7-add-backend-pprof-endpoint-only-if-needed-after-b1-b6"&gt;&lt;/span&gt;
&lt;a href="#b7-add-backend-pprof-endpoint-only-if-needed-after-b1-b6" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h3&gt;&lt;p&gt;If post-B1 measurements still show unexplained Go-side latency, add
&lt;code&gt;net/http/pprof&lt;/code&gt; behind a build flag or a dev-only env var so we can
profile lock contention, GC, or syscalls.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;File: &lt;code&gt;internal/server/server.go&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;Risk: zero (gated, off by default in production).&lt;/li&gt;
&lt;li&gt;Effort: 15 min.&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Frontend cleanup (lower priority)&lt;span class="hx:absolute hx:-mt-20" id="frontend-cleanup-lower-priority"&gt;&lt;/span&gt;
&lt;a href="#frontend-cleanup-lower-priority" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;The original Phase 1 frontend suspects (S1, S4, S5) are not the
cause of perceived stalls per the measurement, but they&amp;rsquo;re still
worth fixing for cleanliness and to reduce backend load. Suggested
order if/when we come back to them:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;F1 (= old S5).&lt;/strong&gt; Pause Dashboard polling on &lt;code&gt;document.hidden&lt;/code&gt;.
30 min, very low risk. Reduces idle traffic on background tabs.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;F2 (= old S4).&lt;/strong&gt; Coalesce favicon + bell pollers into one
shared &lt;code&gt;/api/sessions/notify&lt;/code&gt; poll. 2 h, low risk. Halves the
dashboard-adjacent backend pressure.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;F3 (= old S1).&lt;/strong&gt; Replace the &lt;code&gt;setInterval(0)&lt;/code&gt; palette
dispatcher with &lt;code&gt;useUiStore.subscribe&lt;/code&gt;. 1-2 h, low risk.
Cleanliness fix; not on the perf hot path.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;These are deliberately not in the B-numbered list because the
backend wins are larger and unblock more user pain.&lt;/p&gt;
&lt;h2&gt;Open questions / things to revisit&lt;span class="hx:absolute hx:-mt-20" id="open-questions--things-to-revisit"&gt;&lt;/span&gt;
&lt;a href="#open-questions--things-to-revisit" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Does B4 actually break composer freshness?&lt;/strong&gt; Needs a controlled
test: open a session, send a prompt, watch the cost number.
Verify that the DB-only path stays within ≤1 s of the live path.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;What&amp;rsquo;s the actual story on &lt;code&gt;/api/session/:id/changes&lt;/code&gt;?&lt;/strong&gt; B5&amp;rsquo;s
instrumentation now logs &lt;code&gt;parts_ms&lt;/code&gt; and &lt;code&gt;session_ms&lt;/code&gt; separately
whenever the operation exceeds 200 ms. Next time the endpoint
shows up slow in &lt;code&gt;__ocmanPerf&lt;/code&gt;, grep the server log for
&lt;code&gt;op=session_changes&lt;/code&gt; and look at the field breakdown. That
tells us whether it&amp;rsquo;s the parts query, the session metadata
fetch, or the in-memory aggregation.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Connection reuse to OpenCode:&lt;/strong&gt; the upstream &lt;code&gt;http.Client&lt;/code&gt; has
no custom transport. Default &lt;code&gt;http.DefaultTransport&lt;/code&gt; should reuse
loopback connections, but the latencies were too high for warm
reuse during the original measurements. After B1/B2/B3/B6 most
per-call latency is now upstream-bounded, so this is less
pressing. Revisit only if measurements show OpenCode itself
responding instantly while ocman still reports proxy latency.&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Reference: relevant files&lt;span class="hx:absolute hx:-mt-20" id="reference-relevant-files"&gt;&lt;/span&gt;
&lt;a href="#reference-relevant-files" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;h3&gt;Backend&lt;span class="hx:absolute hx:-mt-20" id="backend"&gt;&lt;/span&gt;
&lt;a href="#backend" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;&lt;code&gt;internal/server/server.go&lt;/code&gt;: mux assembly, middleware wiring.&lt;/li&gt;
&lt;li&gt;&lt;code&gt;internal/server/middleware.go&lt;/code&gt;: request-timing logger.&lt;/li&gt;
&lt;li&gt;&lt;code&gt;internal/server/handlers.go&lt;/code&gt;: &lt;code&gt;/api/sessions&lt;/code&gt;, &lt;code&gt;/api/sessions/notify&lt;/code&gt;,
&lt;code&gt;applyGitInfo&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;&lt;code&gt;internal/platforms/opencode/adapter.go&lt;/code&gt;: Sessions(), port discovery.&lt;/li&gt;
&lt;li&gt;&lt;code&gt;internal/platforms/opencode/client.go&lt;/code&gt;: upstream HTTP helpers
(&lt;code&gt;getJSON&lt;/code&gt;, lsof discovery, prompt fetch). Where B1/B3/B6 land.&lt;/li&gt;
&lt;li&gt;&lt;code&gt;internal/platforms/opencode/operations.go&lt;/code&gt;: per-session catalog
methods (&lt;code&gt;AgentCatalog&lt;/code&gt;, &lt;code&gt;SlashCommands&lt;/code&gt;, &lt;code&gt;SessionModels&lt;/code&gt;,
&lt;code&gt;Session&lt;/code&gt;). Where B4 lands.&lt;/li&gt;
&lt;li&gt;&lt;code&gt;internal/platforms/opencode/info.go&lt;/code&gt;: &lt;code&gt;SessionInfo&lt;/code&gt; parallel
fan-out. Where B2 lands.&lt;/li&gt;
&lt;li&gt;&lt;code&gt;internal/git/info.go&lt;/code&gt;: git status cache (30 s TTL).&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;Frontend&lt;span class="hx:absolute hx:-mt-20" id="frontend"&gt;&lt;/span&gt;
&lt;a href="#frontend" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;&lt;code&gt;frontend/src/App.tsx&lt;/code&gt;: top-level pollers and dev handles.&lt;/li&gt;
&lt;li&gt;&lt;code&gt;frontend/src/pages/Dashboard.tsx&lt;/code&gt;: 5 s &lt;code&gt;/api/sessions&lt;/code&gt; poller.&lt;/li&gt;
&lt;li&gt;&lt;code&gt;frontend/src/pages/SessionDetail.tsx&lt;/code&gt;: the polling jungle.&lt;/li&gt;
&lt;li&gt;&lt;code&gt;frontend/src/lib/api.ts&lt;/code&gt;: &lt;code&gt;fetchJSON&lt;/code&gt; / &lt;code&gt;postJSON&lt;/code&gt; (instrumented).&lt;/li&gt;
&lt;li&gt;&lt;code&gt;frontend/src/lib/perfRing.ts&lt;/code&gt;: perf ring buffer.&lt;/li&gt;
&lt;li&gt;&lt;code&gt;frontend/src/lib/useLongTaskMonitor.ts&lt;/code&gt;: long-task observer.&lt;/li&gt;
&lt;li&gt;&lt;code&gt;frontend/src/components/BackendStats.tsx&lt;/code&gt;: footer display.&lt;/li&gt;
&lt;/ul&gt;</description></item><item><title>Session commits</title><link>https://ocman.stenic.io/docs/features/session-commits/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://ocman.stenic.io/docs/features/session-commits/</guid><description>
&lt;h2&gt;Session commits&lt;span class="hx:absolute hx:-mt-20" id="session-commits"&gt;&lt;/span&gt;
&lt;a href="#session-commits" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Session Info shows commits that ocman observes in live OpenCode bash results.
Capture is best effort and runs in the headless OpenCode event watcher, so it
does not depend on an open browser or permission judging being enabled.&lt;/p&gt;
&lt;p&gt;Ocman records only successful &lt;code&gt;git commit&lt;/code&gt; summary lines printed by completed
or errored bash calls. It does not scan saved transcripts, poll &lt;code&gt;HEAD&lt;/code&gt;, install
hooks, resolve full SHAs, or reconcile history after reconnects. Commits made
while ocman is stopped, disconnected, or unable to keep up with the live event
stream can therefore be absent.&lt;/p&gt;
&lt;p&gt;The displayed SHA is exactly the abbreviated SHA printed by Git. The branch is
the name printed at commit time, or &lt;strong&gt;Detached HEAD&lt;/strong&gt; when Git printed a
detached-HEAD summary. Later branch switches, renames, deletion, amendments,
and worktree removal do not rewrite earlier observations. Amendments appear as
additional observations when Git prints a new commit summary.&lt;/p&gt;
&lt;p&gt;Each ocman instance stores observations for its local sessions in its own
&lt;code&gt;state.db&lt;/code&gt;. A hub reads remote observations from the session owner through the
existing Session Info RPC. It never substitutes rows from the hub database, so
the same session and tool IDs on different machines remain isolated.&lt;/p&gt;
&lt;p&gt;The owner&amp;rsquo;s watcher keeps capturing while a hub is disconnected. Session Info
is unavailable through that hub until the owner reconnects, then the owner
returns its persisted observations without scanning transcripts. After
persistence, the owner sends a change notice through the active session event
stream so the browser refreshes Session Info. Capture does not depend on that
stream or an open browser. Older owners that do not report commit-capture
support leave the Commits section hidden.&lt;/p&gt;
&lt;p&gt;Select a commit in Session Info to jump to the bash call that produced it.
Ocman opens collapsed output and highlights the exact call, including when one
assistant message contains several tool calls. If the message is older than the
loaded transcript window, ocman fetches that session&amp;rsquo;s history from its owner
only after selection. History reads do not create commit observations.&lt;/p&gt;
&lt;p&gt;The recorded commit remains in Session Info if its source message or tool call
was deleted. In that case ocman reports that the source is no longer available
instead of jumping to another call.&lt;/p&gt;</description></item><item><title>Factory delivery</title><link>https://ocman.stenic.io/docs/features/factory/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://ocman.stenic.io/docs/features/factory/</guid><description>
&lt;p&gt;Factory implements a Work Epic sequentially across one or more local Git
repositories. It keeps an independent shared branch and workspace for each
project, then runs a separate Project Delivery session for every changed
project. This model is defined by &lt;a href="https://ocman.stenic.io/docs/adr/0008-coordinate-factory-work-across-projects/"&gt;ADR 0008&lt;/a&gt;
.&lt;/p&gt;
&lt;h2&gt;Projects and Issue targets&lt;span class="hx:absolute hx:-mt-20" id="projects-and-issue-targets"&gt;&lt;/span&gt;
&lt;a href="#projects-and-issue-targets" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Every Work Epic has an &lt;strong&gt;Epic project set&lt;/strong&gt;. Its original Epic project is the
permanent default; when creating the Epic, you can select additional local Git
repositories and acknowledge local command execution for each one. Attached
remote-host projects are not supported. A secondary project can be removed only
before work or Delivery history exists for it, and the Epic project cannot be
removed.&lt;/p&gt;
&lt;p&gt;Planning and unblock sessions start in the Epic project and may read every
admitted project, but no other external project. Each executable Issue has one
project target, shown in Issue and queue views. A plan or graph mutation may set
the target explicitly; an omitted target inherits the Epic project, and a target
outside the admitted set is rejected. Dispatch, capacity accounting, retries,
and recovery use the Issue target. Factory still runs only one implementation
Attempt per Epic at a time.&lt;/p&gt;
&lt;p&gt;Each project has its own Factory workspace lineage: branch, base, checkpoint,
Delivery remote, pull request, recovery, and resume history do not cross project
boundaries. An implementation session writes in its target workspace. It gets
read access to other admitted projects and explicit edit-deny rules for those
paths. Shell commands remain available, however, so this is agent policy rather
than filesystem or container isolation.&lt;/p&gt;
&lt;h2&gt;Use an existing plan&lt;span class="hx:absolute hx:-mt-20" id="use-an-existing-plan"&gt;&lt;/span&gt;
&lt;a href="#use-an-existing-plan" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;If a regular session has already planned the work, ask it to import that plan
into Factory. It creates an Epic, reads &lt;code&gt;issues&lt;/code&gt; to find the root &lt;code&gt;mol&lt;/code&gt; ID,
then calls &lt;code&gt;import_proposal&lt;/code&gt; with the ticket breakdown and dependency edges.
No dedicated planning session starts.&lt;/p&gt;
&lt;p&gt;The imported proposal still requires &lt;strong&gt;human approval&lt;/strong&gt;. Review its tickets,
choose an implementation model, then approve through the action card or Epic
page. Importing alone does not materialize tickets or start implementation.&lt;/p&gt;
&lt;p&gt;Include the scope, acceptance criteria, verification steps, and relevant decisions
in the ticket descriptions and proposal rationale. Implementation sessions do
not inherit the original conversation.&lt;/p&gt;
&lt;p&gt;Import is available before any Factory attempt has been claimed. It marks
the planning work complete, so a competing planner cannot start while the proposal is
awaiting review. You can request a revision and have the original session call
&lt;code&gt;import_proposal&lt;/code&gt; again. Each import creates an immutable revision; approval
must match its exact revision and hash. Approved or rejected plans cannot be
replaced through import. Proposal history and the pending gate survive restart.&lt;/p&gt;
&lt;h2&gt;Formula workflows&lt;span class="hx:absolute hx:-mt-20" id="formula-workflows"&gt;&lt;/span&gt;
&lt;a href="#formula-workflows" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;A Formula is a YAML workflow. Its named &lt;code&gt;steps&lt;/code&gt; contain their own &lt;code&gt;kind&lt;/code&gt;,
&lt;code&gt;needs&lt;/code&gt;, &lt;code&gt;prompt&lt;/code&gt;, and &lt;code&gt;config&lt;/code&gt;. Dependencies point to prerequisite step names,
following the convention used by GitHub Actions jobs. This is ocman&amp;rsquo;s schema,
not a GitHub Actions workflow file.&lt;/p&gt;
&lt;div class="hextra-code-block hx:relative hx:mt-6 hx:first:mt-0 hx:group/code"&gt;
&lt;div&gt;&lt;div class="highlight"&gt;&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-yaml" data-lang="yaml"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;version&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="m"&gt;2&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;name&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;Tracer&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nt"&gt;steps&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;plan&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;kind&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;planning&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;prompt&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="p"&gt;|&lt;/span&gt;&lt;span class="sd"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="sd"&gt; Inspect the repository, clarify requirements, and propose focused tasks.&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;approve&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;kind&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;approval&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;needs&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="p"&gt;[&lt;/span&gt;&lt;span class="l"&gt;plan]&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;implement&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;kind&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;implementation&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;needs&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="p"&gt;[&lt;/span&gt;&lt;span class="l"&gt;approve]&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;config&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;concurrency&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="m"&gt;1&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;prompt&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="p"&gt;|&lt;/span&gt;&lt;span class="sd"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="sd"&gt; Implement the assigned task and run its checks.&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;verify&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;kind&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;verification&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;needs&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="p"&gt;[&lt;/span&gt;&lt;span class="l"&gt;implement]&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;prompt&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="p"&gt;|&lt;/span&gt;&lt;span class="sd"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="sd"&gt; Review the combined changes and run the repository-required checks.
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="sd"&gt; Request recovery if a required check fails.&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;deliver&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;kind&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="l"&gt;delivery&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;needs&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="p"&gt;[&lt;/span&gt;&lt;span class="l"&gt;verify]&lt;/span&gt;&lt;span class="w"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nt"&gt;prompt&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="p"&gt;|&lt;/span&gt;&lt;span class="sd"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="sd"&gt; Summarize the changes and verification results in the final pull request.&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class="hextra-code-copy-btn-container hx:opacity-0 hx:transition hx:group-hover/code:opacity-100 hx:flex hx:gap-1 hx:absolute hx:m-[11px] hx:right-0 hx:top-0"&gt;
&lt;button
class="hextra-code-copy-btn hx:group/copybtn hx:cursor-pointer hx:transition-all hx:active:opacity-50 hx:bg-primary-700/5 hx:border hx:border-black/5 hx:text-gray-600 hx:hover:text-gray-900 hx:rounded-md hx:p-1.5 hx:dark:bg-primary-300/10 hx:dark:border-white/10 hx:dark:text-gray-400 hx:dark:hover:text-gray-50"
title="Copy code"
aria-label="Copy code"
data-copied-label="Copied!"
&gt;
&lt;div class="hextra-copy-icon hx:group-[.copied]/copybtn:hidden hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;div class="hextra-success-icon hx:hidden hx:group-[.copied]/copybtn:block hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;/button&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;p&gt;Use &lt;strong&gt;Customize Tracer&lt;/strong&gt; in Factory configuration to start from the current
built-in workflow, &lt;code&gt;ocman/tracer@3&lt;/code&gt;. Expand &lt;strong&gt;Formula source&lt;/strong&gt; to edit the YAML
in a full-width, 15-line editor that can be resized vertically. Validate and
preview it before saving an immutable revision. The graph includes implementation
and every post-implementation check. Expand the implementation phase in an Epic&amp;rsquo;s
graph to see its planned tasks.&lt;/p&gt;
&lt;p&gt;The first version supports one planning step, one implementation group, and one
final delivery step. An initial plan approval must precede implementation. Add
further human approvals before implementation or after checks, and any number
of verification steps, by naming them and
declaring &lt;code&gt;needs&lt;/code&gt;. Every step must lead to final delivery; cycles, unknown keys,
missing dependencies, and disconnected steps are rejected.&lt;/p&gt;
&lt;p&gt;The implementation group expands into the approved tasks. It succeeds only when
all applicable required tasks succeed. Runnable optional tasks also finish before
verification; deferred optional tasks do not hold the group open. Verification and delivery run once per
changed project in that project&amp;rsquo;s assigned worktree. A dependent step waits for
all project instances of its prerequisites. Failed or paused checks block
delivery; recovery or retry is explicit. Additional approval steps appear in
the action inbox and require a user decision. A user can reconsider a rejected
step with &lt;strong&gt;Approve step&lt;/strong&gt;, which rechecks its prerequisites.&lt;/p&gt;
&lt;p&gt;Agent steps require a multiline prompt of at most 32 KiB. Optional &lt;code&gt;name&lt;/code&gt; gives
a step a display label. &lt;code&gt;config.model&lt;/code&gt; accepts a &lt;code&gt;provider/model&lt;/code&gt; reference and
overrides the approved implementation model for that step. The implementation
group supports &lt;code&gt;config.concurrency: 1&lt;/code&gt;, reflecting the shared workspace&amp;rsquo;s
sequential execution. Planning can supply &lt;code&gt;config.scope_expansion_prompt&lt;/code&gt; for
additive replanning. Other configuration keys are rejected.&lt;/p&gt;
&lt;p&gt;Factory adds runtime context, repository restrictions, attempt credentials, and
completion instructions to each prompt. Changing a prompt does not bypass
permissions, approvals, or commit/PR validation. Planned tasks cannot inject
delivery nodes into a YAML workflow; delivery is declared in the Formula.
Within an implementation group, use ordinary task dependencies. The legacy
cross-project merge-gate plan format remains available only to older revisions.&lt;/p&gt;
&lt;p&gt;The built-in workflow has been converted to YAML. Historical TOML revisions stay
readable for pinned Epics; their source and hashes are not rewritten. New Epics
use the selected YAML revision. Runtime step definitions and their project
instances persist across restarts. Source edits, including comments and formatting,
create a new revision rather than silently restoring an earlier source file.&lt;/p&gt;
&lt;h2&gt;Planning and implementation models&lt;span class="hx:absolute hx:-mt-20" id="planning-and-implementation-models"&gt;&lt;/span&gt;
&lt;a href="#planning-and-implementation-models" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;New planning sessions prefer an available Fable or Astra model. If neither is
available, they use the runtime default.&lt;/p&gt;
&lt;p&gt;Plan approval includes an implementation model selector in the planning session,
Epic page, and inline action card. It suggests an available Opus or Sol model for
balanced implementation, or Sonnet or Terra when only a fast model is available.
Choose a fast model for speed, any other available model, or Runtime default.&lt;/p&gt;
&lt;p&gt;Approval saves the exact model reference with the Plan gate and copies it into
each implementation Attempt, including final delivery. Retrying approval keeps
the original choice. MCP callers can supply &lt;code&gt;implementation_model&lt;/code&gt; as a
&lt;code&gt;provider/model&lt;/code&gt; reference to &lt;code&gt;approve_plan&lt;/code&gt; after confirming it with the user.&lt;/p&gt;
&lt;p&gt;The Epic page&amp;rsquo;s &lt;strong&gt;Models&lt;/strong&gt; controls pick a planning, implementation, and
verification model for that Epic at any time (&lt;code&gt;POST /api/factory/epics/&amp;lt;id&amp;gt;/models&lt;/code&gt;). A choice there wins over the workflow step&amp;rsquo;s
&lt;code&gt;config.model&lt;/code&gt; and the approval-time choice; Default falls back to them.
Delivery uses the implementation model. Each Attempt freezes its model when it
is claimed, so a change mid-implementation affects only work that has not
started.&lt;/p&gt;
&lt;h2&gt;Implementation checkpoints&lt;span class="hx:absolute hx:-mt-20" id="implementation-checkpoints"&gt;&lt;/span&gt;
&lt;a href="#implementation-checkpoints" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Each implementation session tests its change, commits it, pushes the shared
branch, and calls &lt;code&gt;factory&lt;/code&gt; with &lt;code&gt;action: &amp;quot;complete_attempt&amp;quot;&lt;/code&gt;, &lt;code&gt;attempt_id&lt;/code&gt;,
&lt;code&gt;attempt_token&lt;/code&gt;, and &lt;code&gt;summary&lt;/code&gt;. It omits &lt;code&gt;pr_url&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;Ocman checks that the worktree is clean, that its HEAD matches its upstream, and
that it includes the previously accepted commit. The successful Attempt stores
the branch, target branch, and commit SHA. Before launching the next Issue,
Ocman checks that the branch still matches that checkpoint. Missing branches,
dirty worktrees, or unexpected commits require reconciliation rather than a
silent reset. Checkpoints survive an ocman restart.&lt;/p&gt;
&lt;p&gt;Implementation sessions run in the Issue&amp;rsquo;s target project workspace. They may
read other projects admitted to the Epic, but path-specific permission rules
deny edits there. Shell access remains enabled, so these rules enforce agent
policy; they are not filesystem isolation.&lt;/p&gt;
&lt;h2&gt;Project Deliveries&lt;span class="hx:absolute hx:-mt-20" id="project-deliveries"&gt;&lt;/span&gt;
&lt;a href="#project-deliveries" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Factory runs a required Project Delivery for each project with executable work.
YAML workflows wait for the declared checks and approvals across all changed
projects. Older revisions allow Delivery as soon as the project&amp;rsquo;s own required
work succeeds. Starting it seals only that project. The delivery model reviews that
project&amp;rsquo;s combined changes, runs the required checks, and creates a review-ready
PR against its recorded remote and target branch. It searches for an existing
open PR from the same branch into the recorded target first, so retries can reuse
a PR created before the session was interrupted.&lt;/p&gt;
&lt;p&gt;Delivery admission rechecks the current graph in the claim transaction, so new
required work cannot be skipped between a dependency refresh and launch. Ready
optional tasks run before delivery too. Deferred or blocked optional work does
not delay delivery; after delivery succeeds, it remains visible as not applicable
and cannot start on the delivered branch.&lt;/p&gt;
&lt;p&gt;The Delivery session completes with &lt;code&gt;pr_url&lt;/code&gt;. Ocman validates the repository,
source branch, target branch, and pushed HEAD. It rejects draft, closed, merged,
and cross-fork PRs. Project status and Delivery lineage are shown on the Epic.
The Epic shows &lt;strong&gt;Ready for review&lt;/strong&gt; only after every required Project Delivery
succeeds; Factory does not merge the PRs.&lt;/p&gt;
&lt;p&gt;New work discovered before Factory observes a Project Delivery PR merge refreshes
that Delivery on the same branch and PR. After a merge gate records the merge,
new work creates a successor Delivery from the updated target branch and
publishes a new PR. Existing merge gates stay pinned to their original Delivery;
newly created gates use the latest Delivery lineage.&lt;/p&gt;
&lt;h2&gt;Cross-project dependencies&lt;span class="hx:absolute hx:-mt-20" id="cross-project-dependencies"&gt;&lt;/span&gt;
&lt;a href="#cross-project-dependencies" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;An ordinary dependency can cross projects and is satisfied by the blocker&amp;rsquo;s
validated implementation checkpoint. Use a &lt;strong&gt;merge-gated dependency&lt;/strong&gt; when a
downstream Issue must wait for an upstream Project Delivery PR to merge. Factory
polls the forge and records the observation; only a merged PR at the recorded
Delivery commit satisfies the gate. Open, draft, closed-unmerged, changed, and
temporarily unavailable PR observations keep the dependent Issue blocked and
show the reason rather than failing it automatically.&lt;/p&gt;
&lt;h2&gt;Scope expansion&lt;span class="hx:absolute hx:-mt-20" id="scope-expansion"&gt;&lt;/span&gt;
&lt;a href="#scope-expansion" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;An implementation agent that discovers another required repository can request
it with the project path and a reason. Factory pauses and preserves that Attempt
until a user decides. Approval verifies the local Git repository, requires a new
local-execution acknowledgement, admits it to the project set, and launches an
additive replan of the remaining work. Completed Issues and checkpoints remain
intact, and the paused Issue retries only after any new blockers succeed.
Rejection records the response and resumes the same implementation session.&lt;/p&gt;
&lt;p&gt;If a PR is merged before Factory records Delivery, the agent must request
recovery. Factory accepts that merged PR only after a human resumes the same
Attempt with &lt;code&gt;Force-complete the delivery attempt using merged PR #&amp;lt;number&amp;gt;&lt;/code&gt;.
The PR number and target branch must match, and the assigned worktree must
still be clean at its pushed checkpoint.&lt;/p&gt;
&lt;p&gt;Delivery uses the same recovery gates, launch retries, and Reopen control as
implementation. A Delivery failure does not reopen completed implementation
Issues. Structural graph edits are paused while a Delivery is running. Once all
required project Deliveries succeed, additional work belongs in a new Epic.&lt;/p&gt;
&lt;h2&gt;Existing work&lt;span class="hx:absolute hx:-mt-20" id="existing-work"&gt;&lt;/span&gt;
&lt;a href="#existing-work" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Already-running implementation Attempts can complete with a clean, pushed commit
and no &lt;code&gt;pr_url&lt;/code&gt;, even if their original prompt required a PR. Factory recovers the
session&amp;rsquo;s actual worktree branch, including numbered successor branches, and
records its checkpoint for subsequent Issues. This completion does not consult
or change an existing PR. Only Project Delivery requires a review-ready PR.&lt;/p&gt;
&lt;p&gt;Previously completed PR-based results remain valid history. When launching new
work from that history, Factory resolves the prior PR once to preserve the shared
branch and completed work. On Forgejo, converting an existing PR to draft uses
the &lt;code&gt;WIP: &lt;/code&gt; title prefix.&lt;/p&gt;</description></item><item><title>Releases</title><link>https://ocman.stenic.io/docs/other/releases/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://ocman.stenic.io/docs/other/releases/</guid><description>
&lt;p&gt;Release tags are created automatically from Conventional Commit messages on &lt;code&gt;main&lt;/code&gt;.&lt;/p&gt;
&lt;h2&gt;Files&lt;span class="hx:absolute hx:-mt-20" id="files"&gt;&lt;/span&gt;
&lt;a href="#files" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;ul&gt;
&lt;li&gt;&lt;code&gt;.forgejo/workflows/ci.yml&lt;/code&gt; inspects commits on &lt;code&gt;main&lt;/code&gt;, computes the next semver version, and creates a &lt;code&gt;v*&lt;/code&gt; tag through the Forgejo API after CI passes.&lt;/li&gt;
&lt;li&gt;&lt;code&gt;.forgejo/workflows/release.yml&lt;/code&gt; runs when a &lt;code&gt;v*&lt;/code&gt; tag is pushed, builds platform archives, creates the Forgejo release entry, and uploads the assets.&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;One-time setup&lt;span class="hx:absolute hx:-mt-20" id="one-time-setup"&gt;&lt;/span&gt;
&lt;a href="#one-time-setup" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;ol&gt;
&lt;li&gt;Create a Forgejo access token for a user or bot that can create tags in this repository.&lt;/li&gt;
&lt;li&gt;Add that token to repository secrets as &lt;code&gt;RELEASE_TOKEN&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;Make sure the token can trigger follow-up workflows when it creates a tag.&lt;/li&gt;
&lt;li&gt;Push the workflow files to &lt;code&gt;main&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;Trigger the &lt;code&gt;Semantic Tag&lt;/code&gt; workflow with &lt;code&gt;workflow_dispatch&lt;/code&gt; if you want to verify the setup immediately.&lt;/li&gt;
&lt;/ol&gt;
&lt;h2&gt;Commit format&lt;span class="hx:absolute hx:-mt-20" id="commit-format"&gt;&lt;/span&gt;
&lt;a href="#commit-format" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Use Conventional Commits for changes that should drive versioning.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;fix: correct tmux session lookup&lt;/code&gt; -&amp;gt; patch release&lt;/li&gt;
&lt;li&gt;&lt;code&gt;feat: add project detail page&lt;/code&gt; -&amp;gt; minor release&lt;/li&gt;
&lt;li&gt;&lt;code&gt;feat!: change session API shape&lt;/code&gt; -&amp;gt; major release&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Breaking changes can also be written with a footer:&lt;/p&gt;
&lt;div class="hextra-code-block hx:relative hx:mt-6 hx:first:mt-0 hx:group/code"&gt;
&lt;div&gt;&lt;div class="highlight"&gt;&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;feat: change session payload
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;BREAKING CHANGE: session responses now omit archived messages by default&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class="hextra-code-copy-btn-container hx:opacity-0 hx:transition hx:group-hover/code:opacity-100 hx:flex hx:gap-1 hx:absolute hx:m-[11px] hx:right-0 hx:top-0"&gt;
&lt;button
class="hextra-code-copy-btn hx:group/copybtn hx:cursor-pointer hx:transition-all hx:active:opacity-50 hx:bg-primary-700/5 hx:border hx:border-black/5 hx:text-gray-600 hx:hover:text-gray-900 hx:rounded-md hx:p-1.5 hx:dark:bg-primary-300/10 hx:dark:border-white/10 hx:dark:text-gray-400 hx:dark:hover:text-gray-50"
title="Copy code"
aria-label="Copy code"
data-copied-label="Copied!"
&gt;
&lt;div class="hextra-copy-icon hx:group-[.copied]/copybtn:hidden hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;div class="hextra-success-icon hx:hidden hx:group-[.copied]/copybtn:block hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;/button&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;p&gt;The semantic tag workflow currently treats these commit types as releasable:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;feat&lt;/code&gt; -&amp;gt; minor&lt;/li&gt;
&lt;li&gt;&lt;code&gt;fix&lt;/code&gt;, &lt;code&gt;perf&lt;/code&gt;, &lt;code&gt;refactor&lt;/code&gt; -&amp;gt; patch&lt;/li&gt;
&lt;li&gt;breaking change marker or &lt;code&gt;!&lt;/code&gt; -&amp;gt; major&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;If no releasable Conventional Commits are present since the latest &lt;code&gt;v*&lt;/code&gt; tag, no new tag is created.&lt;/p&gt;
&lt;h2&gt;Normal release flow&lt;span class="hx:absolute hx:-mt-20" id="normal-release-flow"&gt;&lt;/span&gt;
&lt;a href="#normal-release-flow" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;ol&gt;
&lt;li&gt;Merge or push Conventional Commit changes onto &lt;code&gt;main&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;The &lt;code&gt;Semantic Tag&lt;/code&gt; workflow runs.&lt;/li&gt;
&lt;li&gt;It finds the latest &lt;code&gt;v*&lt;/code&gt; tag and evaluates commits since that tag.&lt;/li&gt;
&lt;li&gt;It calculates the next semantic version.&lt;/li&gt;
&lt;li&gt;It creates the new &lt;code&gt;v*&lt;/code&gt; tag through the Forgejo API.&lt;/li&gt;
&lt;li&gt;The existing &lt;code&gt;Release&lt;/code&gt; workflow sees the new tag, builds the platform archives, creates the release entry, and uploads the assets.&lt;/li&gt;
&lt;/ol&gt;
&lt;h2&gt;Direct pushes to main&lt;span class="hx:absolute hx:-mt-20" id="direct-pushes-to-main"&gt;&lt;/span&gt;
&lt;a href="#direct-pushes-to-main" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Direct pushes to &lt;code&gt;main&lt;/code&gt; are the normal case for this setup.&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;You push commits directly to &lt;code&gt;main&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;The &lt;code&gt;Semantic Tag&lt;/code&gt; workflow runs on that push.&lt;/li&gt;
&lt;li&gt;If the new commits include releasable Conventional Commits, it creates the next tag immediately.&lt;/li&gt;
&lt;li&gt;That tag triggers the packaging and release workflow.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;There is no release PR in this flow.&lt;/p&gt;
&lt;h2&gt;First release&lt;span class="hx:absolute hx:-mt-20" id="first-release"&gt;&lt;/span&gt;
&lt;a href="#first-release" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;If the repository has no &lt;code&gt;v*&lt;/code&gt; tags yet, the semantic tag workflow starts from &lt;code&gt;0.0.0&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;Examples:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;first releasable &lt;code&gt;fix:&lt;/code&gt; commit -&amp;gt; &lt;code&gt;v0.0.1&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;first releasable &lt;code&gt;feat:&lt;/code&gt; commit -&amp;gt; &lt;code&gt;v0.1.0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;first breaking change -&amp;gt; &lt;code&gt;v1.0.0&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Why a dedicated token is required&lt;span class="hx:absolute hx:-mt-20" id="why-a-dedicated-token-is-required"&gt;&lt;/span&gt;
&lt;a href="#why-a-dedicated-token-is-required" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;The semantic tag workflow uses &lt;code&gt;RELEASE_TOKEN&lt;/code&gt; instead of the default automation token so the tag it creates can trigger the separate tag-based release workflow.&lt;/p&gt;
&lt;p&gt;If your Forgejo instance suppresses follow-up workflow triggers from automation-created refs, the default automation token will create the tag without starting &lt;code&gt;.forgejo/workflows/release.yml&lt;/code&gt;.&lt;/p&gt;
&lt;h2&gt;Troubleshooting&lt;span class="hx:absolute hx:-mt-20" id="troubleshooting"&gt;&lt;/span&gt;
&lt;a href="#troubleshooting" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;If no tag is created:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Confirm the workflow ran on &lt;code&gt;main&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;Confirm &lt;code&gt;RELEASE_TOKEN&lt;/code&gt; exists and has repository write access.&lt;/li&gt;
&lt;li&gt;Confirm at least one commit since the latest &lt;code&gt;v*&lt;/code&gt; tag follows the releasable Conventional Commit patterns.&lt;/li&gt;
&lt;li&gt;Check the workflow log for &lt;code&gt;No releasable conventional commits found&lt;/code&gt;.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;If the tag is created but no release assets appear:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Confirm the new &lt;code&gt;v*&lt;/code&gt; tag exists in Forgejo.&lt;/li&gt;
&lt;li&gt;Confirm &lt;code&gt;.forgejo/workflows/release.yml&lt;/code&gt; ran for that tag.&lt;/li&gt;
&lt;li&gt;Confirm the token used to create the tag is allowed to trigger follow-up workflows on your Forgejo instance.&lt;/li&gt;
&lt;/ol&gt;</description></item><item><title>Session and event data flow</title><link>https://ocman.stenic.io/docs/other/architecture-events/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://ocman.stenic.io/docs/other/architecture-events/</guid><description>
&lt;p&gt;Part of the &lt;a href="../architecture/"&gt;architecture overview&lt;/a&gt;
.&lt;/p&gt;
&lt;div role="img" aria-label="Diagram"&gt;
&lt;pre class="mermaid hx:mt-6"&gt;
sequenceDiagram
participant B as Browser
participant S as server handlers
participant Q as routines.Service
participant R as Registry/Router
participant O as Remote owner
participant A as opencode adapter
participant D as opencode.db / OC HTTP
participant E as SSE broadcast
participant I as state.db
B-&amp;gt;&amp;gt;S: GET /api/sessions
S-&amp;gt;&amp;gt;R: resolve platform/host
R-&amp;gt;&amp;gt;A: ListSessions()
D--&amp;gt;&amp;gt;A: background /global/event prompts + terminal parts
A-&amp;gt;&amp;gt;D: SQL json_extract
A-&amp;gt;&amp;gt;A: overlay pending prompt registry
D--&amp;gt;&amp;gt;B: JSON (status settled at query time)
D--&amp;gt;&amp;gt;E: watcher observes message/part mutation
E--&amp;gt;&amp;gt;B: /api/events: ocman.session.activity (id, timestamp)
B-&amp;gt;&amp;gt;B: patch activity; stable minute-bucket sort; animate moved rows
B-&amp;gt;&amp;gt;S: GET /api/projects
I--&amp;gt;&amp;gt;S: cached local projects snapshot
S--&amp;gt;&amp;gt;B: cached projects JSON
S-&amp;gt;&amp;gt;D: background project aggregate refresh
S-&amp;gt;&amp;gt;I: persist refreshed snapshot
E--&amp;gt;&amp;gt;B: SSE (ocman.projects.changed)
R-&amp;gt;&amp;gt;O: gRPC Session / SessionInfo / StreamEvents (remote only)
O-&amp;gt;&amp;gt;A: Session / SessionInfo / ProxyEvents
O-&amp;gt;&amp;gt;I: read owner-local approvals and commit observations
O-&amp;gt;&amp;gt;O: inject persisted approvals,&amp;lt;br/&amp;gt;tee synthetic approval events
O--&amp;gt;&amp;gt;S: owner-enriched JSON / framed SSE
Note over S,Q: every 5 s: claim due routines
Q-&amp;gt;&amp;gt;R: ensure project instance and create fresh session
R-&amp;gt;&amp;gt;A: create session and send saved prompt
Note over Q,A: poll linked session until it settles
E--&amp;gt;&amp;gt;B: SSE (session.updated)
A-&amp;gt;&amp;gt;I: persist owner-local commit observations / MCP inbox send
A-&amp;gt;&amp;gt;I: create permission Inbox item; archive on resolution
Q-&amp;gt;&amp;gt;I: commit run outcome and Routine Inbox notification
I--&amp;gt;&amp;gt;S: local state or owner-routed remote RPC
B-&amp;gt;&amp;gt;S: REST Inbox list/read/archive and session permission reply
S--&amp;gt;&amp;gt;B: categorized Inbox JSON with permission actions
E--&amp;gt;&amp;gt;B: SSE (ocman.inbox.changed); polling fallback
&lt;/pre&gt;
&lt;/div&gt;&lt;ul&gt;
&lt;li&gt;The local watcher broadcasts activity for identified message/part mutations
on the shared &lt;code&gt;/api/events&lt;/code&gt; stream. The sidebar updates known rows directly,
fetching a single session only when it is missing. This avoids list refetches
on each token. The broadcast hub keeps the latest activity per session when
a subscriber falls behind.&lt;/li&gt;
&lt;li&gt;Activity timestamps stay exact, but sorting uses one-minute buckets with
stable ties. Concurrent streams in the same minute do not continually swap
places. Stale list responses cannot roll activity timestamps backwards.&lt;/li&gt;
&lt;li&gt;Sending a message updates sidebar activity optimistically. Queued messages
wait until sent. Failed sends restore the old timestamp unless newer SSE
activity has arrived. Reorders use a 180 ms native animation and respect
reduced-motion preferences. Pinned ordering and manual project ordering remain.&lt;/li&gt;
&lt;li&gt;Reconnecting and the slow reconciliation poll recover missed events.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Ocman never persists session status. The live turn signal from the running
OpenCode instance decides whether a session is busy; the last stored message
row only settles which terminal state a finished session is in. Nothing is
written back to OpenCode&amp;rsquo;s database.&lt;/p&gt;
&lt;p&gt;The local projects index uses stale-while-refresh persistence. On startup the
server hydrates the last snapshot from &lt;code&gt;state.db&lt;/code&gt;; a request receives that
snapshot immediately while the expensive &lt;code&gt;opencode.db&lt;/code&gt; aggregate refreshes in
the background. A changed result is persisted and announced over global SSE so
TanStack Query refetches the authoritative list. Remote inventories and archive
overlays remain live and are not stored in this cache.&lt;/p&gt;
&lt;p&gt;Ocman owns routine state. The Routines page creates, edits, soft-deletes, and
starts routines over REST. Manual and scheduled paths first claim an immutable
run snapshot in &lt;code&gt;state.db&lt;/code&gt;, then create and prompt a fresh session through the
shared session service. The run stays active until that session settles. The
same row stores success or failure, any error, and the session link shown in
history.&lt;/p&gt;
&lt;p&gt;Remote commit observations stay on the owner. The hub neither writes raw remote
tool output to its state database nor appends hub-local observations to a remote
Session Info response. While connected, the existing proxied session event
stream triggers the browser&amp;rsquo;s debounced Session Info refresh. While
disconnected, the owner can continue capturing independently; the hub follows
its normal unavailable behavior and reads the persisted owner records after
reconnect. Neither side reconstructs missed observations from transcripts.&lt;/p&gt;</description></item><item><title>Plugins</title><link>https://ocman.stenic.io/docs/features/plugins/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://ocman.stenic.io/docs/features/plugins/</guid><description>
&lt;p&gt;Ocman runs native plugin executables on the machine where they are installed.
Two capabilities are supported: &lt;code&gt;action.v1&lt;/code&gt;, which adds commands to the command
palette and returns results rendered by ocman, and &lt;code&gt;conversation.v1&lt;/code&gt;, which lets
a chat provider start an ocman session and receive its completed reply. Manage
installations in &lt;strong&gt;Settings → Plugins&lt;/strong&gt;, selecting the plugin owner before making
changes.&lt;/p&gt;
&lt;h2&gt;Trust and installation&lt;span class="hx:absolute hx:-mt-20" id="trust-and-installation"&gt;&lt;/span&gt;
&lt;a href="#trust-and-installation" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;&lt;strong&gt;Plugins are trusted native code, including during discovery.&lt;/strong&gt; Rescanning runs
each candidate&amp;rsquo;s &lt;code&gt;describe&lt;/code&gt; command before you enable it. Only install code you
trust to run with the ocman user&amp;rsquo;s operating-system access. Grants limit the
context ocman sends through its action broker; they do not restrict a plugin&amp;rsquo;s
filesystem or network access. Checksums detect changes, not publisher identity.
There is no sandbox or signature verification.&lt;/p&gt;
&lt;p&gt;Install a regular executable named &lt;code&gt;ocman-plugin-&amp;lt;name&amp;gt;&lt;/code&gt; directly under
&lt;code&gt;~/.local/share/ocman/plugins&lt;/code&gt;. Set &lt;code&gt;OCMAN_PLUGIN_DIR&lt;/code&gt; on the owning ocman process
to use another directory. Hidden files, symlinks, subdirectories, nested files,
and files without executable permission are ignored. The host does not download
or install binaries for you.&lt;/p&gt;
&lt;p&gt;Build the bundled plugins from an ocman checkout with one target, which creates
the directory, builds reproducibly and prints the checksum you are about to
approve:&lt;/p&gt;
&lt;div class="hextra-code-block hx:relative hx:mt-6 hx:first:mt-0 hx:group/code"&gt;
&lt;div&gt;&lt;div class="highlight"&gt;&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-sh" data-lang="sh"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;make install-plugin &lt;span class="nv"&gt;PLUGIN&lt;/span&gt;&lt;span class="o"&gt;=&lt;/span&gt;slack &lt;span class="c1"&gt;# or PLUGIN=fixture&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;make install-plugin &lt;span class="nv"&gt;PLUGIN&lt;/span&gt;&lt;span class="o"&gt;=&lt;/span&gt;slack &lt;span class="nv"&gt;PLUGIN_DIR&lt;/span&gt;&lt;span class="o"&gt;=&lt;/span&gt;/custom/plugins&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class="hextra-code-copy-btn-container hx:opacity-0 hx:transition hx:group-hover/code:opacity-100 hx:flex hx:gap-1 hx:absolute hx:m-[11px] hx:right-0 hx:top-0"&gt;
&lt;button
class="hextra-code-copy-btn hx:group/copybtn hx:cursor-pointer hx:transition-all hx:active:opacity-50 hx:bg-primary-700/5 hx:border hx:border-black/5 hx:text-gray-600 hx:hover:text-gray-900 hx:rounded-md hx:p-1.5 hx:dark:bg-primary-300/10 hx:dark:border-white/10 hx:dark:text-gray-400 hx:dark:hover:text-gray-50"
title="Copy code"
aria-label="Copy code"
data-copied-label="Copied!"
&gt;
&lt;div class="hextra-copy-icon hx:group-[.copied]/copybtn:hidden hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;div class="hextra-success-icon hx:hidden hx:group-[.copied]/copybtn:block hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;/button&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;p&gt;Set &lt;code&gt;PLUGIN_DIR&lt;/code&gt; to match &lt;code&gt;OCMAN_PLUGIN_DIR&lt;/code&gt; if the owning ocman process uses a
directory override.&lt;/p&gt;
&lt;p&gt;Ocman scans at startup. Click &lt;strong&gt;Rescan plugins&lt;/strong&gt; after installing, replacing, or
removing an executable; there is no filesystem watcher. New registrations start
disabled. Review the ID, version, SHA-256 checksum, capabilities, execution scope,
and requested grants. Configure required settings, then click &lt;strong&gt;Enable&lt;/strong&gt; and
&lt;strong&gt;Approve grants and enable&lt;/strong&gt;. Enablement requires approval of all requested
grants, including an explicit empty set when none are requested.
Approval is bound to the reviewed executable and declaration. If another rescan
changes them before approval, refresh the catalog and review the new registration.&lt;/p&gt;
&lt;p&gt;Changed code or declarations revoke approval. Duplicate plugin IDs conflict and
disable all matching candidates. An executable path cannot change its registered
plugin ID. Fix the installation and rescan before approving it again.&lt;/p&gt;
&lt;h2&gt;Configuration and secrets&lt;span class="hx:absolute hx:-mt-20" id="configuration-and-secrets"&gt;&lt;/span&gt;
&lt;a href="#configuration-and-secrets" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;&lt;strong&gt;Configure&lt;/strong&gt; renders declared string, boolean, number, integer, and string-enum
settings. The host rejects unknown fields, wrong types, and missing required
values. Public values are replaced when saved and declared defaults are applied.
Secret inputs are write-only strings: omitting a secret preserves it, while an
empty string clears an optional secret. Reads show only whether it is configured.&lt;/p&gt;
&lt;p&gt;Configuration and health belong to the installation&amp;rsquo;s owner. Public settings live
in its &lt;code&gt;state.db&lt;/code&gt;. Beside that database, ocman stores private plugin files under
&lt;code&gt;plugin-data/&amp;lt;sha256(id)&amp;gt;/&lt;/code&gt; and secret snapshots under
&lt;code&gt;plugin-secrets/&amp;lt;sha256(id)&amp;gt;/&lt;/code&gt;. Directories use mode &lt;code&gt;0700&lt;/code&gt;; secret files use &lt;code&gt;0600&lt;/code&gt;.
These are permission-protected files, not an encrypted secret vault. Historical
snapshots remain for rollback and diagnostic redaction.&lt;/p&gt;
&lt;p&gt;Serve processes receive effective configuration, including secrets, as one JSON
object followed by a newline on inherited file descriptor 3. Read and close it
before emitting the serve hello. Configuration is absent from arguments and
environment variables; describe has no configuration descriptor. No repository
path, inherited ocman credentials, or secret-store path is supplied.&lt;/p&gt;
&lt;p&gt;Saving configuration for a ready, enabled plugin restarts it and waits for its
handshake. Failure restores the last working public and secret snapshots and
restarts the previous configuration, even if the HTTP request was cancelled.
If ocman exits before activation finishes, startup restores the last working
checkpoint before launching enabled plugins. Saved edits to disabled plugins remain.
Check &lt;strong&gt;Refresh health&lt;/strong&gt; after a failed save. An enabled unhealthy plugin must be
disabled before repairing its configuration, then enabled again. Disabled plugins
can be configured without starting a serve process.&lt;/p&gt;
&lt;h2&gt;Process contract and lifecycle&lt;span class="hx:absolute hx:-mt-20" id="process-contract-and-lifecycle"&gt;&lt;/span&gt;
&lt;a href="#process-contract-and-lifecycle" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;An executable accepts one argument, &lt;code&gt;describe&lt;/code&gt; or &lt;code&gt;serve&lt;/code&gt;. Stdout is strictly
UTF-8 newline-delimited JSON, one object per line; diagnostics go to stderr.
Each launch receives a fresh &lt;code&gt;OCMAN_PLUGIN_TOKEN&lt;/code&gt;, which the first &lt;code&gt;hello&lt;/code&gt; must
echo. Never log or persist it. The remaining environment is
&lt;code&gt;PATH=/usr/bin:/bin&lt;/code&gt; and &lt;code&gt;LANG=C.UTF-8&lt;/code&gt;; package runtime dependencies accordingly.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;describe&lt;/code&gt; runs in &lt;code&gt;/&lt;/code&gt; with stdin at EOF, emits one token-bound hello containing
the description, and exits successfully within three seconds.&lt;/li&gt;
&lt;li&gt;&lt;code&gt;serve&lt;/code&gt; runs in the plugin&amp;rsquo;s private data directory. It reads configuration,
emits the same approved description in a token-bound hello, then waits for the
host&amp;rsquo;s hello acknowledgment before handling calls. Readiness has a three-second
deadline.&lt;/li&gt;
&lt;li&gt;The description includes a reverse-domain ID, release version, process version,
capability versions, execution scope, concurrency, grants, settings, and actions.
Process major versions must match; the lower minor is selected. Capabilities
negotiate independently. Unknown or incompatible capabilities are omitted.&lt;/li&gt;
&lt;li&gt;General calls have an ID, stable operation ID, capability/version, method,
deadline, and parameters. They produce zero or more ordered chunks and exactly
one result. &lt;code&gt;action.v1&lt;/code&gt; permits only the terminal result, without chunks.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;The server keeps one supervised serve process per enabled local registration and
rechecks its approved checksum before launch. Disable, removal, or conflict stops
it. Crashes and protocol failures settle callers without replaying their calls.
Restart delays start at 100 ms and double to a five-second cap, with at most five
automatic restarts per supervised lifetime. A successful handshake does not reset
that budget. Exhaustion leaves durable unhealthy state across ocman restarts;
&lt;strong&gt;Retry&lt;/strong&gt;, &lt;strong&gt;Restart&lt;/strong&gt;, or explicit enablement resets it.&lt;/p&gt;
&lt;p&gt;Calls respect advertised concurrency, at most 256. Deadlines or cancellation send
a cancel frame; a plugin that fails to finish within the one-second cancellation
grace is terminated. Shutdown sends a shutdown frame, allows one second to exit,
then kills and reaps the process group. Frames are limited to 1 MiB, general call
output to 8 MiB, and chunk/event queues are bounded. Invalid framing, stream
ordering, or slow-consumer overflow terminates the process.&lt;/p&gt;
&lt;p&gt;The &lt;a href="https://forgejo.nousefreak.be/dries/ocman/src/branch/main/internal/plugins/README.md" target="_blank" rel="noopener"&gt;wire contract&lt;/a&gt;
defines exact frames, validation rules, error categories, and HTTP endpoints.&lt;/p&gt;
&lt;h2&gt;Authoring action.v1&lt;span class="hx:absolute hx:-mt-20" id="authoring-actionv1"&gt;&lt;/span&gt;
&lt;a href="#authoring-actionv1" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Advertise capability &lt;code&gt;{&amp;quot;name&amp;quot;:&amp;quot;action&amp;quot;,&amp;quot;version&amp;quot;:{&amp;quot;major&amp;quot;:1,&amp;quot;minor&amp;quot;:0}}&lt;/code&gt;
and declare actions in the description. For example:&lt;/p&gt;
&lt;div class="hextra-code-block hx:relative hx:mt-6 hx:first:mt-0 hx:group/code"&gt;
&lt;div&gt;&lt;div class="highlight"&gt;&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-json" data-lang="json"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="p"&gt;{&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; &lt;span class="nt"&gt;&amp;#34;id&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt; &lt;span class="s2"&gt;&amp;#34;report&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; &lt;span class="nt"&gt;&amp;#34;label&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt; &lt;span class="s2"&gt;&amp;#34;Create report&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; &lt;span class="nt"&gt;&amp;#34;placement&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt; &lt;span class="s2"&gt;&amp;#34;session&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; &lt;span class="nt"&gt;&amp;#34;requiredGrants&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt; &lt;span class="p"&gt;[&lt;/span&gt;&lt;span class="s2"&gt;&amp;#34;context.session&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;],&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; &lt;span class="nt"&gt;&amp;#34;surfaces&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt; &lt;span class="p"&gt;[&lt;/span&gt;&lt;span class="s2"&gt;&amp;#34;command-palette&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;],&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; &lt;span class="nt"&gt;&amp;#34;confirmation&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt; &lt;span class="s2"&gt;&amp;#34;Create a report for this session?&amp;#34;&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="p"&gt;}&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class="hextra-code-copy-btn-container hx:opacity-0 hx:transition hx:group-hover/code:opacity-100 hx:flex hx:gap-1 hx:absolute hx:m-[11px] hx:right-0 hx:top-0"&gt;
&lt;button
class="hextra-code-copy-btn hx:group/copybtn hx:cursor-pointer hx:transition-all hx:active:opacity-50 hx:bg-primary-700/5 hx:border hx:border-black/5 hx:text-gray-600 hx:hover:text-gray-900 hx:rounded-md hx:p-1.5 hx:dark:bg-primary-300/10 hx:dark:border-white/10 hx:dark:text-gray-400 hx:dark:hover:text-gray-50"
title="Copy code"
aria-label="Copy code"
data-copied-label="Copied!"
&gt;
&lt;div class="hextra-copy-icon hx:group-[.copied]/copybtn:hidden hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;div class="hextra-success-icon hx:hidden hx:group-[.copied]/copybtn:block hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;/button&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;p&gt;Also include &lt;code&gt;context.session&lt;/code&gt; in the plugin&amp;rsquo;s &lt;code&gt;requestedGrants&lt;/code&gt;. Placements are
&lt;code&gt;global&lt;/code&gt;, &lt;code&gt;project&lt;/code&gt;, and &lt;code&gt;session&lt;/code&gt;; &lt;code&gt;command-palette&lt;/code&gt; is the only current surface.
Each invocation uses method &lt;code&gt;invoke&lt;/code&gt; with parameters such as:&lt;/p&gt;
&lt;div class="hextra-code-block hx:relative hx:mt-6 hx:first:mt-0 hx:group/code"&gt;
&lt;div&gt;&lt;div class="highlight"&gt;&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-json" data-lang="json"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="p"&gt;{&lt;/span&gt;&lt;span class="nt"&gt;&amp;#34;actionId&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="s2"&gt;&amp;#34;report&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt;&lt;span class="nt"&gt;&amp;#34;context&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;:{&lt;/span&gt;&lt;span class="nt"&gt;&amp;#34;sessionId&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="s2"&gt;&amp;#34;ses-1&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;}}&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class="hextra-code-copy-btn-container hx:opacity-0 hx:transition hx:group-hover/code:opacity-100 hx:flex hx:gap-1 hx:absolute hx:m-[11px] hx:right-0 hx:top-0"&gt;
&lt;button
class="hextra-code-copy-btn hx:group/copybtn hx:cursor-pointer hx:transition-all hx:active:opacity-50 hx:bg-primary-700/5 hx:border hx:border-black/5 hx:text-gray-600 hx:hover:text-gray-900 hx:rounded-md hx:p-1.5 hx:dark:bg-primary-300/10 hx:dark:border-white/10 hx:dark:text-gray-400 hx:dark:hover:text-gray-50"
title="Copy code"
aria-label="Copy code"
data-copied-label="Copied!"
&gt;
&lt;div class="hextra-copy-icon hx:group-[.copied]/copybtn:hidden hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;div class="hextra-success-icon hx:hidden hx:group-[.copied]/copybtn:block hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;/button&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;p&gt;There are no arbitrary action parameters. The broker sends only context covered
by both the action&amp;rsquo;s required grants and current user approvals:&lt;/p&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Grant&lt;/th&gt;
&lt;th&gt;Supplied context&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;context.owner&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;Opaque owner ID&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;context.project&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;Opaque project ID, never a directory&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;context.session&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;Opaque session ID&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;context.route&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;Core route name without parameters or query strings&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;context.selection&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;Up to 100 project/session references, never selected text&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;These references provide no API access to transcripts, credentials, databases,
or filesystem paths. The host handles confirmation before dispatch. The plugin
receives a maximum 30-second deadline and must honor cancellation.&lt;/p&gt;
&lt;p&gt;Return &lt;code&gt;{&amp;quot;results&amp;quot;:[...]}&lt;/code&gt; with 1–16 typed items, at most 1 MiB total:&lt;/p&gt;
&lt;div class="hextra-code-block hx:relative hx:mt-6 hx:first:mt-0 hx:group/code"&gt;
&lt;div&gt;&lt;div class="highlight"&gt;&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-json" data-lang="json"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="p"&gt;{&lt;/span&gt;&lt;span class="nt"&gt;&amp;#34;results&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;:[{&lt;/span&gt;&lt;span class="nt"&gt;&amp;#34;kind&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="s2"&gt;&amp;#34;notice&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt;&lt;span class="nt"&gt;&amp;#34;text&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="s2"&gt;&amp;#34;Report ready&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;}]}&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class="hextra-code-copy-btn-container hx:opacity-0 hx:transition hx:group-hover/code:opacity-100 hx:flex hx:gap-1 hx:absolute hx:m-[11px] hx:right-0 hx:top-0"&gt;
&lt;button
class="hextra-code-copy-btn hx:group/copybtn hx:cursor-pointer hx:transition-all hx:active:opacity-50 hx:bg-primary-700/5 hx:border hx:border-black/5 hx:text-gray-600 hx:hover:text-gray-900 hx:rounded-md hx:p-1.5 hx:dark:bg-primary-300/10 hx:dark:border-white/10 hx:dark:text-gray-400 hx:dark:hover:text-gray-50"
title="Copy code"
aria-label="Copy code"
data-copied-label="Copied!"
&gt;
&lt;div class="hextra-copy-icon hx:group-[.copied]/copybtn:hidden hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;div class="hextra-success-icon hx:hidden hx:group-[.copied]/copybtn:block hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;/button&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;p&gt;Other result kinds are HTTP(S) &lt;code&gt;link&lt;/code&gt;, base64 &lt;code&gt;artifact&lt;/code&gt;, static core &lt;code&gt;navigation&lt;/code&gt;,
and &lt;code&gt;refresh&lt;/code&gt; hints. The host renders text, validates URLs and targets, and
replaces artifact bytes with authorized download handles. HTML, JavaScript,
arbitrary routes, filesystem download paths, and unknown result fields are rejected.
Failures return a safe error category rather than diagnostic text.&lt;/p&gt;
&lt;p&gt;Keep the same operation ID across confirmation and retries. The owner deduplicates
concurrent calls and commits a receipt before dispatch. Reusing an ID with changed
input conflicts; after a host restart, a prior receipt also conflicts instead of
repeating an uncertain side effect. Results and artifacts are memory-only, and
their reads recheck grants. Never automatically retry a failed action with a new
operation ID.&lt;/p&gt;
&lt;h2&gt;Authoring conversation.v1&lt;span class="hx:absolute hx:-mt-20" id="authoring-conversationv1"&gt;&lt;/span&gt;
&lt;a href="#authoring-conversationv1" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Advertise capability &lt;code&gt;{&amp;quot;name&amp;quot;:&amp;quot;conversation&amp;quot;,&amp;quot;version&amp;quot;:{&amp;quot;major&amp;quot;:1,&amp;quot;minor&amp;quot;:0}}&lt;/code&gt;.
A conversation plugin must use execution scope &lt;code&gt;owner&lt;/code&gt;, request the
&lt;code&gt;conversation.session&lt;/code&gt; grant, and declare a required, non-secret &lt;code&gt;project&lt;/code&gt;
setting. Ocman refuses a declaration missing any of the three, so the project you
approve in &lt;strong&gt;Configure&lt;/strong&gt; is always the one project the plugin can reach.&lt;/p&gt;
&lt;p&gt;The capability has exactly two moves. Inbound, the plugin emits an unsolicited
&lt;code&gt;message&lt;/code&gt; event:&lt;/p&gt;
&lt;div class="hextra-code-block hx:relative hx:mt-6 hx:first:mt-0 hx:group/code"&gt;
&lt;div&gt;&lt;div class="highlight"&gt;&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-json" data-lang="json"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="p"&gt;{&lt;/span&gt;&lt;span class="nt"&gt;&amp;#34;type&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="s2"&gt;&amp;#34;event&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt;&lt;span class="nt"&gt;&amp;#34;event&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;:{&lt;/span&gt;&lt;span class="nt"&gt;&amp;#34;capability&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="s2"&gt;&amp;#34;conversation&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt;&lt;span class="nt"&gt;&amp;#34;name&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="s2"&gt;&amp;#34;message&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; &lt;span class="nt"&gt;&amp;#34;data&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;:{&lt;/span&gt;&lt;span class="nt"&gt;&amp;#34;accountId&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="s2"&gt;&amp;#34;T0WORKSPACE&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt;&lt;span class="nt"&gt;&amp;#34;threadId&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="s2"&gt;&amp;#34;C123:1700000000.000100&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; &lt;span class="nt"&gt;&amp;#34;eventId&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="s2"&gt;&amp;#34;Ev0A1B2C3&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt;&lt;span class="nt"&gt;&amp;#34;text&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="s2"&gt;&amp;#34;ship it&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;}}}&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class="hextra-code-copy-btn-container hx:opacity-0 hx:transition hx:group-hover/code:opacity-100 hx:flex hx:gap-1 hx:absolute hx:m-[11px] hx:right-0 hx:top-0"&gt;
&lt;button
class="hextra-code-copy-btn hx:group/copybtn hx:cursor-pointer hx:transition-all hx:active:opacity-50 hx:bg-primary-700/5 hx:border hx:border-black/5 hx:text-gray-600 hx:hover:text-gray-900 hx:rounded-md hx:p-1.5 hx:dark:bg-primary-300/10 hx:dark:border-white/10 hx:dark:text-gray-400 hx:dark:hover:text-gray-50"
title="Copy code"
aria-label="Copy code"
data-copied-label="Copied!"
&gt;
&lt;div class="hextra-copy-icon hx:group-[.copied]/copybtn:hidden hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;div class="hextra-success-icon hx:hidden hx:group-[.copied]/copybtn:block hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;/button&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;p&gt;&lt;code&gt;accountId&lt;/code&gt; (the provider workspace) and &lt;code&gt;threadId&lt;/code&gt; (the conversation) are
opaque identities ocman only compares and echoes back. An optional &lt;code&gt;project&lt;/code&gt;
field is a claim, denied unless it matches the configured project; the directory
actually used always comes from configuration. Ocman keeps one managed session
per &lt;code&gt;(plugin, account, thread)&lt;/code&gt; and remembers it in its own database, so every
later message in the thread continues the same session — across a restart of
ocman or of the plugin — and two workspaces that reuse a thread identity stay
isolated.&lt;/p&gt;
&lt;p&gt;&lt;code&gt;eventId&lt;/code&gt; must identify the &lt;em&gt;event&lt;/em&gt;, not the delivery attempt, so that a
provider redelivering the same event repeats it. Ocman drops a repeat: a
duplicate delivery never creates a second session or a second prompt. A mention
that arrives while the session is still working is queued and sent when the turn
finishes, in arrival order — it does not interrupt the running turn.&lt;/p&gt;
&lt;p&gt;Outbound, ocman calls method &lt;code&gt;reply&lt;/code&gt; on the same capability when the session&amp;rsquo;s
turn completes:&lt;/p&gt;
&lt;div class="hextra-code-block hx:relative hx:mt-6 hx:first:mt-0 hx:group/code"&gt;
&lt;div&gt;&lt;div class="highlight"&gt;&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-json" data-lang="json"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="p"&gt;{&lt;/span&gt;&lt;span class="nt"&gt;&amp;#34;accountId&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="s2"&gt;&amp;#34;T0WORKSPACE&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt;&lt;span class="nt"&gt;&amp;#34;threadId&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="s2"&gt;&amp;#34;C123:1700000000.000100&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt;&lt;span class="nt"&gt;&amp;#34;text&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="s2"&gt;&amp;#34;Shipped.&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;}&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class="hextra-code-copy-btn-container hx:opacity-0 hx:transition hx:group-hover/code:opacity-100 hx:flex hx:gap-1 hx:absolute hx:m-[11px] hx:right-0 hx:top-0"&gt;
&lt;button
class="hextra-code-copy-btn hx:group/copybtn hx:cursor-pointer hx:transition-all hx:active:opacity-50 hx:bg-primary-700/5 hx:border hx:border-black/5 hx:text-gray-600 hx:hover:text-gray-900 hx:rounded-md hx:p-1.5 hx:dark:bg-primary-300/10 hx:dark:border-white/10 hx:dark:text-gray-400 hx:dark:hover:text-gray-50"
title="Copy code"
aria-label="Copy code"
data-copied-label="Copied!"
&gt;
&lt;div class="hextra-copy-icon hx:group-[.copied]/copybtn:hidden hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;div class="hextra-success-icon hx:hidden hx:group-[.copied]/copybtn:block hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;/button&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;p&gt;Replies are plain text in v1: only the newest assistant message&amp;rsquo;s text parts,
capped at 64 KiB. Reasoning, tool, and file parts are excluded. The call carries
a 30-second deadline and a stable operation ID. Return &lt;code&gt;{}&lt;/code&gt; on success.&lt;/p&gt;
&lt;h3&gt;Reply delivery&lt;span class="hx:absolute hx:-mt-20" id="reply-delivery"&gt;&lt;/span&gt;
&lt;a href="#reply-delivery" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h3&gt;&lt;p&gt;A completed reply is durable work, not a best-effort call. Ocman writes it to a
delivery outbox before contacting the plugin, so a disconnect, a plugin crash or
a restart of ocman retries the reply instead of losing it. Each delivery has an
immutable id that doubles as its sequence number and is passed to the plugin as
the call&amp;rsquo;s &lt;code&gt;operationId&lt;/code&gt;.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Ordering.&lt;/strong&gt; Replies for one conversation are delivered strictly in
sequence, one at a time: a later reply never overtakes an earlier one. Every
conversation is its own ordering group, so one failing thread never holds up
another&amp;rsquo;s replies.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Duplicate guarantee.&lt;/strong&gt; Delivery is &lt;strong&gt;at-least-once&lt;/strong&gt;. The acknowledgment
happens after the call returns, so a crash in that window replays the same
delivery with the &lt;em&gt;same&lt;/em&gt; &lt;code&gt;operationId&lt;/code&gt;. A provider adapter is expected to
recognize a repeat and not post a second visible message. A repeated idle
edge for the same completed turn, by contrast, never produces a second
delivery: that is deduplicated durably on the host side.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Retries.&lt;/strong&gt; Failures retry with exponential backoff from 5 seconds to a
5-minute ceiling. Provider-specific cooldowns (Slack&amp;rsquo;s &lt;code&gt;Retry-After&lt;/code&gt;) are
absorbed inside the plugin, which knows its provider&amp;rsquo;s limits.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Dead letters.&lt;/strong&gt; After 6 failed attempts a delivery stops retrying and waits
for a decision under &lt;strong&gt;Settings → Plugins → Reply delivery&lt;/strong&gt;: &lt;em&gt;Retry
delivery&lt;/em&gt; puts it back at the head of its conversation, &lt;em&gt;Discard reply&lt;/em&gt; drops
it and lets the conversation continue. Its own conversation stays paused until
then, rather than delivering out of order.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Backpressure.&lt;/strong&gt; The backlog is capped per plugin (500 undelivered replies,
8 MiB of reply text). At either cap ocman stops accepting &lt;em&gt;new&lt;/em&gt; conversation
messages — visibly, in the same panel — instead of dropping replies it already
owes. A message refused this way is not marked as seen, so the provider&amp;rsquo;s
redelivery is accepted once the backlog drains.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Retry and discard are privileged, localhost-only controls, like every other
plugin mutation.&lt;/p&gt;
&lt;p&gt;A revoked grant, a disabled plugin, or an unresolvable project denies both
directions, and denials never reach the provider. A denial that arrives while a
reply is already owed pauses that reply rather than failing it: the delivery
stays queued, unattempted, and keeps its full retry budget, so disabling a
connector for an afternoon does not turn every owed reply into a dead letter.
Re-enabling it, or restoring the grant, delivers exactly the replies that were
owed — nothing that was denied in between is replayed, because nothing was
attempted. Mappings, queued replies and private data are never deleted by a
denial.&lt;/p&gt;
&lt;h3&gt;Attention notices&lt;span class="hx:absolute hx:-mt-20" id="attention-notices"&gt;&lt;/span&gt;
&lt;a href="#attention-notices" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h3&gt;&lt;p&gt;A thread only ever sees completed answers, so without help a session blocked on
a permission prompt looks exactly like one that is still thinking. Ocman posts a
short notice into the thread when its session cannot proceed on its own, or when
a turn ends somewhere other than an answer:&lt;/p&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Notice&lt;/th&gt;
&lt;th&gt;When&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;Needs a permission decision&lt;/td&gt;
&lt;td&gt;A permission prompt has been left for you — either auto-approve is off for the session, or it declined to approve&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Waiting on an answer&lt;/td&gt;
&lt;td&gt;The agent asked a question, which is never auto-answered&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Ended with an error&lt;/td&gt;
&lt;td&gt;The last turn failed&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Could not be delivered&lt;/td&gt;
&lt;td&gt;A message was accepted from the provider but no session could be reached for it&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;Four things are worth knowing about them.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Decisions still happen in ocman.&lt;/strong&gt; A notice is a pointer, never a prompt: it
carries a sentence and a link, and there is no way to approve, deny or answer
anything from the thread. Permission provenance and the auto-approve rules are
untouched — a thread cannot become a second approval channel.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;They follow the auto-approve decision, not the prompt.&lt;/strong&gt; A permission the
judge approves by itself produces no notice at all. Notices only appear once
ocman has established that a prompt is genuinely yours to answer, so a session
with auto-approve on stays quiet through the work it can do unattended.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;They carry no session content.&lt;/strong&gt; A notice never includes the permission text,
the command, the patterns, the tool metadata, the error message or any part of
the transcript — a provider thread is usually a wider audience than the
session&amp;rsquo;s own operator. To see what is being asked, follow the link.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;They resolve, they are not retracted.&lt;/strong&gt; Nothing posted into a thread is ever
edited or deleted. A notice is resolved by what follows it: answer the prompt in
ocman, the turn continues, and the completed reply lands underneath. Each notice
is keyed on the prompt&amp;rsquo;s own request id, so re-observing the same prompt — after
a stream reconnect, a replayed notification, or an ocman restart — adds nothing.
A second, different prompt in the same session is a new notice.&lt;/p&gt;
&lt;h3&gt;Linking back: set a public base URL&lt;span class="hx:absolute hx:-mt-20" id="linking-back-set-a-public-base-url"&gt;&lt;/span&gt;
&lt;a href="#linking-back-set-a-public-base-url" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h3&gt;&lt;p&gt;Every notice links to the session it is about. Ocman builds that link from
&lt;code&gt;--public-base-url&lt;/code&gt; / &lt;code&gt;OCMAN_PUBLIC_BASE_URL&lt;/code&gt; (see
&lt;a href="../../configuration/"&gt;Configuration&lt;/a&gt;
). &lt;strong&gt;Set it before pointing a conversation
plugin at a provider.&lt;/strong&gt; Without it ocman falls back to its own listen address,
which is normally &lt;code&gt;http://localhost:8228&lt;/code&gt; — fine for you on this machine, and
useless to anyone else reading the thread.&lt;/p&gt;
&lt;p&gt;Use the address a reader&amp;rsquo;s browser would use: your Tailscale or reverse-proxy
hostname, for example &lt;code&gt;OCMAN_PUBLIC_BASE_URL=https://ocman.example.ts.net&lt;/code&gt;. A
base URL with a subpath works too. Ocman does not publish anything at that
address on your behalf — the value only shapes the links it writes.&lt;/p&gt;
&lt;h2&gt;Slack&lt;span class="hx:absolute hx:-mt-20" id="slack"&gt;&lt;/span&gt;
&lt;a href="#slack" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;See &lt;a href="../slack/"&gt;Slack integration&lt;/a&gt;
for the reusable app manifest, setup,
configuration, security model, and acceptance walkthrough.&lt;/p&gt;
&lt;h2&gt;Remote ownership&lt;span class="hx:absolute hx:-mt-20" id="remote-ownership"&gt;&lt;/span&gt;
&lt;a href="#remote-ownership" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Every installation is identified by its owner and plugin ID. Install the binary
on each machine that needs it; the hub neither copies executables nor persists
remote secrets. Settings projects the selected owner&amp;rsquo;s catalog, grants,
configuration, health, and redacted stderr over authenticated gRPC. Secret updates
pass through the hub to the owner, while responses expose only presence flags.&lt;/p&gt;
&lt;p&gt;For project/session contexts, owner-scoped actions run on the project owner and
hub-scoped actions run on the hub. Global placements resolve to the hub, as does
the &lt;code&gt;global&lt;/code&gt; execution scope. The installation&amp;rsquo;s &lt;code&gt;ownerId&lt;/code&gt; is distinct from the
project/session &lt;code&gt;context.ownerId&lt;/code&gt;. A disconnected explicit owner fails closed,
including when a hub action targets its project. Older remotes without plugin
management show an unavailable state.&lt;/p&gt;
&lt;p&gt;The browser talks only to the hub. Remote calls use the closed &lt;code&gt;PluginOperation&lt;/code&gt;
RPC, not a raw plugin protocol tunnel. Each owner applies its own grants,
confirmation, operation receipts, process supervision, and artifact checks.&lt;/p&gt;
&lt;p&gt;Conversation plugins are owner-scoped by declaration, so a connector belongs to
the machine it is installed on. Give each installation its own provider app and
credentials: two machines configured with the same Slack app would both open a
Socket Mode connection and each receive an arbitrary half of the events. Thread
mappings, inbound deduplication, and reply delivery all live in the owner&amp;rsquo;s own
&lt;code&gt;state.db&lt;/code&gt;, and a conversation&amp;rsquo;s session is always created on that owner — never
on a machine inferred from the project inventory, and never on the hub as a
fallback. The hub&amp;rsquo;s only role is projecting Settings, which fails visibly when
the owner is disconnected.&lt;/p&gt;
&lt;h2&gt;Diagnostics and removal&lt;span class="hx:absolute hx:-mt-20" id="diagnostics-and-removal"&gt;&lt;/span&gt;
&lt;a href="#diagnostics-and-removal" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Settings lists up to 128 rejected executable filenames and safe host errors from
the latest scan, including invalid descriptions, timeouts, and identity changes.
Rescanning replaces this list; plugin stdout is never included in diagnostics.&lt;/p&gt;
&lt;p&gt;Use &lt;strong&gt;Refresh health&lt;/strong&gt; to inspect status, restart count, and the safe last error.
&lt;strong&gt;Load recent stderr&lt;/strong&gt; shows bounded output from the current and most recently
stopped process. Each capture is capped at 64 KiB and redacts launch tokens and
current/historical secrets. Raw stderr stays out of SQL, telemetry, and API errors.
Redaction cannot protect secrets deliberately encoded by trusted native code.&lt;/p&gt;
&lt;p&gt;Management mutations and stderr reads require a loopback peer and safe browser
origin in addition to configured authentication. If those controls fail through
a reverse proxy, use ocman&amp;rsquo;s local endpoint. Action endpoints use normal
authentication and origin protection.&lt;/p&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Symptom&lt;/th&gt;
&lt;th&gt;Check&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;No plugin discovered&lt;/td&gt;
&lt;td&gt;Selected owner, directory override, executable name/permission, regular file, describe handshake and exit&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Conflict or approval lost&lt;/td&gt;
&lt;td&gt;Duplicate IDs, changed binary/declaration, attempted ID change at a registered path; correct files and rescan&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Enabled but unhealthy&lt;/td&gt;
&lt;td&gt;Recent stderr, configuration initialization, stdout framing, cancellation; repair and explicitly retry&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Action missing&lt;/td&gt;
&lt;td&gt;Placement, selected owner, negotiated capability, enablement, and required grants&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Owner unavailable&lt;/td&gt;
&lt;td&gt;Remote connection and that owner&amp;rsquo;s plugin-management support&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;&lt;strong&gt;Revoke grants&lt;/strong&gt; clears approvals and stops in-flight work before restarting the
process. It denies affected actions and cached results, but cannot recall context
already delivered. Use &lt;strong&gt;Disable&lt;/strong&gt; to stop execution while retaining configuration,
grants, and private data.&lt;/p&gt;
&lt;p&gt;To uninstall, disable the plugin, remove its executable on the owner, and rescan.
Missing registrations remain visible as removed, retaining configuration and data.
To erase those too, use &lt;strong&gt;Remove data&lt;/strong&gt; and confirm permanent removal while the
plugin is disabled. This deletes configuration, secrets, grants, and private data;
it does not delete the executable. A remaining executable is rediscovered as
disabled on the next scan. Operation receipts survive deletion to prevent
uncertain action replay.&lt;/p&gt;
&lt;h2&gt;Development workflow&lt;span class="hx:absolute hx:-mt-20" id="development-workflow"&gt;&lt;/span&gt;
&lt;a href="#development-workflow" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Use the optional &lt;a href="https://forgejo.nousefreak.be/dries/ocman/src/branch/main/sdk/plugin/README.md" target="_blank" rel="noopener"&gt;Go SDK&lt;/a&gt;
at &lt;code&gt;github.com/NoUseFreak/ocman/sdk/plugin&lt;/code&gt;, or implement the NDJSON contract in
another language. &lt;code&gt;plugin.Run&lt;/code&gt; handles the lifecycle and &lt;code&gt;plugin.ActionHandler&lt;/code&gt;
validates action dispatch/results. &lt;code&gt;plugin.RunWithEvents&lt;/code&gt; adds a plugin-initiated
event source, and &lt;code&gt;plugin.ConversationHandler&lt;/code&gt; plus &lt;code&gt;plugin.NewConversationMessage&lt;/code&gt;
cover both conversation directions. Plugins with settings read fd 3 before calling
&lt;code&gt;Run&lt;/code&gt; in serve mode. The deterministic
&lt;a href="https://forgejo.nousefreak.be/dries/ocman/src/branch/main/examples/ocman-plugin-fixture/main.go" target="_blank" rel="noopener"&gt;fixture&lt;/a&gt;
demonstrates both modes, actions, cancellation, and safe failures.&lt;/p&gt;
&lt;p&gt;From the ocman checkout, run:&lt;/p&gt;
&lt;div class="hextra-code-block hx:relative hx:mt-6 hx:first:mt-0 hx:group/code"&gt;
&lt;div&gt;&lt;div class="highlight"&gt;&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-sh" data-lang="sh"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;go &lt;span class="nb"&gt;test&lt;/span&gt; ./sdk/plugin/... ./internal/plugins
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;go &lt;span class="nb"&gt;test&lt;/span&gt; ./internal/server ./internal/remote ./internal/state -run Plugin&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class="hextra-code-copy-btn-container hx:opacity-0 hx:transition hx:group-hover/code:opacity-100 hx:flex hx:gap-1 hx:absolute hx:m-[11px] hx:right-0 hx:top-0"&gt;
&lt;button
class="hextra-code-copy-btn hx:group/copybtn hx:cursor-pointer hx:transition-all hx:active:opacity-50 hx:bg-primary-700/5 hx:border hx:border-black/5 hx:text-gray-600 hx:hover:text-gray-900 hx:rounded-md hx:p-1.5 hx:dark:bg-primary-300/10 hx:dark:border-white/10 hx:dark:text-gray-400 hx:dark:hover:text-gray-50"
title="Copy code"
aria-label="Copy code"
data-copied-label="Copied!"
&gt;
&lt;div class="hextra-copy-icon hx:group-[.copied]/copybtn:hidden hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;div class="hextra-success-icon hx:hidden hx:group-[.copied]/copybtn:block hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;/button&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;p&gt;External plugins can use &lt;code&gt;sdk/plugin/conformance.Run&lt;/code&gt; for executable lifecycle
checks, &lt;code&gt;RunActionGrants&lt;/code&gt; for broker grant, minimization, deduplication, and
revocation checks, and &lt;code&gt;RunConversationGrants&lt;/code&gt; for the conversation declaration
contract plus grant and project denials. Supply deterministic, side-effect-free
test calls. Rebuild, rescan, review the new checksum, and enable again for a
manual palette check.&lt;/p&gt;
&lt;h2&gt;Future work&lt;span class="hx:absolute hx:-mt-20" id="future-work"&gt;&lt;/span&gt;
&lt;a href="#future-work" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;The current release supports native processes, &lt;code&gt;action.v1&lt;/code&gt;, and
&lt;code&gt;conversation.v1&lt;/code&gt;. The following are future work, not available plugin
capabilities or integrations:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Platform-provider capability.&lt;/li&gt;
&lt;li&gt;Iframe UI contributions.&lt;/li&gt;
&lt;li&gt;Relay inbox capability, separate from existing core Inbox and webhook features.&lt;/li&gt;
&lt;li&gt;A plugin registry and automatic updates.&lt;/li&gt;
&lt;li&gt;Publisher signatures and process sandboxing.&lt;/li&gt;
&lt;li&gt;A Codex provider.&lt;/li&gt;
&lt;li&gt;Conversation images, attachments, streaming replies, more than one project per
plugin, and answering permission prompts from the thread.&lt;/li&gt;
&lt;/ul&gt;</description></item><item><title>Slack integration</title><link>https://ocman.stenic.io/docs/features/slack/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://ocman.stenic.io/docs/features/slack/</guid><description>
&lt;p&gt;The bundled Slack plugin connects a Slack thread to an ocman session over
Socket Mode. It needs no inbound network exposure and no relay. Channel messages
require an explicit &lt;code&gt;@ocman&lt;/code&gt; mention. Allowlisted users can also talk to ocman
from the app&amp;rsquo;s Messages tab without mentioning it.&lt;/p&gt;
&lt;h2&gt;Create the Slack app&lt;span class="hx:absolute hx:-mt-20" id="create-the-slack-app"&gt;&lt;/span&gt;
&lt;a href="#create-the-slack-app" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Create an app &lt;strong&gt;from a manifest&lt;/strong&gt; and paste
&lt;a href="https://forgejo.nousefreak.be/dries/ocman/src/branch/main/examples/ocman-plugin-slack/slack-app-manifest.yaml" target="_blank" rel="noopener"&gt;&lt;code&gt;examples/ocman-plugin-slack/slack-app-manifest.yaml&lt;/code&gt;&lt;/a&gt;
.
The manifest is the configuration blueprint and enables:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Socket Mode.&lt;/li&gt;
&lt;li&gt;The App Home Messages tab and AI Assistant view.&lt;/li&gt;
&lt;li&gt;&lt;code&gt;app_mention&lt;/code&gt; events for channels and &lt;code&gt;message.im&lt;/code&gt; events for direct chats.&lt;/li&gt;
&lt;li&gt;The exact bot scopes: &lt;code&gt;app_mentions:read&lt;/code&gt;, &lt;code&gt;assistant:write&lt;/code&gt;, &lt;code&gt;chat:write&lt;/code&gt;, and
&lt;code&gt;im:history&lt;/code&gt;.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;The plugin reads no channel history. &lt;code&gt;assistant:write&lt;/code&gt; only displays
&lt;code&gt;Thinking...&lt;/code&gt; while a turn runs; the plugin clears it after posting the reply.&lt;/p&gt;
&lt;p&gt;After creating the app:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Under &lt;strong&gt;Basic Information → App-Level Tokens&lt;/strong&gt;, generate a token with the
&lt;code&gt;connections:write&lt;/code&gt; scope. It starts with &lt;code&gt;xapp-&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;Under &lt;strong&gt;Install App&lt;/strong&gt;, install the app to the workspace and copy the bot
token. It starts with &lt;code&gt;xoxb-&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;If Slack changed any scope after installation, reinstall the app and use the
newly displayed bot token.&lt;/li&gt;
&lt;li&gt;Invite the bot to each channel where it should receive mentions.&lt;/li&gt;
&lt;/ol&gt;
&lt;h2&gt;Install and configure&lt;span class="hx:absolute hx:-mt-20" id="install-and-configure"&gt;&lt;/span&gt;
&lt;a href="#install-and-configure" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Build and install the executable on the machine that owns the project:&lt;/p&gt;
&lt;div class="hextra-code-block hx:relative hx:mt-6 hx:first:mt-0 hx:group/code"&gt;
&lt;div&gt;&lt;div class="highlight"&gt;&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-sh" data-lang="sh"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;make install-plugin &lt;span class="nv"&gt;PLUGIN&lt;/span&gt;&lt;span class="o"&gt;=&lt;/span&gt;slack&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class="hextra-code-copy-btn-container hx:opacity-0 hx:transition hx:group-hover/code:opacity-100 hx:flex hx:gap-1 hx:absolute hx:m-[11px] hx:right-0 hx:top-0"&gt;
&lt;button
class="hextra-code-copy-btn hx:group/copybtn hx:cursor-pointer hx:transition-all hx:active:opacity-50 hx:bg-primary-700/5 hx:border hx:border-black/5 hx:text-gray-600 hx:hover:text-gray-900 hx:rounded-md hx:p-1.5 hx:dark:bg-primary-300/10 hx:dark:border-white/10 hx:dark:text-gray-400 hx:dark:hover:text-gray-50"
title="Copy code"
aria-label="Copy code"
data-copied-label="Copied!"
&gt;
&lt;div class="hextra-copy-icon hx:group-[.copied]/copybtn:hidden hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;div class="hextra-success-icon hx:hidden hx:group-[.copied]/copybtn:block hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;/button&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;p&gt;A conversation plugin is owner-scoped. The binary, tokens, and project all live
on that host; the hub never copies them. In &lt;strong&gt;Settings → Plugins&lt;/strong&gt;, select that
owner, rescan, and configure the Slack plugin:&lt;/p&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Setting&lt;/th&gt;
&lt;th&gt;Value&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;project&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;Absolute path of the one project sessions may run in&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;appToken&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;App-level &lt;code&gt;xapp-&lt;/code&gt; token&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;botToken&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;Bot &lt;code&gt;xoxb-&lt;/code&gt; token&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;allowedUsers&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;Comma-separated Slack member IDs allowed to drive sessions&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;agent&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;Optional OpenCode agent, such as &lt;code&gt;build&lt;/code&gt;&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;model&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;Optional &lt;code&gt;provider/model&lt;/code&gt; for a new Slack thread&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;Enable the plugin and approve the &lt;code&gt;conversation.session&lt;/code&gt; grant. Set
&lt;code&gt;OCMAN_PUBLIC_BASE_URL&lt;/code&gt; to the address Slack users can open before using the
integration; attention notices link back to that address.&lt;/p&gt;
&lt;h2&gt;Conversation behavior&lt;span class="hx:absolute hx:-mt-20" id="conversation-behavior"&gt;&lt;/span&gt;
&lt;a href="#conversation-behavior" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;&lt;code&gt;@ocman ship it&lt;/code&gt; in a channel or channel thread starts a session. A direct
message starts one without a mention. The first message becomes the Slack thread
root, completed replies stay in that thread, and follow-ups in the same thread
continue the same durable ocman session. A new top-level direct message starts a
new session.&lt;/p&gt;
&lt;p&gt;Messages accepted while a turn is running are queued and sent to the session in
arrival order. Permission prompts, questions, and turn failures post a fixed
notice linking to ocman; Slack never receives permission details, errors, tool
output, or hidden model reasoning.&lt;/p&gt;
&lt;p&gt;Both tokens use write-only secret handling. They are delivered only on file
descriptor 3, never appear in results or error text, and are redacted from
captured stderr. &lt;code&gt;allowedUsers&lt;/code&gt; fails closed: an empty list authorizes nobody.
Bot messages, message edits, and unauthorized users are dropped silently.&lt;/p&gt;
&lt;p&gt;Reply delivery is durable and at-least-once. Slack &lt;code&gt;429&lt;/code&gt; responses honor
&lt;code&gt;Retry-After&lt;/code&gt;. An uncertain transport or Slack 5xx outcome is not immediately
repeated because a duplicate visible reply is worse than one that may already
have landed.&lt;/p&gt;
&lt;h2&gt;Verify the installation&lt;span class="hx:absolute hx:-mt-20" id="verify-the-installation"&gt;&lt;/span&gt;
&lt;a href="#verify-the-installation" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Walk these steps in order before using a shared channel:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;Installed.&lt;/strong&gt; &lt;code&gt;make install-plugin PLUGIN=slack&lt;/code&gt; prints the path and SHA-256.
Rescan must show &lt;code&gt;org.ocman.slack&lt;/code&gt; as disabled with the same checksum.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Configured.&lt;/strong&gt; Save the project and tokens, enable the plugin, and approve
&lt;code&gt;conversation.session&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Connected.&lt;/strong&gt; &lt;strong&gt;Refresh health&lt;/strong&gt; should report healthy. Recent stderr should
contain &lt;code&gt;slack socket connected&lt;/code&gt;, never tokens or message content.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Channel.&lt;/strong&gt; Mention &lt;code&gt;@ocman&lt;/code&gt; in an invited channel. A session appears and
the completed reply lands in the same thread.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Direct chat.&lt;/strong&gt; Send a message from an allowlisted account in the app&amp;rsquo;s
Messages tab. The reply remains in that assistant thread.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Follow-up.&lt;/strong&gt; Send another message in the same thread while a turn runs. It
is answered after that turn rather than interleaved into it.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Restart.&lt;/strong&gt; Restart ocman and continue the thread. It should reuse the same
session and deliver any reply owed before the restart.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;If nothing happens, confirm the channel invite, member ID allowlist, Socket Mode,
event subscriptions, and reinstallation after scope changes. Then inspect
&lt;strong&gt;Refresh health&lt;/strong&gt; and &lt;strong&gt;Load recent stderr&lt;/strong&gt;. &lt;code&gt;invalid_auth&lt;/code&gt; indicates a token
problem; an &lt;code&gt;assistant.threads.setStatus&lt;/code&gt; error affects only the thinking status,
not prompt or reply delivery.&lt;/p&gt;
&lt;h2&gt;Test and remove&lt;span class="hx:absolute hx:-mt-20" id="test-and-remove"&gt;&lt;/span&gt;
&lt;a href="#test-and-remove" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;The automated path uses the real executable with mock Slack Web API and Socket
Mode endpoints:&lt;/p&gt;
&lt;div class="hextra-code-block hx:relative hx:mt-6 hx:first:mt-0 hx:group/code"&gt;
&lt;div&gt;&lt;div class="highlight"&gt;&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-sh" data-lang="sh"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;go &lt;span class="nb"&gt;test&lt;/span&gt; ./examples/ocman-plugin-slack
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;go &lt;span class="nb"&gt;test&lt;/span&gt; ./internal/server -run Slack&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class="hextra-code-copy-btn-container hx:opacity-0 hx:transition hx:group-hover/code:opacity-100 hx:flex hx:gap-1 hx:absolute hx:m-[11px] hx:right-0 hx:top-0"&gt;
&lt;button
class="hextra-code-copy-btn hx:group/copybtn hx:cursor-pointer hx:transition-all hx:active:opacity-50 hx:bg-primary-700/5 hx:border hx:border-black/5 hx:text-gray-600 hx:hover:text-gray-900 hx:rounded-md hx:p-1.5 hx:dark:bg-primary-300/10 hx:dark:border-white/10 hx:dark:text-gray-400 hx:dark:hover:text-gray-50"
title="Copy code"
aria-label="Copy code"
data-copied-label="Copied!"
&gt;
&lt;div class="hextra-copy-icon hx:group-[.copied]/copybtn:hidden hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;div class="hextra-success-icon hx:hidden hx:group-[.copied]/copybtn:block hx:pointer-events-none hx:h-4 hx:w-4"&gt;&lt;/div&gt;
&lt;/button&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;p&gt;These tests cannot validate workspace configuration, tokens, channel membership,
or Slack&amp;rsquo;s rendered UI.&lt;/p&gt;
&lt;p&gt;To remove the integration, disable the plugin, delete &lt;code&gt;ocman-plugin-slack&lt;/code&gt; from
the owner&amp;rsquo;s plugin directory, rescan, and use &lt;strong&gt;Remove data&lt;/strong&gt; to erase tokens,
thread mappings, and undelivered replies. Revoke the tokens or delete the app in
Slack separately.&lt;/p&gt;</description></item><item><title>Inbox</title><link>https://ocman.stenic.io/docs/features/inbox/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://ocman.stenic.io/docs/features/inbox/</guid><description>
&lt;p&gt;The Inbox collects messages that need attention across connected machines.
Press &lt;strong&gt;Alt+I&lt;/strong&gt;, or &lt;strong&gt;Option+I&lt;/strong&gt; on macOS, to open it from anywhere in the app.
The first header row combines fuzzy search with All, Unread, and Archived.
Search matches message titles, bodies, originating sessions, source machines, and permission details.
The second row contains the message categories and an actions dropdown with
Archive selected and Archive all read.
Choose &lt;strong&gt;All&lt;/strong&gt;, &lt;strong&gt;Primary&lt;/strong&gt;, &lt;strong&gt;Factory&lt;/strong&gt;, &lt;strong&gt;Routines&lt;/strong&gt;, or &lt;strong&gt;Permissions&lt;/strong&gt; in the
message-type group. One type is selected at a time; its icon and label are shown,
while the others show icons with hover titles. Each message in the sidebar also
shows its category icon before the title. Older messages appear under Primary.&lt;/p&gt;
&lt;p&gt;Opening a message marks it read. Mark unread returns it to the unread list.
Messages show their source machine in the reading pane, but there is no machine
filter. Every detail header includes the originating session link, or
&amp;ldquo;Originating session unavailable&amp;rdquo; for messages without recorded session metadata.
Archived messages remain readable, but opening them does not mark them read or
change the navigation&amp;rsquo;s unread count. Resolved permission actions are hidden.&lt;/p&gt;
&lt;h2&gt;Permission requests&lt;span class="hx:absolute hx:-mt-20" id="permission-requests"&gt;&lt;/span&gt;
&lt;a href="#permission-requests" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Every observed permission request creates one durable Inbox message on its owning
machine, including requests from subagent sessions and sessions with auto-approval
disabled. Open the message to review its command or patterns and choose &lt;strong&gt;Allow
once&lt;/strong&gt;, &lt;strong&gt;Allow always&lt;/strong&gt;, or &lt;strong&gt;Reject&lt;/strong&gt;. Allow always uses the same confirmation
step as the session&amp;rsquo;s permission prompt. Permission titles include the requesting
session&amp;rsquo;s title. The originating-session link in the header opens that session,
including when it is a subagent. If the session title is unavailable, its ID is shown.&lt;/p&gt;
&lt;p&gt;Successful replies archive the message automatically, whether answered in the
Inbox, a session, another browser, the OpenCode TUI, or by auto-approval. A failed
reply keeps the message available for retry. A background check every 30 seconds
archives requests that disappeared after an aborted turn or a missed event;
failed checks against an unavailable owner leave the message intact.&lt;/p&gt;
&lt;p&gt;Permission toast and system-notification links open the permission-filtered
Inbox. Question notifications still open the session.&lt;/p&gt;
&lt;h2&gt;Other messages&lt;span class="hx:absolute hx:-mt-20" id="other-messages"&gt;&lt;/span&gt;
&lt;a href="#other-messages" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Factory delivery notifications use the Factory category. Routine runs create a
Routine message when they succeed, fail, or are interrupted.&lt;/p&gt;
&lt;p&gt;Agents can use the &lt;code&gt;inbox&lt;/code&gt; MCP tool to send messages with an optional &lt;code&gt;category&lt;/code&gt;
of &lt;code&gt;general&lt;/code&gt;, &lt;code&gt;factory&lt;/code&gt;, or &lt;code&gt;routine&lt;/code&gt;. The default, &lt;code&gt;general&lt;/code&gt;, appears as Primary
in the dashboard. The &lt;code&gt;permission&lt;/code&gt;
category is reserved for actual permission requests and cannot be supplied by
an agent. Include &lt;code&gt;session_id&lt;/code&gt; and the owner-local &lt;code&gt;platform&lt;/code&gt; together when
sending from a session so the header can link back to it. Factory deliveries and
routine completions record their session automatically. Use the tool&amp;rsquo;s &lt;code&gt;help&lt;/code&gt;
action for its current schema.&lt;/p&gt;</description></item><item><title>Read answers aloud</title><link>https://ocman.stenic.io/docs/features/read-aloud/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://ocman.stenic.io/docs/features/read-aloud/</guid><description>
&lt;p&gt;Use the speaker icon at the end of a completed assistant turn to read its final
answer aloud. The icon changes to &lt;strong&gt;Stop&lt;/strong&gt; during playback. The bookmark icon
beside it bookmarks the turn&amp;rsquo;s last assistant message.&lt;/p&gt;
&lt;p&gt;Only the final answer&amp;rsquo;s text is spoken. Reasoning, progress messages, tool calls,
command output, code blocks, images, and compaction summaries are excluded.
Links are read by their labels and inline code is kept, so commands mentioned in
ordinary prose can still be spoken. Failed, interrupted, and tool-only endings
do not have a speaker button.&lt;/p&gt;
&lt;p&gt;Under &lt;strong&gt;Settings → Sessions&lt;/strong&gt;, enable &lt;strong&gt;Read answers aloud&lt;/strong&gt; to automatically
read new answers in the focused session tab. This is off by default and saved
for the current browser. Opening history or reconnecting does not replay old
answers. Playback stops when you send another prompt, leave the session, or
disable automatic reading.&lt;/p&gt;
&lt;p&gt;The same settings offer a voice selector, reading speed, and voice preview.
Voices come from your browser or operating system. Automatic selection prefers
a local voice matching the browser language. Voices marked &lt;strong&gt;online&lt;/strong&gt; may send
answer text to their speech service. Ocman does not run a TTS server or store
audio files.&lt;/p&gt;
&lt;p&gt;Voice availability and automatic playback vary by browser and desktop webview.
If playback is blocked, use the speaker button or preview the voice in Settings.
Browsers without speech synthesis hide the speaker and disable automatic reading.&lt;/p&gt;</description></item><item><title>Database maintenance</title><link>https://ocman.stenic.io/docs/features/maintenance/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://ocman.stenic.io/docs/features/maintenance/</guid><description>
&lt;p&gt;OpenCode&amp;rsquo;s database grows fast. Whenever a turn changes files, OpenCode stores
a full patch of every changed file on that turn&amp;rsquo;s user message. It then stores
the patch again in every &lt;code&gt;message.updated&lt;/code&gt; event in its event log. A checkout
that moves across a large upstream change can add hundreds of megabytes in a
single turn.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Settings → Maintenance&lt;/strong&gt; removes those patches from sessions that haven&amp;rsquo;t
been updated for 30 days, then compacts the database. The patches are kept in
a dump, so you can put them back.&lt;/p&gt;
&lt;h2&gt;What is lost&lt;span class="hx:absolute hx:-mt-20" id="what-is-lost"&gt;&lt;/span&gt;
&lt;a href="#what-is-lost" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Only OpenCode&amp;rsquo;s own web app reads the patches, for its per-turn &amp;ldquo;changes&amp;rdquo;
view. For cleaned sessions, that view, &lt;code&gt;GET /session/{id}/diff?messageID=&lt;/code&gt;, and
the diffs section of &lt;code&gt;opencode export&lt;/code&gt; come back empty.&lt;/p&gt;
&lt;p&gt;These are unaffected:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;ocman&lt;/li&gt;
&lt;li&gt;the OpenCode terminal UI&lt;/li&gt;
&lt;li&gt;reverting a turn, which works from snapshots&lt;/li&gt;
&lt;li&gt;the per-session added, removed and changed-file counts&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Clean up&lt;span class="hx:absolute hx:-mt-20" id="clean-up"&gt;&lt;/span&gt;
&lt;a href="#clean-up" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;&lt;strong&gt;Clean up&lt;/strong&gt; runs these steps and shows each one&amp;rsquo;s progress:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;Check disk space.&lt;/strong&gt; Refuses unless the database&amp;rsquo;s volume has three times
the database size free. That&amp;rsquo;s the worst case for the backup, the dump and
compaction together.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Stop opencode.&lt;/strong&gt; Stops every opencode instance ocman manages and blocks
new launches until the job ends. In-flight turns are interrupted. If any
other process still has the database open (an opencode you started
yourself, a &lt;code&gt;sqlite3&lt;/code&gt; shell), the job lists them and stops without changing
anything.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Back up database.&lt;/strong&gt; Writes a consistent copy to
&lt;code&gt;opencode.db.ocman-backup&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Dump diffs.&lt;/strong&gt; Copies the patches into &lt;code&gt;opencode.db.ocman-diffs&lt;/code&gt;, next to
the database.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Remove diffs.&lt;/strong&gt; Removes only the patches that are in the dump.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Compact database.&lt;/strong&gt; Runs &lt;code&gt;VACUUM&lt;/code&gt; to return the freed space to the disk.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Delete backup.&lt;/strong&gt; Deletes the backup once everything succeeded. After a
failure, the backup stays. It is one self-contained file. To restore it,
stop every opencode, delete &lt;code&gt;opencode.db-wal&lt;/code&gt; and &lt;code&gt;opencode.db-shm&lt;/code&gt;, then
copy the backup over &lt;code&gt;opencode.db&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Relaunch opencode.&lt;/strong&gt; Restarts the instances stopped in step 2. This step
runs even when an earlier step failed.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Running it again later adds newly old sessions to the same dump.&lt;/p&gt;
&lt;h2&gt;Restore and delete&lt;span class="hx:absolute hx:-mt-20" id="restore-and-delete"&gt;&lt;/span&gt;
&lt;a href="#restore-and-delete" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;&lt;strong&gt;Restore&lt;/strong&gt; stops opencode the same way and puts every dumped patch back.
&lt;strong&gt;Delete dump&lt;/strong&gt; removes the dump file for good.&lt;/p&gt;
&lt;p&gt;Both buttons, like Clean up, only work from a browser on the machine running
ocman. Each machine cleans its own database from its own Settings page.&lt;/p&gt;</description></item><item><title/><link>https://ocman.stenic.io/docs/adr/0001-keep-factory-mcp-contract-implementation-neutral/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://ocman.stenic.io/docs/adr/0001-keep-factory-mcp-contract-implementation-neutral/</guid><description>
&lt;h1&gt;Keep the Factory MCP contract implementation-neutral&lt;/h1&gt;&lt;p&gt;The single Factory MCP tool and globally installed Factory skill expose native Epics, Mols, Issues, Plans, Formulas, and domain-level errors. Operational details are excluded from model-facing schemas and results so agents do not couple to storage, filesystem, or process internals; agents use the tool&amp;rsquo;s &lt;code&gt;help&lt;/code&gt; action for its current contract.&lt;/p&gt;</description></item><item><title/><link>https://ocman.stenic.io/docs/adr/0002-build-factory-as-a-native-issue-graph/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://ocman.stenic.io/docs/adr/0002-build-factory-as-a-native-issue-graph/</guid><description>
&lt;h1&gt;Build Factory as a native Issue graph&lt;/h1&gt;&lt;p&gt;Factory owns Epics, composable Mols, typed Issues, dependencies, attempts, Formulas, immutable Plan manifests, and provenance in ocman&amp;rsquo;s state rather than integrating an external issue tracker. This permits atomic revision-bound materialization and outcome-sensitive scheduling under one authority boundary; the trade-off is that ocman owns these semantics and provides no compatibility or migration for the discarded Factory implementation.&lt;/p&gt;</description></item><item><title/><link>https://ocman.stenic.io/docs/adr/0003-separate-plan-approval-from-materialization/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://ocman.stenic.io/docs/adr/0003-separate-plan-approval-from-materialization/</guid><description>
&lt;h1&gt;Approval creates and starts the approved work&lt;/h1&gt;&lt;p&gt;Plan approval binds one exact immutable machine-readable manifest, materializes its implementation Issues and dependencies, and dispatches ready work. The user approves once and receives a toast confirming that work is starting. Materialization remains an internal atomic, idempotent operation so retries cannot create duplicate work. A failed materialization can be retried against the same approved revision. Dispatch still respects paused Epics and capacity limits.&lt;/p&gt;</description></item><item><title/><link>https://ocman.stenic.io/docs/adr/0004-separate-lifecycle-requirement-and-dispatch-state/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://ocman.stenic.io/docs/adr/0004-separate-lifecycle-requirement-and-dispatch-state/</guid><description>
&lt;h1&gt;Separate lifecycle, requirement, outcome, and dispatch state&lt;/h1&gt;&lt;p&gt;Factory records Issue lifecycle status, closure outcome, requirement classification, and derived dispatch state as separate concepts. This avoids treating closure as success or dependency waiting as lifecycle, permits conditional failure branches and accurate progress, and accepts a more explicit transition model in exchange for preventing ambiguous scheduling behavior.&lt;/p&gt;</description></item><item><title/><link>https://ocman.stenic.io/docs/adr/0005-use-human-addressable-factory-graph-identifiers/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://ocman.stenic.io/docs/adr/0005-use-human-addressable-factory-graph-identifiers/</guid><description>
&lt;h1&gt;Use human-addressable Factory graph identifiers&lt;/h1&gt;&lt;p&gt;Factory graph nodes use a Work Epic goal&amp;rsquo;s lowercase ASCII word-initial prefix (up to ten characters, with &lt;code&gt;epic&lt;/code&gt; as fallback), a unique four-character cryptographic base32 suffix, and immutable monotonic child indices. A Work Epic is the root, its first Mol is &lt;code&gt;.1&lt;/code&gt;, and every child appends its index to its parent. This replaces opaque root IDs and Formula-key child IDs so people can recognize, discuss, and navigate graph hierarchy without compromising uniqueness or audit stability.&lt;/p&gt;</description></item><item><title/><link>https://ocman.stenic.io/docs/adr/0006-keep-the-factory-graph-mutable/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://ocman.stenic.io/docs/adr/0006-keep-the-factory-graph-mutable/</guid><description>
&lt;h1&gt;Keep the Factory graph mutable&lt;/h1&gt;&lt;p&gt;Users may create, edit, reparent, link, unlink, and soft-delete unstarted Factory graph work through typed REST and authenticated MCP actions, including local cross-Work Epic dependencies. Mutations globally reject cycles; running and closed work remains structurally immutable, while soft-deletion retains audit history and removes the Issue and its incident edges from the active graph atomically. Plan materialization remains a graph-generation path, not the only graph-write authority.&lt;/p&gt;</description></item><item><title/><link>https://ocman.stenic.io/docs/adr/0007-use-explicit-recovery-gates-for-blocked-implementation/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://ocman.stenic.io/docs/adr/0007-use-explicit-recovery-gates-for-blocked-implementation/</guid><description>
&lt;h1&gt;Use explicit Recovery Gates for blocked implementation&lt;/h1&gt;&lt;p&gt;An implementation agent creates a Recovery Gate through Factory MCP only when it explicitly cannot safely continue. The Gate pauses and preserves its Attempt/session/worktree without consuming capacity; a human may resume it with a response, retry in a fresh Attempt, or cancel it. Ordinary permission prompts remain direct, while requests outside the selected profile create Authority Escalation Gates.&lt;/p&gt;</description></item><item><title/><link>https://ocman.stenic.io/docs/adr/0008-coordinate-factory-work-across-projects/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://ocman.stenic.io/docs/adr/0008-coordinate-factory-work-across-projects/</guid><description>
&lt;h1&gt;Coordinate Factory work across projects&lt;/h1&gt;&lt;p&gt;A Work Epic owns an explicitly admitted set of local projects while retaining its original Epic project as the permanent default. Every executable Issue targets one project; Factory keeps workspace, checkpoint, Delivery, and pull-request lineage per &lt;code&gt;(Epic, project)&lt;/code&gt;, creates Project Deliveries progressively, and uses forge-observed merge gates when downstream work requires an upstream Delivery to merge. Scope can expand only after human admission and local-execution acknowledgement. Execution remains sequential, and non-target write restrictions are agent policy rather than filesystem isolation.&lt;/p&gt;</description></item></channel></rss>