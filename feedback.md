# Feedback

- [ ] Add a pin to session so they show up (in order of when they got pinned) on top of all sessions and as a group
      called pinned when we are in the project grouping view.
- [x] handle frontend performance
- [ ] should we allow a small/lightweight code inspector (read-only, syntax highlighting, like a mini vscode)
- [ ] session sometimes look done, but after a few seconds they continue, the ui should show working when in doubt
      (opencode only)
- [x] we want the 3 working dots to become 5 dots
- [ ] the list indicates a prompt is available, the current session (that has it) does not show it. users have to open
      another sesssion and return or rrefresh in order to see it (opencode only)
- [x] When we stream in some block, the scroll to bottom stops even though we had botom in view before the part got
      added
